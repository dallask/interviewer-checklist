---
phase: 05-scoring-ui-notes-candidate-custom-questions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/storage/migrations/v2-to-v3.ts
  - src/storage/migrations/v2-to-v3.test.ts
  - src/storage/migrations/index.ts
  - src/store/app.ts
  - src/store/app.test.ts
  - src/utils/buildFlatRows.ts
  - src/utils/buildFlatRows.test.ts
  - src/app/main.tsx
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02
  - SCORE-03
  - SCORE-04
  - SCORE-05
  - SCORE-06

must_haves:
  truths:
    - "V3SessionSchema validates: version 3, id, scores, overrides, notes, topicNotes, customQuestions, candidate"
    - "migrateV2ToV3 maps all V2 field renames (questionScore→scores, topicOverride→overrides, questionComment→notes, cardComment→topicNotes) and flattens customQuestions to CustomQuestion[]"
    - "runMigrations pipeline includes the v2→v3 step; bootstrap() returns V3 sessions on next load"
    - "useAppStore AppState includes ScoringState fields (scores, overrides, notes, topicNotes, customQuestions, candidate, activeSessionId) and all ScoringActions are implemented"
    - "buildFlatRows emits original topic.questions index (not filtered-subset index) when difficulty filter is active"
    - "main.tsx reads uiState back from storage after bootstrap(); activeSessionId hydrated from manifest"
  artifacts:
    - path: src/storage/migrations/v2-to-v3.ts
      provides: "Pure migrateV2ToV3 function"
    - path: src/storage/migrations/v2-to-v3.test.ts
      provides: "Fixture-pinned unit tests for the migration"
    - path: src/storage/types.ts
      provides: "V3SessionSchema, CustomQuestionSchema, V3Session type, createDefaultV3Session factory"
    - path: src/store/app.ts
      provides: "ScoringState, ScoringActions, activeSessionId — all wired into useAppStore"
  key_links:
    - from: src/store/app.ts
      to: src/storage/adapter.ts
      via: "subscribe handler calls storageAdapter.write for session:${activeSessionId} key"
      pattern: "storageAdapter\\.write"
    - from: src/storage/migrations/index.ts
      to: src/storage/migrations/v2-to-v3.ts
      via: "MIGRATIONS array entry fromVersion: 2"
      pattern: "fromVersion.*2"
---

<objective>
Wire the V3 data layer that all scoring UI depends on: schema definition, migration function, store extension, bug fixes, and main.tsx hydration.

Purpose: All scoring UI components (Wave 2 and 3) read from useAppStore. This plan makes the store ready — correct fields, correct actions, correct persistence wiring — before any component is written.

Output: V3SessionSchema + valibot types, migrateV2ToV3 (fixture-pinned tests), store with ScoringState+ScoringActions, buildFlatRows index bug fixed, main.tsx uiState hydration fixed.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-CONTEXT.md
@.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-RESEARCH.md
@.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-PATTERNS.md
@.planning/phases/04-shell-sidebar-read-only-content-tree/04-03-SUMMARY.md
@.planning/phases/03-storage-layer-migration-bootstrap/03-03-SUMMARY.md
@src/storage/types.ts
@src/storage/migrations/index.ts
@src/storage/migrations/v1-to-v2.ts
@src/storage/migrations/fixtures/
@src/store/app.ts
@src/store/app.test.ts
@src/utils/buildFlatRows.ts
@src/utils/buildFlatRows.test.ts
@src/app/main.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: V3 schema + migration (TDD)</name>
  <files>
    src/storage/types.ts,
    src/storage/migrations/v2-to-v3.ts,
    src/storage/migrations/v2-to-v3.test.ts,
    src/storage/migrations/fixtures/v2-session-fixture.ts,
    src/storage/migrations/index.ts
  </files>

  <read_first>
    - src/storage/types.ts — full file: V2SessionSchema shape (fields: questionScore, topicOverride, questionComment, cardComment, customQuestions Record shape, candidate), CandidateSchema, createDefaultSession pattern
    - src/storage/migrations/v1-to-v2.ts — full file: exact pure-function pattern (Readonly input, named export, no try/catch, no class, import paths with .js extensions)
    - src/storage/migrations/v1-to-v2.test.ts — full file: fixture-pinned test structure (describe block, inline fixture object, assertion per field)
    - src/storage/migrations/fixtures/ — list and read existing fixture files for naming conventions
    - src/storage/migrations/index.ts — full file: MIGRATIONS array structure, runMigrations return type, existing fromVersion entries
    - src/data/bank/types.ts — Difficulty type definition (for CustomQuestion schema)
  </read_first>

  <behavior>
    - migrateV2ToV3(v2): maps questionScore → scores, topicOverride → overrides, questionComment → notes, cardComment → topicNotes; null-coalesces each to {}
    - migrateV2ToV3(v2): flattens customQuestions from Record<string, Array<{id:number,text,level}>> to CustomQuestion[] using `custom-${topicId}-${q.id}` as string id
    - migrateV2ToV3(v2): removes customSeq; sets version: 3; preserves id
    - migrateV2ToV3(v2 with empty candidate {}): produces candidate: null
    - migrateV2ToV3(v2 with populated candidate): preserves all 6 CandidateDetails fields
    - V3SessionSchema validates a correct V3 object → parse succeeds
    - V3SessionSchema rejects version: 2 object → safeParse returns success: false
    - runMigrations with fromVersion:2 entry present: a version-2 payload runs through migrateV2ToV3 and produces a version-3 result
  </behavior>

  <action>
    RED phase — create test file first, implementation files do not exist yet.

    1. Create `src/storage/migrations/fixtures/v2-session-fixture.ts` — a V2Session object with: version:2, id:'test-session', questionScore:{}, topicOverride:{}, questionComment:{}, cardComment:{}, customQuestions: {'topic-a': [{id:1, text:'Custom Q', level:'intermediate'}]}, candidate:{name:'Alice',email:'',role:'',date:'',interviewer:'',details:''}, customSeq:1. Include a second fixture with all records non-empty to cover rename mapping.

    2. Create `src/storage/migrations/v2-to-v3.test.ts` — import fixtures; write describe('migrateV2ToV3') block with tests for every behavior listed above. Tests must import from `'../v2-to-v3.js'` (does not exist yet — tests will fail at import). Run `npx vitest run src/storage/migrations/v2-to-v3.test.ts` — confirm it fails (RED gate). Commit: `test(05-01): add failing v2-to-v3 migration tests (TDD RED)`.

    GREEN phase:

    3. Edit `src/storage/types.ts`:
       - Import Difficulty type from `'../data/bank/types.js'` (already likely in scope — check and add if missing)
       - Add CandidateDetailsSchema (if not already present matching the 6-field CandidateDetails: name, email, role, date, interviewer, details — all v.string())
       - Add CustomQuestionSchema: v.object with id:v.string(), topicId:v.string(), text:v.string(), level:v.union([v.literal('novice'), v.literal('intermediate'), v.literal('advanced'), v.literal('expert')])
       - Add V3SessionSchema: v.object with version:v.literal(3), id:v.string(), scores:v.record(v.string(),v.nullable(v.number())), overrides:v.record(v.string(),v.nullable(v.number())), notes:v.record(v.string(),v.string()), topicNotes:v.record(v.string(),v.string()), customQuestions:v.array(CustomQuestionSchema), candidate:v.nullable(CandidateDetailsSchema)
       - Export `type V3Session = v.InferOutput<typeof V3SessionSchema>`
       - Add `createDefaultV3Session(id: string): V3Session` factory returning {version:3, id, scores:{}, overrides:{}, notes:{}, topicNotes:{}, customQuestions:[], candidate:null}
       - DO NOT remove existing V2 types — the migration pipeline still needs them

    4. Create `src/storage/migrations/v2-to-v3.ts` — pure migration function following v1-to-v2.ts structure exactly: named export `migrateV2ToV3(session: Readonly<V2Session>): V3Session`. Field mapping: `scores: session.questionScore ?? {}`, `overrides: session.topicOverride ?? {}`, `notes: session.questionComment ?? {}`, `topicNotes: session.cardComment ?? {}`. For customQuestions: call private `migrateCustomQuestions` which does `Object.entries(session.customQuestions ?? {}).flatMap(([topicId, qs]) => qs.map(q => ({id: \`custom-${topicId}-${q.id}\`, topicId, text: q.text, level: q.level as Difficulty})))`. For candidate: if session.candidate and Object.keys(session.candidate).length > 0 cast to V3Session['candidate'], else null. Set version:3, preserve id. Remove customSeq (do not include it in output shape). Import paths use .js extensions.

    5. Edit `src/storage/migrations/index.ts`:
       - Add import: `import type { V2Session } from '../types.js'` (if not already importing it)
       - Add import: `import { migrateV2ToV3 } from './v2-to-v3.js'`
       - Add entry to MIGRATIONS array: `{ fromVersion: 2, fn: (r) => migrateV2ToV3(r as V2Session) }` — insert after the existing fromVersion:1 entry
       - Update the return type annotation of runMigrations if it is typed as returning only V2Session — it should return V2Session | V3Session (or unknown/the widest type already used)

    6. Run `npx vitest run src/storage/migrations/v2-to-v3.test.ts` — all tests must pass (GREEN gate). Run `npx vitest run src/storage/` — full storage suite must pass with no regressions. Run `npm run ci` (biome + tsc). Commit: `feat(05-01): implement V3 schema, CustomQuestionSchema, and v2-to-v3 migration`.

    All import paths must use `.js` extension on relative imports. All exports are named (no default exports). Follow Biome import ordering (type imports before value imports, grouped alphabetically).
  </action>

  <verify>
    <automated>npx vitest run src/storage/ --reporter=dot</automated>
  </verify>

  <done>
    - `npx vitest run src/storage/migrations/v2-to-v3.test.ts` exits 0 with all behavior cases passing
    - V3SessionSchema, CustomQuestionSchema, V3Session type, createDefaultV3Session are exported from src/storage/types.ts
    - MIGRATIONS array in migrations/index.ts has a fromVersion:2 entry pointing to migrateV2ToV3
    - Full storage test suite passes; no regressions
    - `npm run ci` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store extension, buildFlatRows fix, main.tsx hydration (TDD)</name>
  <files>
    src/store/app.ts,
    src/store/app.test.ts,
    src/utils/buildFlatRows.ts,
    src/utils/buildFlatRows.test.ts,
    src/app/main.tsx
  </files>

  <read_first>
    - src/store/app.ts — full file: AppState interface, AppActions interface, DEFAULT_STATE, createStore call, existing action implementations (e.g. toggleDifficulty for set-spread pattern), subscribe handler (lines exporting uiState write)
    - src/store/app.test.ts — full file: existing test structure and imports for adding new tests
    - src/utils/buildFlatRows.ts — full file: function signature, filteredQuestions.forEach loop (the bug), existing hideMarked parameter handling, VirtualRow/QuestionRow type shapes
    - src/utils/buildFlatRows.test.ts — full file: existing test cases for adding the index-fix regression test
    - src/app/main.tsx — full file: current bootstrap() call, _initialState usage, createRoot call, storageAdapter import status
    - src/storage/adapter.ts — confirm storageAdapter.read(['uiState']) signature exists (read method)
    - src/data/bank/types.ts — Difficulty type and Section/Topic/Question shapes (for buildFlatRows param types)
  </read_first>

  <behavior>
    Store — new actions:
    - setScore('topic1-0', 5): state.scores['topic1-0'] === 5
    - setScore('topic1-0', null): state.scores['topic1-0'] === null
    - setScore('topic1-0', 0): state.scores['topic1-0'] === 0 (not falsy-filtered)
    - setOverride('topic1', 7.5): state.overrides['topic1'] === 7.5
    - setOverride('topic1', null): state.overrides['topic1'] === null
    - setNote('topic1-2', 'hello'): state.notes['topic1-2'] === 'hello'
    - setTopicNote('topic1', 'topic note'): state.topicNotes['topic1'] === 'topic note'
    - addCustomQuestion({id:'custom-t1-1',topicId:'t1',text:'Q?',level:'novice'}): customQuestions array contains the new entry
    - deleteCustomQuestion('custom-t1-1'): entry removed from customQuestions array
    - setCandidate({name:'Alice',...}): state.candidate equals the provided object
    - setCandidate(null): state.candidate === null
    - resetAll(): scores={}, overrides={}, notes={}, topicNotes={}, customQuestions=[], candidate=null (activeSessionId preserved)

    buildFlatRows index fix:
    - Given a topic with 3 questions [q0, q1, q2] and a difficulty filter that hides q0 (intermediate when filter is 'expert' only): the visible QuestionRow for q1 has index===1, the row for q2 has index===2 (original positions, not 0 and 1)
    - With no filter active: QuestionRow.index matches the question's 0-based position in topic.questions (unchanged from existing behavior)

    hideMarked extension:
    - buildFlatRows with hideMarked:true and markedTopicIds containing topic 'A': topic 'A' does not appear in output rows
    - buildFlatRows with hideMarked:false: topic 'A' appears even if it is in markedTopicIds
  </behavior>

  <action>
    RED phase — add failing tests first.

    1. In `src/store/app.test.ts`: add a describe('ScoringActions') block with tests for every behavior above. The store does not yet have these fields/actions — tests will fail. Run `npx vitest run src/store/app.test.ts` — confirm failures. Commit: `test(05-01): add failing ScoringActions store tests (TDD RED)`.

    2. In `src/utils/buildFlatRows.test.ts`: add describe('index fix under filtering') and describe('hideMarked') test blocks covering the three index behaviors above. Run — confirm failures. Commit: `test(05-01): add failing buildFlatRows index and hideMarked tests (TDD RED)`.

    GREEN phase:

    3. Edit `src/store/app.ts`:
       - Add imports: `import type { Difficulty } from '../data/bank/types.js'` (if not already present)
       - Add CandidateDetails interface export: `{name: string; email: string; role: string; date: string; interviewer: string; details: string}`
       - Add CustomQuestion interface export: `{id: string; topicId: string; text: string; level: Difficulty}`
       - Add ScoringState interface to AppState (extend inline — do not replace AppState): `scores: Record<string, number | null>`, `overrides: Record<string, number | null>`, `notes: Record<string, string>`, `topicNotes: Record<string, string>`, `customQuestions: CustomQuestion[]`, `candidate: CandidateDetails | null`, `activeSessionId: string`
       - Add ScoringActions to AppActions: `setScore(questionId: string, score: number | null): void`, `setOverride(topicId: string, override: number | null): void`, `setNote(questionId: string, note: string): void`, `setTopicNote(topicId: string, note: string): void`, `addCustomQuestion(q: CustomQuestion): void`, `deleteCustomQuestion(id: string): void`, `setCandidate(candidate: CandidateDetails | null): void`, `resetAll(): void`
       - Add to DEFAULT_STATE: `scores: {}`, `overrides: {}`, `notes: {}`, `topicNotes: {}`, `customQuestions: []`, `candidate: null`, `activeSessionId: ''`
       - Implement actions in the createStore/create call following the existing set-spread pattern:
         - setScore: `set((s) => ({ scores: { ...s.scores, [questionId]: score } }))`
         - setOverride: `set((s) => ({ overrides: { ...s.overrides, [topicId]: override } }))`
         - setNote: `set((s) => ({ notes: { ...s.notes, [questionId]: note } }))`
         - setTopicNote: `set((s) => ({ topicNotes: { ...s.topicNotes, [topicId]: note } }))`
         - addCustomQuestion: `set((s) => ({ customQuestions: [...s.customQuestions, q] }))`
         - deleteCustomQuestion: `set((s) => ({ customQuestions: s.customQuestions.filter((cq) => cq.id !== id) }))`
         - setCandidate: `set({ candidate })`
         - resetAll: `set({ scores: {}, overrides: {}, notes: {}, topicNotes: {}, customQuestions: [], candidate: null })` — does NOT reset activeSessionId or any uiState fields
       - Extend the existing subscribe handler: after the existing `storageAdapter.write({uiState: {...}})` call, add a second `storageAdapter.write` call guarded by `if (state.activeSessionId)` that writes `{[\`session:${state.activeSessionId}\`]: {version:3, id:state.activeSessionId, scores:state.scores, overrides:state.overrides, notes:state.notes, topicNotes:state.topicNotes, customQuestions:state.customQuestions, candidate:state.candidate}}` — this is the session persistence hook per D-XX (scoring state location decision)

    4. Fix `src/utils/buildFlatRows.ts`:
       - Replace `topic.filteredQuestions.forEach((question, index) => {...})` with a for-of loop: `for (const question of topic.filteredQuestions) { const index = topic.questions.indexOf(question); rows.push({...existing fields..., index}); }`
       - Extend the function signature to accept `hideMarked?: boolean` and `markedTopicIds?: Set<string>` inside the filters object (or as separate optional parameters — match the existing signature structure). After computing `filteredQuestions` for a topic, add: `if (filters.hideMarked && filters.markedTopicIds?.has(topic.id) && filteredQuestions.length > 0) { continue; }` — skip the entire topic block when it is marked and hideMarked is true.
       - QuestionRow type: ensure it includes `isCustom?: boolean` and `customId?: string` fields (needed by Wave 2 QuestionCard for badge/delete button rendering). Add these optional fields to the QuestionRow interface/type definition.
       - When pushing custom questions into rows (if buildFlatRows already handles customQuestions from store — check): set `isCustom: true` and `customId: cq.id`. If buildFlatRows does not yet handle customQuestions (likely — it reads DEFAULT_SECTIONS), add logic after the built-in questions loop for each topic to also push custom questions from a `customQuestions: CustomQuestion[]` parameter (add this param to the signature). Custom question rows get `question: {q: cq.text, level: cq.level}` shape, `index = topic.questions.length + customQsForTopic.indexOf(cq)`, `isCustom: true`, `customId: cq.id`.

    5. Edit `src/app/main.tsx`:
       - After the existing `const initialState = await bootstrap()` call, add: `const uiStateResult = await storageAdapter.read(['uiState'])` and extract `const uiState = (uiStateResult.uiState as Partial<AppState> | undefined) ?? {}` — replacing or augmenting the current `_initialState` stub pattern
       - Read `activeSessionId` from the manifest: `const activeSessionId = initialState.manifest.activeSessionId ?? ''`
       - Pass both `uiState` and `activeSessionId` to `useAppStore.setState`: merge `{...hydratedUiState, activeSessionId}` using the existing Set reconstruction pattern for selectedDifficulties and selectedSections
       - Follow the existing Biome import ordering rules; storageAdapter must already be importable from `'../storage/index.js'` (confirmed in Phase 3)

    6. Run `npx vitest run src/store/app.test.ts src/utils/buildFlatRows.test.ts` — all new tests must pass (GREEN gate). Run `npx vitest run` — full suite (no regressions). Run `npm run ci`. Commit: `feat(05-01): extend store with ScoringActions, fix buildFlatRows index, wire main.tsx hydration`.
  </action>

  <verify>
    <automated>npx vitest run src/store/ src/utils/buildFlatRows.test.ts --reporter=dot && npm run ci</automated>
  </verify>

  <done>
    - All ScoringActions tests pass (setScore, setOverride, setNote, setTopicNote, addCustomQuestion, deleteCustomQuestion, setCandidate, resetAll)
    - buildFlatRows test cases for index-under-filtering and hideMarked pass
    - main.tsx reads uiState from storageAdapter.read and hydrates activeSessionId from manifest
    - `npm run ci` exits 0 (biome + tsc --noEmit)
    - Full test suite exits 0 with no regressions from Phase 4 (≥199 tests)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → bootstrap() | Session data read from storage; may be V2 or corrupt |
| User input → Zustand actions | Score values, note text, custom question text enter via React events |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | V2→V3 migration input | mitigate | valibot safeParse(V3SessionSchema) after migration; failure writes recovery:timestamp key via existing bootstrap() error path |
| T-05-01-02 | Tampering | setScore action | mitigate | `Math.min(10, Math.max(0, value))` clamp in setScore action body; slider min/max enforced by browser for mouse input; keyboard can bypass — clamp in action is the guard |
| T-05-01-03 | Tampering | setOverride action | mitigate | Clamp to [0, 10] on blur in TopicMarkDisplay (Wave 2); in setOverride action body apply `Math.min(10, Math.max(0, override))` when override !== null |
| T-05-01-04 | Denial of Service | customQuestions array | accept | No hard cap; storageAdapter.#checkQuota() fires at 80% threshold toast (Phase 3); no additional guard needed in Phase 5 |
| T-05-01-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages in Phase 5 (RESEARCH.md confirmed); slopcheck not required |
</threat_model>

<verification>
After both tasks complete:
1. `npx vitest run` — full suite passes (≥199 + new tests)
2. `npm run ci` — biome + tsc --noEmit both exit 0
3. `npm run build` — bundle builds without errors
4. V3SessionSchema validates a constructed V3 session object in a quick manual check
5. `buildFlatRows` called with a difficulty filter returns QuestionRow.index === original topic.questions position (verify via test output)
</verification>

<success_criteria>
- V3SessionSchema, CustomQuestionSchema, V3Session, createDefaultV3Session exported from src/storage/types.ts
- migrateV2ToV3 pure function in src/storage/migrations/v2-to-v3.ts with fixture-pinned tests passing
- MIGRATIONS array in migrations/index.ts has fromVersion:2 entry
- useAppStore has ScoringState fields and all 8 ScoringActions implemented and tested
- buildFlatRows emits original question index under filtering; hideMarked + markedTopicIds param added
- main.tsx hydrates uiState from storageAdapter.read + activeSessionId from bootstrap().manifest
- Full test suite green; `npm run ci` exits 0
</success_criteria>

<output>
Create `.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-01-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| File | Type | Used By |
|------|------|---------|
| src/storage/types.ts (V3SessionSchema, CustomQuestionSchema, V3Session, createDefaultV3Session) | Schema + types | Plan 02 components (TopicMarkDisplay), Plan 03 (ResetConfirmDialog), Phase 7 YAML export |
| src/storage/migrations/v2-to-v3.ts | Pure migration | bootstrap() migration pipeline |
| src/storage/migrations/v2-to-v3.test.ts | Tests | CI gate |
| src/store/app.ts (ScoringState, ScoringActions, subscribe extension) | Store | All scoring UI components (Plans 02 and 03) |
| src/utils/buildFlatRows.ts (index fix + hideMarked + customQuestions) | Utility | ContentTree (App.tsx) rendering correct rows |
| src/app/main.tsx (uiState hydration + activeSessionId) | App entry | Session persistence round-trip |
</output>
