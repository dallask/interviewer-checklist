---
phase: 12-uat-defect-cleanup
plan: 04
type: execute
wave: 2
depends_on:
  - 12-02
autonomous: true
files_modified:
  - src/components/ActionsGroup.tsx
requirements:
  - UI-09
  - UI-10
must_haves:
  truths:
    - "ActionsGroup has a Hide notes button wired to setHideNotes(!hideNotes) with aria-pressed reflecting hideNotes"
    - "All ActionsGroup buttons are icon-only (no visible text labels) with title and aria-label attributes"
    - "Every icon-only button has title matching its aria-label and meets 44px touch target minimum"
    - "aria-pressed buttons (Hide marked topics, Hide notes, Dark mode) show pressed visual state when active"
    - "Reset all button retains its destructive text-red-600 color"
    - "The hr separator between session/layout action groups is preserved"
  artifacts:
    - path: "src/components/ActionsGroup.tsx"
      provides: "Hide notes button + all icon-only buttons with title tooltips"
      contains: "setHideNotes"
  key_links:
    - from: "ActionsGroup.tsx Hide notes button"
      to: "src/store/app.ts setHideNotes"
      via: "useAppStore((s) => s.setHideNotes)"
      pattern: "setHideNotes"
    - from: "ActionsGroup.tsx buttons"
      to: "title attribute"
      via: "native browser tooltip"
      pattern: "title="
---

<objective>
Complete UI-09 (ActionsGroup wiring) and implement UI-10 (icon-only button conversion) in a single plan, because both changes apply to the same file and must be co-located.

UI-09 ActionsGroup wiring (per D-09): Add `hideNotes` and `setHideNotes` store subscriptions. Add a "Hide notes" button (📝) after "Hide marked topics" with `aria-pressed={hideNotes}` and `onClick={() => setHideNotes(!hideNotes)}`. The store changes this button depends on were made in Plan 02.

UI-10 (per D-14, D-15, D-16): Convert all 10 existing ActionsGroup buttons plus the new "Hide notes" button (11 total) from full-width text-label buttons to icon-only buttons. For each button: replace visible text content with the emoji glyph from the UI-SPEC.md table, add `title="[action name]"` matching `aria-label`, remove `w-full text-left` from className, reduce padding to `p-2` with `min-h-[44px] min-w-[44px]`.

Purpose: ActionsGroup.tsx is the only file in this plan. Plan 02 (Wave 1) must complete first since it adds `hideNotes` and `setHideNotes` to the store.
Output: Updated ActionsGroup.tsx with the Hide notes button and all icon-only conversions.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Hide notes button to ActionsGroup (UI-09, per D-09)</name>
  <files>src/components/ActionsGroup.tsx, src/test/phase-12-defects.test.tsx</files>
  <behavior>
    - Test: ActionsGroup renders a button with aria-label="Hide notes"
    - Test: that button has aria-pressed=false when hideNotes=false
    - Test: that button has aria-pressed=true when hideNotes=true
    - Test: clicking the button calls setHideNotes with the toggled value
  </behavior>
  <action>
    In `src/components/ActionsGroup.tsx`, add two store subscriptions near the existing `hideMarked` subscriptions (lines 30–33 area):
      `const hideNotes = useAppStore((s) => s.hideNotes);`
      `const setHideNotes = useAppStore((s) => s.setHideNotes);`

    Locate the "Hide marked topics" button (the `aria-pressed={hideMarked}` button). Immediately after it, add the "Hide notes" button using the same className structure (PATTERNS.md "aria-pressed toggle button" analog). The button must have:
      - `type="button"`
      - `aria-pressed={hideNotes}`
      - `onClick={() => setHideNotes(!hideNotes)}`
      - `title="Hide notes"`
      - `aria-label="Hide notes"`
      - visible content: `📝` (icon-only, as this task and Task 2 run together)
      - className with pressed state: `${hideNotes ? 'bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300' : 'text-gray-900 dark:text-gray-100 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600'}` plus `p-2 min-h-[44px] min-w-[44px] text-sm rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none`

    Extend `src/test/phase-12-defects.test.tsx` with a "UI-09: ActionsGroup Hide notes button" describe block. Mock `useAppStore` to inject `hideNotes` and `setHideNotes`. Render `ActionsGroup` (may require mocking additional store dependencies — see existing modal-focus-trap.test.tsx for how the test environment handles refs). Assert button presence, aria-pressed state, and onClick call.
  </action>
  <verify>
    <automated>npx vitest run src/test/phase-12-defects.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Hide notes button present in ActionsGroup. aria-pressed toggles with hideNotes value. setHideNotes called on click. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert all ActionsGroup buttons to icon-only with title tooltips (UI-10, per D-14, D-15, D-16)</name>
  <files>src/components/ActionsGroup.tsx, src/test/phase-12-defects.test.tsx</files>
  <behavior>
    - Test: every button inside the ActionsGroup flex/grid container has a non-empty title attribute
    - Test: every button's title attribute matches its aria-label attribute
    - Test: no button renders visible text content that is a word (content is emoji/glyph only)
    - Test: all aria-pressed buttons retain their aria-pressed attribute
  </behavior>
  <action>
    Convert all 11 buttons (10 existing + 1 "Hide notes" added in Task 1) in `ActionsGroup.tsx` from full-text to icon-only following the UI-SPEC.md icon table and the PATTERNS.md "icon-only button with aria-label" pattern. For each button:

    Icon assignments (from UI-SPEC.md and RESEARCH.md Q4):
    - "Switch session" → 🔄
    - "AI feedback prompt" → 🤖
    - "Expand all" → ↕
    - "Collapse all" → ↔
    - "Hide marked topics" → 👁 (aria-pressed applies)
    - "Hide notes" → 📝 (aria-pressed applies — already set in Task 1)
    - "Dark mode" / "Light mode" → 🌙 / ☀ (dynamic based on darkMode; aria-pressed applies)
    - "Candidate details" → 👤
    - "Import YAML" → 📥
    - "Export YAML" → 📤
    - "Reset all" → 🗑

    For each button:
    1. Replace visible text content with the emoji glyph.
    2. Add `aria-label="[original text]"` if not already present.
    3. Add `title="[original text]"` matching the aria-label.
    4. Remove `w-full` and `text-left` from className.
    5. Change padding from `px-3 py-2` to `p-2` and add `min-h-[44px] min-w-[44px]`.

    Layout: keep `flex flex-col gap-1` for the button container — this keeps the vertical stacking intact in the 280px sidebar. Do NOT change to a grid unless the vertical stack clearly overflows at reasonable window heights (RESEARCH.md Open Question 2 says keep flex-col as the default).

    "Reset all" button: keep its destructive `text-red-600 dark:text-red-400` color and change hover to `hover:bg-red-50 dark:hover:bg-red-900/20` (UI-SPEC.md interaction states).

    The `<hr>` separator between the session-action group and the layout/view-action group is preserved.

    Extend `src/test/phase-12-defects.test.tsx` with a "UI-10: ActionsGroup icon-only buttons" describe block. Query all buttons rendered by ActionsGroup. For each button, assert that `title` is non-empty and equals `aria-label`. Assert that button text content is a single emoji character (or short glyph) — not a full English word. (A regex like `/^[a-zA-Z]{3,}/` must NOT match any button's text content.)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All 11 ActionsGroup buttons are icon-only. Every button has a title attribute matching its aria-label. No visible text labels remain. All tests pass. Hide notes button (📝) with aria-pressed is present. Reset all retains red destructive styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → store action → component re-render | setHideNotes toggle crosses no trust boundary; purely in-memory Zustand state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Tampering | XSS via emoji in title/aria-label attributes | accept | React escapes all string props; emoji characters contain no HTML; title attribute is a plain string (D-14 — native title, no custom tooltip component) |
| T-12-08 | Denial of Service | 11 icon-only buttons without visible labels | accept | Native title attribute provides tooltip discovery on hover/focus; aria-label ensures screen reader accessibility; touch targets meet 44px minimum |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes all tests including all prior phase-12-defects.test.tsx describe blocks
- `src/components/ActionsGroup.tsx` imports `hideNotes` and `setHideNotes` from store
- `src/components/ActionsGroup.tsx` contains a button with `aria-label="Hide notes"` and `aria-pressed={hideNotes}`
- `grep -c 'title=' src/components/ActionsGroup.tsx` returns 11 (one per button)
- No button in ActionsGroup contains visible English-word text content
- "Reset all" button retains `text-red-600 dark:text-red-400`
</verification>

<success_criteria>
- Hide notes button exists in ActionsGroup and wires to setHideNotes (UI-09 complete)
- All ActionsGroup buttons are icon-only with title tooltips and no visible text labels (UI-10 complete)
- All 6 UAT defects (SCORE-07, SESS-05, UI-09, UI-10, UI-11, UI-12) are implemented across Plans 01–04
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/12-uat-defect-cleanup/12-04-SUMMARY.md` when done
</output>
