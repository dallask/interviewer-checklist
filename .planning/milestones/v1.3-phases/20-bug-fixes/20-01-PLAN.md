---
phase: 20-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/buildFlatRows.ts
  - src/utils/buildFlatRows.test.ts
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "A topic added via AddTopicForm appears in the content tree immediately after submit — no page reload required"
    - "A newly added topic with zero questions is visible as a TopicRow in the flat row output"
    - "A section containing only empty topics is NOT skipped by the section-skip guard"
    - "Topics with visible filtered questions continue to appear as before (no regression)"
    - "Existing test suite passes without any new failures"
  artifacts:
    - path: "src/utils/buildFlatRows.ts"
      provides: "Fixed topic inclusion condition at line 154"
      contains: "topic.questions.length === 0"
    - path: "src/utils/buildFlatRows.test.ts"
      provides: "Regression test for empty-topic visibility (BUG-01)"
      contains: "BUG-01"
  key_links:
    - from: "src/utils/buildFlatRows.ts"
      to: "src/components/ContentTree.tsx"
      via: "flat row array consumed by virtualizer"
      pattern: "buildFlatRows"
    - from: "src/utils/buildFlatRows.test.ts"
      to: "src/utils/buildFlatRows.ts"
      via: "vitest import"
      pattern: "import.*buildFlatRows"
---

<objective>
Fix BUG-01: topics added by the user never appear in the content tree because `buildFlatRows` excludes them when `filteredQuestions.length === 0`. The fix is a single conditional change at line 154 of `buildFlatRows.ts`. A targeted regression test is added to lock the corrected behavior.

Purpose: A user who adds a new topic expects to see it immediately. The current bug makes the topic invisible even after reload (data is saved correctly; rendering is the failure point).

Output: Modified `buildFlatRows.ts` with corrected inclusion guard, and two new test cases in `buildFlatRows.test.ts` covering empty-topic visibility and section-skip guard self-correction.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/20-bug-fixes/20-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/20-bug-fixes/20-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix empty-topic inclusion in buildFlatRows and add regression tests (BUG-01, per D-01, D-02, D-03)</name>

  <read_first>
    - src/utils/buildFlatRows.ts — read the full file; focus on lines 140–175 (topic inclusion loop and section-skip guard)
    - src/utils/buildFlatRows.test.ts — read the full file; note the `emptyFilters` constant at line 44 and the synthetic section pattern at lines 53–72
    - src/components/TopicRow.tsx — read lines 115–135 to verify the custom question form at line 123 renders gracefully when `row.topic.questions.length === 0` (read-only verification; no change expected)
  </read_first>

  <behavior>
    - buildFlatRows([section with empty topic], {}, {}, emptyFilters) returns a row with type === 'topic' and id matching the empty topic's id
    - buildFlatRows([section with one empty topic], {}, {}, emptyFilters) returns a row with type === 'section' (section-skip guard does not fire)
    - buildFlatRows([section with populated topic], {}, {}, emptyFilters) still returns the topic row (no regression to existing behavior)
  </behavior>

  <action>
    In src/utils/buildFlatRows.ts, at line 154, replace the condition:

      if (filteredQuestions.length > 0) {

    with:

      if (filteredQuestions.length > 0 || topic.questions.length === 0) {

    The inner body (hideMarked guard, visibleTopics.push) remains unchanged. The section-skip guard at line 168 requires no change — once empty topics are in visibleTopics, the guard self-corrects (per D-03).

    Do NOT mutate any section or topic object — use the existing spread pattern `{ ...topic, filteredQuestions }` already at line 162.

    In src/utils/buildFlatRows.test.ts, add a new describe block after the last existing describe block. Reuse the `emptyFilters` constant already defined at line 44. Add two test cases:

    Case 1 — "includes a topic with no questions so it appears in the tree":
      - Construct a V4Section with id 'sec-empty', label 'Section With Empty Topic', icon 'E', isDefault: false, and one topic: { id: 'topic-empty', name: 'Empty Topic', desc: '', tag: '', isDefault: false, questions: [] }
      - Call buildFlatRows([emptyTopicSection], {}, {}, emptyFilters)
      - Assert rows.find(r => r.type === 'topic' && r.id === 'topic-empty') is defined

    Case 2 — "section containing only empty topic is NOT skipped by section-skip guard":
      - Construct a V4Section with id 'sec-empty2', label 'Section', icon 'S', isDefault: false, and one topic: { id: 'topic-e', name: 'T', desc: '', tag: '', isDefault: false, questions: [] }
      - Call buildFlatRows([emptyTopicSection], {}, {}, emptyFilters)
      - Assert rows.find(r => r.type === 'section' && r.id === 'sec-empty2') is defined

    Name the describe block: "buildFlatRows — BUG-01: empty topic visibility"

    Import shape: V4Section type is already imported at the top of the test file. Use the same import; do not add a duplicate.
  </action>

  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/utils/buildFlatRows.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>

  <done>
    - buildFlatRows.ts line 154 condition reads: filteredQuestions.length > 0 || topic.questions.length === 0
    - buildFlatRows.test.ts contains a describe block named "buildFlatRows — BUG-01: empty topic visibility" with two passing it() cases
    - All pre-existing tests in buildFlatRows.test.ts continue to pass
    - npx vitest run src/utils/buildFlatRows.test.ts exits 0 with no failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → store → buildFlatRows | topic objects flow from Zustand store (populated by AddTopicForm) into buildFlatRows; no external network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | buildFlatRows.ts topic inclusion | accept | Fix is additive OR condition; does not introduce new data paths or user-controlled branching |
| T-20-02 | Denial of Service | virtualizer row count growth | accept | Empty topics add at most one extra row each; no unbounded growth path introduced |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Run full test suite to confirm no regressions:

  cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run 2>&1 | tail -20

Expected: all tests pass (2693+ passing, 0 failing).

Specific BUG-01 check:

  cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/utils/buildFlatRows.test.ts --reporter=verbose

Expected: new describe block "buildFlatRows — BUG-01: empty topic visibility" shows 2 passing tests.
</verification>

<success_criteria>
1. src/utils/buildFlatRows.ts line 154 contains the condition: filteredQuestions.length > 0 || topic.questions.length === 0
2. Two new test cases pass under "buildFlatRows — BUG-01: empty topic visibility"
3. Full vitest run exits 0 — no regressions
4. TopicRow at line 123 renders the "Add custom question" form for the empty topic (read-only verification confirms no change needed)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `src/utils/buildFlatRows.ts` (modified) | utility | topic inclusion condition changed from `filteredQuestions.length > 0` to `filteredQuestions.length > 0 \|\| topic.questions.length === 0` at line 154 |
| `src/utils/buildFlatRows.test.ts` (modified) | test | new describe block `"buildFlatRows — BUG-01: empty topic visibility"` with two `it()` cases: `"includes a topic with no questions so it appears in the tree"` and `"section containing only empty topic is NOT skipped by section-skip guard"` |

<output>
Create `.planning/phases/20-bug-fixes/20-01-SUMMARY.md` when done
</output>
