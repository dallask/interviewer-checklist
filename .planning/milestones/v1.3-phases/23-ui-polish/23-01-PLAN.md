---
phase: 23-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AiPromptModal.tsx
  - src/components/AboutModal.tsx
  - src/components/CandidateModal.tsx
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-04
must_haves:
  truths:
    - "AiPromptModal renders at max-w-2xl width (672px) instead of max-w-sm (384px)"
    - "AiPromptModal textarea is h-80 (320px) instead of h-64 (256px)"
    - "Every action button in AiPromptModal, AboutModal, and CandidateModal shows a Lucide icon before its text label"
    - "All modified buttons have transition-colors duration-150 applied"
    - "No aria-label values change — existing tests continue to pass"
  artifacts:
    - path: "src/components/AiPromptModal.tsx"
      provides: "Resized modal + icon buttons (POL-01, POL-02 per D-01, D-02, D-05)"
      contains: "max-w-2xl"
    - path: "src/components/AboutModal.tsx"
      provides: "Close button with X icon (POL-02 per D-05)"
      contains: "lucide-react"
    - path: "src/components/CandidateModal.tsx"
      provides: "Discard/Save/Reset buttons with icons (POL-02 per D-05)"
      contains: "lucide-react"
  key_links:
    - from: "AiPromptModal.tsx line 102"
      to: "dialog className"
      via: "max-w-2xl replaces max-w-sm"
      pattern: "max-w-2xl"
    - from: "AiPromptModal.tsx line 116"
      to: "textarea className"
      via: "h-80 replaces h-64"
      pattern: "h-80"
---

<objective>
Apply POL-01 (taller AI dialog), POL-02 (modal button icons), and POL-04 (hover transition-colors) to AiPromptModal, AboutModal, and CandidateModal.

Purpose: The AI dialog currently shows only ~256px of the generated prompt before requiring scroll; widening to max-w-2xl and increasing textarea to h-80 lets users read significantly more content at once. Icon-annotated buttons improve scannability across all modals.

Output: Three modified component files with wider/taller AI dialog, Lucide icons on all action buttons, and transition-colors on all hover states.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-ui-polish/23-CONTEXT.md
@.planning/phases/23-ui-polish/23-UI-SPEC.md
@src/components/AiPromptModal.tsx
@src/components/AboutModal.tsx
@src/components/CandidateModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resize AiPromptModal and add icon buttons (POL-01 + POL-02 + POL-04 per D-01, D-02, D-05, D-12, D-13)</name>
  <files>src/components/AiPromptModal.tsx</files>
  <action>
    Make three targeted edits to AiPromptModal.tsx:

    1. Add Lucide imports at the top of the file (after the existing React import line):
       Import `Copy` and `X` from `lucide-react`.

    2. Dialog container (line 102): Change `max-w-sm` to `max-w-2xl` in the dialog's className (per D-01). All other classes on that element stay unchanged.

    3. Textarea (line 116): Change `h-64` to `h-80` in the textarea's className (per D-02). The `resize-y` attribute stays.

    4. Close button (line 133–139): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string, and insert `<X className="w-4 h-4" aria-hidden="true" />` as the first child before the text "Close" (per D-05, D-12, D-13).

    5. Copy to clipboard button (line 140–149): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string, and insert `<Copy className="w-4 h-4" aria-hidden="true" />` as the first child before the text "Copy to clipboard" (per D-05, D-12, D-13).

    Do NOT change: focus trap logic, copy handler, aria-live paragraph, the `disabled` attribute, or any other className tokens beyond what is listed above.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -c "max-w-2xl" src/components/AiPromptModal.tsx && grep -c "h-80" src/components/AiPromptModal.tsx && grep -c "lucide-react" src/components/AiPromptModal.tsx && npx vitest run src/components/AiPromptModal.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    AiPromptModal.tsx contains max-w-2xl, h-80, lucide-react import, X and Copy icons on buttons, transition-colors duration-150 on both buttons. All AiPromptModal tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add icon button to AboutModal Close button (POL-02 + POL-04 per D-05, D-12, D-13)</name>
  <files>src/components/AboutModal.tsx</files>
  <action>
    Make two targeted edits to AboutModal.tsx:

    1. Add Lucide import at the top of the file (after the existing React import):
       Import `X` from `lucide-react`.

    2. Close button (line 98–105): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string, and insert `<X className="w-4 h-4" aria-hidden="true" />` as the first child before the text "Close" (per D-05, D-12, D-13).

    Do NOT change: backdrop click handler, focus trap logic, dialog structure, aria-labelledby, or any other element.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -c "lucide-react" src/components/AboutModal.tsx && grep -c "transition-colors" src/components/AboutModal.tsx && npx vitest run src/components/AboutModal.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    AboutModal.tsx has lucide-react import, X icon before "Close" text, flex items-center gap-2 transition-colors duration-150 on the Close button. All AboutModal tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add icon buttons to CandidateModal (POL-02 + POL-04 per D-05, D-12, D-13)</name>
  <files>src/components/CandidateModal.tsx</files>
  <action>
    Make targeted edits to CandidateModal.tsx:

    1. Add Lucide imports at the top of the file (after the existing React import):
       Import `Check`, `RotateCcw`, and `X` from `lucide-react`.

    2. "Discard changes" button (line 231–237): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string. Insert `<X className="w-4 h-4" aria-hidden="true" />` as the first child before "Discard changes" (per D-05, D-12, D-13).

    3. "Save details" button (line 238–243, type="submit"): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string. Insert `<Check className="w-4 h-4" aria-hidden="true" />` as the first child before "Save details" (per D-05, D-12, D-13). Use `Check` as the icon — this is the executor's discretion choice per the CONTEXT.md Discretion section.

    4. "Reset details" link-style button (line 222–228): Add `flex items-center gap-2 transition-colors duration-150` to the existing className string. Insert `<RotateCcw className="w-4 h-4" aria-hidden="true" />` as the first child before "Reset details" (per D-05, D-12, D-13).

    Do NOT change: form submit handler, handleDiscard handler, handleResetDetails handler, focus trap, inputClass constant, or any form field.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -c "lucide-react" src/components/CandidateModal.tsx && grep -c "RotateCcw" src/components/CandidateModal.tsx && npx vitest run src/components/CandidateModal.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    CandidateModal.tsx has lucide-react import with Check, RotateCcw, X; all three action buttons show their icon before text; transition-colors duration-150 present on each. All CandidateModal tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props → render | All changes are purely presentational className and icon insertions — no data paths affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | AiPromptModal resize | accept | max-w-2xl / h-80 are static class changes; no data or auth paths touched |
| T-23-02 | Tampering | Icon imports from lucide-react | accept | lucide-react is an installed npm dep (^1.21.0 in package.json), no registry fetch at runtime |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — lucide-react already in package.json |
</threat_model>

<verification>
After all three tasks complete:
- `grep -c "max-w-2xl" src/components/AiPromptModal.tsx` returns 1
- `grep -c "h-80" src/components/AiPromptModal.tsx` returns 1
- Each of the three files contains `lucide-react` import
- `npx vitest run src/components/AiPromptModal.test.tsx src/components/AboutModal.test.tsx src/components/CandidateModal.test.tsx` all pass
</verification>

<success_criteria>
1. AiPromptModal dialog is wider (max-w-2xl) and textarea is taller (h-80) with resize-y intact
2. All three modal files have Lucide icon imports; every action button renders icon before text with gap-2
3. transition-colors duration-150 is present on every modified button
4. All existing tests for these three components pass without modification
</success_criteria>

<output>
Create `.planning/phases/23-ui-polish/23-01-SUMMARY.md` when done
</output>
