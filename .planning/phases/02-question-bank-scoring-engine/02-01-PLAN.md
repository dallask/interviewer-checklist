---
phase: 02-question-bank-scoring-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/data/bank/types.ts
  - src/data/bank/frontend.ts
  - src/data/bank/design.ts
  - src/data/bank/backend.ts
  - src/data/bank/environment.ts
  - src/data/bank/testing.ts
  - src/data/bank/cicd.ts
  - src/data/bank/tooling.ts
  - src/data/bank/integrations.ts
  - src/data/bank/ai.ts
  - src/data/bank/index.ts
  - src/data/bank/bank.test.ts
autonomous: true
requirements:
  - BANK-01
  - BANK-03

must_haves:
  truths:
    - "DEFAULT_SECTIONS is importable from src/data/bank/index.ts and contains exactly 9 Section objects"
    - "Total topic count across all groups is exactly 86"
    - "Total question count across all groups is at least 1000 (actual: 1067)"
    - "Every question has a level property that is one of: novice | intermediate | advanced | expert"
    - "DIFFICULTY_COEFFICIENTS is exported from src/data/bank/types.ts with values novice=1.00, intermediate=1.25, advanced=1.50, expert=1.75"
    - "bank.test.ts passes all structural assertions: npm test exits 0"
    - "@vitest/coverage-v8 is installed and vitest.config.ts has a coverage block targeting src/scoring/**"
  artifacts:
    - path: "src/data/bank/types.ts"
      provides: "Difficulty union type, Question/Topic/Section interfaces, DIFFICULTY_COEFFICIENTS record"
      exports: ["Difficulty", "Question", "Topic", "Section", "DIFFICULTY_COEFFICIENTS"]
    - path: "src/data/bank/index.ts"
      provides: "DEFAULT_SECTIONS assembled from 9 group files; re-exports all types"
      exports: ["DEFAULT_SECTIONS", "Difficulty", "Question", "Topic", "Section", "DIFFICULTY_COEFFICIENTS"]
    - path: "src/data/bank/bank.test.ts"
      provides: "Structural assertions: 9 groups, 86 topics, ≥1000 questions, valid levels"
    - path: "vitest.config.ts"
      provides: "Coverage config with provider: v8, 100% thresholds scoped to src/scoring/**"
      contains: "coverage"
  key_links:
    - from: "src/data/bank/frontend.ts (and 8 siblings)"
      to: "src/data/bank/types.ts"
      via: "import type { Section } from './types.js'"
      pattern: "import type \\{ Section \\} from '\\./"
    - from: "src/data/bank/index.ts"
      to: "src/data/bank/frontend.ts (and 8 siblings)"
      via: "named import of each group export"
      pattern: "import \\{ .*Section \\} from '"
    - from: "src/data/bank/bank.test.ts"
      to: "src/data/bank/index.ts"
      via: "import { DEFAULT_SECTIONS } from './index.js'"
      pattern: "DEFAULT_SECTIONS"
---

<objective>
Install @vitest/coverage-v8, configure coverage in vitest.config.ts, define all TypeScript types for the question bank, extract DEFAULT_SECTIONS verbatim from the prototype for all 9 section groups, assemble them in a barrel index, and write structural tests asserting exact bank counts and valid difficulty levels.

Purpose: Establishes the build-time data contract (types, constants, test coverage infrastructure) that the Wave 2 scoring engine and all downstream phases depend on. Phase 3 imports types; Phase 4+ imports DEFAULT_SECTIONS.

Output: 15 files — types.ts, 9 group files, index.ts, bank.test.ts, updated vitest.config.ts, updated package.json/package-lock.json.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-question-bank-scoring-engine/02-CONTEXT.md
@.planning/phases/02-question-bank-scoring-engine/02-RESEARCH.md
@.planning/phases/02-question-bank-scoring-engine/02-PATTERNS.md
@.planning/phases/01-foundation-scaffolding/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @vitest/coverage-v8 and add coverage config to vitest.config.ts</name>
  <files>package.json, package-lock.json, vitest.config.ts</files>
  <read_first>
    - vitest.config.ts — current config (existing keys must be preserved unchanged)
    - package.json — verify @vitest/coverage-v8 is absent before installing
    - .planning/phases/02-question-bank-scoring-engine/02-RESEARCH.md lines 506–529 — exact coverage config block
    - .planning/phases/02-question-bank-scoring-engine/02-PATTERNS.md lines 421–466 — existing file + target state diff
  </read_first>
  <behavior>
    - After install: `npm ls @vitest/coverage-v8` resolves at version 4.1.9 (matching Vitest)
    - After config update: `npx vitest run --coverage` does not throw "No coverage provider found"
    - Coverage block scopes include to `['src/scoring/**']` and excludes `['src/data/bank/**']`
    - Thresholds: lines=100, branches=100, functions=100, statements=100
    - All existing vitest.config.ts keys (environment, globals, setupFiles, passWithNoTests) remain unchanged
  </behavior>
  <action>
    Run: `npm install --save-dev @vitest/coverage-v8` from the project root.

    Then edit vitest.config.ts — add a `coverage` block inside the `test` object, after the existing `passWithNoTests: true` line. Use the target state from PATTERNS.md (lines 439–462): provider is 'v8', include is `['src/scoring/**']`, exclude is `['src/data/bank/**']`, thresholds sets lines/branches/functions/statements all to 100. Do NOT modify or remove any existing keys.

    Run `npm run ci` to confirm biome and tsc pass. Run `npm test` to confirm tests still pass (passWithNoTests is still set — 0 test files is fine).

    Note: Coverage cannot be run yet — the scorer module does not exist. The config is wired; execution is gated on Wave 2.
  </action>
  <verify>
    <automated>npm ls @vitest/coverage-v8 && npm test && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @vitest/coverage-v8` outputs a resolved version (no `UNMET` or `missing` text)
    - `npm test` exits 0
    - `npm run ci` exits 0 (biome + tsc)
    - vitest.config.ts contains `provider: 'v8'` and `thresholds`
    - vitest.config.ts retains `environment: 'happy-dom'`, `globals: true`, `setupFiles`, `passWithNoTests: true`
  </acceptance_criteria>
  <done>@vitest/coverage-v8 installed, coverage config in vitest.config.ts, all existing checks still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create bank types, extract DEFAULT_SECTIONS from prototype, and write structural tests</name>
  <files>
    src/data/bank/types.ts,
    src/data/bank/frontend.ts,
    src/data/bank/design.ts,
    src/data/bank/backend.ts,
    src/data/bank/environment.ts,
    src/data/bank/testing.ts,
    src/data/bank/cicd.ts,
    src/data/bank/tooling.ts,
    src/data/bank/integrations.ts,
    src/data/bank/ai.ts,
    src/data/bank/index.ts,
    src/data/bank/bank.test.ts
  </files>
  <read_first>
    - stack-checklist.html line 649 — DEFAULT_SECTIONS array verbatim source of truth; extract all 9 groups from this exact location
    - .planning/phases/02-question-bank-scoring-engine/02-PATTERNS.md lines 34–218 — types.ts pattern, group file pattern (name→export mapping table), index.ts barrel pattern, bank.test.ts assertions
    - .planning/phases/02-question-bank-scoring-engine/02-RESEARCH.md lines 86–103 — verified group/topic/question counts per group
    - .planning/phases/02-question-bank-scoring-engine/02-RESEARCH.md lines 288–374 — Pattern 1 (TypeScript types + group file + index barrel)
    - .planning/phases/02-question-bank-scoring-engine/02-RESEARCH.md lines 609–657 — bank.test.ts full example
    - .planning/phases/01-foundation-scaffolding/01-01-SUMMARY.md — Bundler moduleResolution confirmed (all relative imports need .js extension)
  </read_first>
  <behavior>
    - TDD: Write bank.test.ts FIRST with 5 assertions (9 groups, 86 topics, ≥1000 questions, all levels valid, all ids non-empty). Run npm test — tests fail with "Cannot find module './index.js'". Then create all source files. Tests pass.
    - src/data/bank/types.ts exports: Difficulty (string literal union), Question (readonly q + level), Topic (readonly id/name/desc/tag/questions), Section (readonly id/label/icon/items), DIFFICULTY_COEFFICIENTS (Record<Difficulty,number> = {novice:1.00, intermediate:1.25, advanced:1.50, expert:1.75})
    - Each group file (frontend.ts, design.ts, ...) exports exactly one named const typed `: Section` with verbatim data from stack-checklist.html DEFAULT_SECTIONS
    - Group file → export name: frontend→frontendSection, design→designSection, backend→backendSection, environment→environmentSection, testing→testingSection, cicd→cicdSection, tooling→toolingSection, integrations→integrationsSection, ai→aiSection
    - index.ts exports DEFAULT_SECTIONS: readonly Section[] assembled in group order matching stack-checklist.html; re-exports Difficulty/Question/Topic/Section as type-only; re-exports DIFFICULTY_COEFFICIENTS as value
    - All relative imports in src/ use .js extension (moduleResolution: Bundler — RESEARCH.md Pitfall 4)
    - Do NOT use TypeScript enum for Difficulty — use string literal union only
    - Do NOT use `as const` on individual group exports — typed `: Section` interface enforces readonly
    - After creating each group file, run `npm run check` to catch Biome import-order issues before proceeding
  </behavior>
  <action>
    Step 1 — RED: Create src/data/bank/bank.test.ts with 5 describe/it assertions (import DEFAULT_SECTIONS from './index.js'): group count === 9, topic count === 86, question count >= 1000, every question level in valid set, every id/q non-empty. Run `npm test src/data/bank/bank.test.ts` — must fail (module missing).

    Step 2 — Create src/data/bank/types.ts: export Difficulty union, Question/Topic/Section interfaces (all properties readonly), DIFFICULTY_COEFFICIENTS record. No imports needed in this file.

    Step 3 — Create all 9 group files in order: frontend.ts, design.ts, backend.ts, environment.ts, testing.ts, cicd.ts, tooling.ts, integrations.ts, ai.ts. For each file: single `import type { Section } from './types.js'` at top; single named export typed `: Section`; question data extracted verbatim from stack-checklist.html DEFAULT_SECTIONS. The source array is at line 649 of stack-checklist.html — read that file to extract the exact question text and level values. Run `npm run check` after each file to auto-fix Biome import ordering.

    Step 4 — Create src/data/bank/index.ts: import all 9 group constants; export DEFAULT_SECTIONS as `readonly Section[]` in prototype group order (frontend, design, backend, environment, testing, cicd, tooling, integrations, ai); re-export types with `export type { Difficulty, Question, Topic, Section } from './types.js'`; re-export DIFFICULTY_COEFFICIENTS as value.

    Step 5 — GREEN: Run `npm test src/data/bank/bank.test.ts` — all 5 tests must pass. Then run `npm test && npm run ci` — full suite must pass.

    Key constraints from RESEARCH.md / PATTERNS.md:
    - Use `typeof score !== 'number'` null-guard pattern (not `!score`) — this is for the scorer (Wave 2) but establish the null discipline here
    - Question key scheme is `${topic.id}-${questionIndex}` — do not embed keys in bank data; the scorer derives them
    - DEFAULT_SECTIONS order must exactly match prototype group order for Phase 4+ render order
  </action>
  <verify>
    <automated>npm test src/data/bank/bank.test.ts && npm test && npm run ci</automated>
  </verify>
  <acceptance_criteria>
    - `npm test src/data/bank/bank.test.ts` exits 0 with 5 tests passing
    - DEFAULT_SECTIONS.length === 9
    - Total topics across all groups === 86
    - Total questions across all groups >= 1000
    - Every question.level is in {'novice','intermediate','advanced','expert'}
    - Every group/topic/question has a non-empty id/q field
    - DIFFICULTY_COEFFICIENTS exported from types.ts with exactly {novice:1.00, intermediate:1.25, advanced:1.50, expert:1.75}
    - `npm run ci` exits 0 (biome + tsc — no type errors, no import extension violations)
    - No TypeScript enum used anywhere in src/data/bank/ (grep confirms `enum` absent)
    - All relative imports in src/data/bank/ use .js extension
  </acceptance_criteria>
  <done>All 9 group files created with verbatim prototype data, types.ts defines the contract, index.ts assembles DEFAULT_SECTIONS, bank.test.ts passes with exact count assertions, npm run ci exits 0.</done>
</task>

</tasks>

## Artifacts This Phase Produces

| File | Type | Consumed By |
|------|------|-------------|
| `src/data/bank/types.ts` | TypeScript types + DIFFICULTY_COEFFICIENTS | Scorer (Wave 2), Storage (Phase 3), UI (Phase 4+) |
| `src/data/bank/frontend.ts` | Section data constant (13 topics) | index.ts |
| `src/data/bank/design.ts` | Section data constant (5 topics) | index.ts |
| `src/data/bank/backend.ts` | Section data constant (22 topics) | index.ts |
| `src/data/bank/environment.ts` | Section data constant (8 topics) | index.ts |
| `src/data/bank/testing.ts` | Section data constant (6 topics) | index.ts |
| `src/data/bank/cicd.ts` | Section data constant (6 topics) | index.ts |
| `src/data/bank/tooling.ts` | Section data constant (5 topics) | index.ts |
| `src/data/bank/integrations.ts` | Section data constant (9 topics) | index.ts |
| `src/data/bank/ai.ts` | Section data constant (12 topics) | index.ts |
| `src/data/bank/index.ts` | DEFAULT_SECTIONS barrel + type re-exports | Phase 3 Storage, Phase 4+ UI |
| `src/data/bank/bank.test.ts` | BANK-01 structural assertions | CI gate |
| `vitest.config.ts` | Coverage config (v8, 100% thresholds on src/scoring/**) | Wave 2 coverage runs |
| `package.json` | @vitest/coverage-v8 devDependency added | npm install |
| `package-lock.json` | Lockfile updated | CI |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time constant → runtime bundle | DEFAULT_SECTIONS is frozen at build time; no runtime user input enters this module |
| Test runner → bank module | Vitest imports bank directly; no network or file I/O at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | DIFFICULTY_COEFFICIENTS in types.ts | mitigate | Declare as `Record<Difficulty, number>` with `as const`; TypeScript prevents any downstream assignment; no mutable export |
| T-02-02 | Tampering | DEFAULT_SECTIONS group data | mitigate | All Section/Topic/Question interface properties are `readonly`; TypeScript enforces immutability at compile time; bank data is a build-time constant with no runtime write path |
| T-02-03 | Information Disclosure | Question text in DEFAULT_SECTIONS | accept | Bank content is English technical interview questions — no PII, no secrets; content is intentionally bundled for end users; React escapes on render (Phase 4+) |
| T-02-04 | Tampering | npm install @vitest/coverage-v8 | mitigate | Package pre-approved in RESEARCH.md Package Legitimacy Audit (OK verdict); version 4.1.9 co-versioned with installed Vitest; no [ASSUMED]/[SUS] flag |
| T-02-SC | Tampering | npm/cargo installs | mitigate | slopcheck passed — single package @vitest/coverage-v8 reviewed and approved in RESEARCH.md audit table |
</threat_model>

<verification>
Phase 2 Wave 1 complete when:
- `npm test` exits 0 with all bank.test.ts assertions passing (5 tests minimum)
- `npm run ci` exits 0 (biome + tsc — no type errors, no import extension issues)
- `npm ls @vitest/coverage-v8` resolves at 4.1.9
- DEFAULT_SECTIONS accessible via `import { DEFAULT_SECTIONS } from './src/data/bank/index.js'` with 9 groups
- vitest.config.ts contains coverage block with provider:v8 scoped to src/scoring/**
</verification>

<success_criteria>
1. DEFAULT_SECTIONS exports 9 groups, exactly 86 topics, ≥1000 questions, all questions have valid Difficulty level
2. DIFFICULTY_COEFFICIENTS exported with exact coefficient values (novice=1.00, intermediate=1.25, advanced=1.50, expert=1.75)
3. bank.test.ts passes all 5 structural assertions
4. @vitest/coverage-v8 installed; vitest.config.ts has 100%-threshold coverage config targeting src/scoring/**
5. npm run ci exits 0 — no type errors, no Biome violations
6. No enum, no widened types, all relative imports use .js extension
</success_criteria>

<output>
Create `.planning/phases/02-question-bank-scoring-engine/02-01-SUMMARY.md` when done.
</output>
