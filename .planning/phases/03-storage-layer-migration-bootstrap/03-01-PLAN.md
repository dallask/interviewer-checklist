---
phase: 03-storage-layer-migration-bootstrap
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/storage/types.ts
  - src/storage/migrations/fixtures/v1-snapshot.json
  - src/storage/migrations/v1-to-v2.ts
  - src/storage/migrations/v1-to-v2.test.ts
  - src/storage/migrations/index.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02

must_haves:
  truths:
    - "npm test exits 0 after this plan — vitest-chrome global available in all test files"
    - "migrateV1ToV2 maps V1Schema fields to V2 {manifest, session} shape without mutating frozen input"
    - "v1-to-v2.test.ts RED gate commit exists before v1-to-v2.ts is written"
    - "v2 output from migrateV1ToV2 passes valibot V2SessionSchema and V2ManifestSchema safeParse"
    - "migration pipeline runner (migrations/index.ts) iterates an array of {fromVersion, fn} entries — extensible for future phases"
  artifacts:
    - path: "src/storage/types.ts"
      provides: "V1Schema, V2Manifest, V2Session TypeScript interfaces + valibot schemas + factory functions createDefaultManifest/createDefaultSession"
      exports:
        - "V1Schema"
        - "V2Manifest"
        - "V2ManifestSchema"
        - "V2Session"
        - "V2SessionSchema"
        - "createDefaultManifest"
        - "createDefaultSession"
    - path: "src/storage/migrations/v1-to-v2.ts"
      provides: "pure migrateV1ToV2 function mapping V1Schema → {manifest: V2Manifest, session: V2Session}"
      exports: ["migrateV1ToV2"]
    - path: "src/storage/migrations/v1-to-v2.test.ts"
      provides: "fixture-pinned unit tests for migrateV1ToV2"
      min_lines: 40
    - path: "src/storage/migrations/fixtures/v1-snapshot.json"
      provides: "anonymized frozen V1 fixture matching V1Schema interface"
    - path: "src/storage/migrations/index.ts"
      provides: "runMigrations pipeline orchestrator"
      exports: ["runMigrations"]
    - path: "src/test/setup.ts"
      provides: "vitest-chrome chrome global installed for all test files"
      contains: "Object.assign(globalThis, { chrome })"
    - path: "vitest.config.ts"
      provides: "coverage include array extended with src/storage/**"
      contains: "src/storage/**"
  key_links:
    - from: "src/storage/types.ts"
      to: "valibot"
      via: "import * as v from 'valibot'"
      pattern: "v\\.object\\|v\\.literal"
    - from: "src/storage/migrations/v1-to-v2.test.ts"
      to: "src/storage/migrations/fixtures/v1-snapshot.json"
      via: "import with { type: 'json' }"
      pattern: "v1-snapshot\\.json"
    - from: "src/storage/migrations/index.ts"
      to: "src/storage/migrations/v1-to-v2.ts"
      via: "named import migrateV1ToV2"
      pattern: "migrateV1ToV2"
---

<objective>
Install the three new dependencies (vitest-chrome, zustand, valibot), extend the vitest test harness for chrome API mocking, define all V1/V2 schema types and valibot validators, author the v1→v2 pure migration function using TDD, and wire the migration pipeline runner.

Purpose: Establishes the foundational type contracts and migration logic that all later storage plans depend on. Every downstream file in Phase 3 imports from `src/storage/types.ts` and the migration pipeline is tested in isolation before the adapter or bootstrap code exists.

Output: Installed packages, types.ts with valibot schemas, v1-to-v2.ts with fixture-pinned tests, migrations/index.ts pipeline runner, vitest-chrome global in setup.ts, coverage config extended.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-storage-layer-migration-bootstrap/03-CONTEXT.md
@.planning/phases/03-storage-layer-migration-bootstrap/03-RESEARCH.md
@.planning/phases/03-storage-layer-migration-bootstrap/03-PATTERNS.md
@.planning/phases/02-question-bank-scoring-engine/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, extend vitest harness, define storage types and valibot schemas</name>
  <files>
    package.json,
    package-lock.json,
    vitest.config.ts,
    src/test/setup.ts,
    src/storage/types.ts
  </files>
  <read_first>
    - vitest.config.ts — full file (extend coverage.include array)
    - src/test/setup.ts — full file (append vitest-chrome lines below existing import)
    - src/scoring/scoring.ts lines 1-5 — import type pattern with .js extensions
    - .planning/phases/03-storage-layer-migration-bootstrap/03-RESEARCH.md Pattern 4 (vitest-chrome setup) and Pattern 5 (valibot schema definitions)
    - .planning/phases/03-storage-layer-migration-bootstrap/03-PATTERNS.md sections for types.ts and setup.ts
  </read_first>
  <action>
    Step 1 — Install packages. Run: npm install --save-dev vitest-chrome@0.1.0 and npm install zustand@5.0.14 valibot@1.4.1. Verify each exits 0. Do not install any other packages.

    Step 2 — Extend vitest.config.ts coverage block. The existing include array is ['src/scoring/**']. Change it to ['src/scoring/**', 'src/storage/**']. No other changes to the file.

    Step 3 — Extend src/test/setup.ts. The file currently contains only `import '@testing-library/jest-dom/vitest';`. Append two lines below it per RESEARCH.md Pattern 4: `import * as chrome from 'vitest-chrome';` then `Object.assign(globalThis, { chrome });`. Preserve the existing import on line 1.

    Step 4 — Create src/storage/types.ts. This file is the single source of truth for all Phase 3 types and valibot schemas. Follow PATTERNS.md types.ts section exactly:

    - All interfaces use readonly on primitive fields
    - Named exports only, no default exports
    - .js extension on relative imports (none here — only valibot import)
    - Import valibot as namespace: `import * as v from 'valibot';`

    Export the following in this order:

    a) V1Schema TypeScript interface matching RESEARCH.md "V1 Schema — Exact Format" section verbatim. Fields: version (number), sections (unknown[] | null), sectionOpen, cardOpen, topicOverride, questionScore (both Record<string, number>), cardComment, questionComment, questionNoteOpen (Record<string, boolean>), candidate (object with optional name/email/role/date/interviewer/details strings), customQuestions (Record<string, Array<{id: number, text: string, level: string}>>), customSeq (number), filters (string[]), levels (string[]), search (string), hideReviewed (boolean), sidebarCollapsed (boolean), sidebarGroups ({search: boolean, difficulty: boolean, sections: boolean, actions: boolean}).

    b) CandidateSchema valibot object with all optional string fields per RESEARCH.md Pattern 5.

    c) V2SessionSchema valibot object: version v.literal(2), id v.string(), questionScore v.record(v.string(), v.nullable(v.number())), topicOverride v.record(v.string(), v.nullable(v.number())), cardComment v.record(v.string(), v.string()), questionComment v.record(v.string(), v.string()), candidate CandidateSchema, customQuestions v.record(v.string(), v.array(v.object({id: v.number(), text: v.string(), level: v.string()}))), customSeq v.number(). Per RESEARCH.md Pattern 5 exactly.

    d) V2ManifestSchema valibot object: version v.literal(2), activeSessionId v.string(), sessions v.array(v.object({id: v.string(), name: v.string(), createdAt: v.string(), updatedAt: v.string()})).

    e) Derived types: `export type V2Session = v.InferOutput<typeof V2SessionSchema>;` and `export type V2Manifest = v.InferOutput<typeof V2ManifestSchema>;`. Use InferOutput (valibot v1 API — not Output).

    f) Factory functions:
    - `export function createDefaultManifest(): V2Manifest` — creates a V2Manifest with a single session using `crypto.randomUUID()` for the id, name "Session 1", and ISO timestamp for createdAt/updatedAt. activeSessionId = the generated id. version = 2. No uuid library — use crypto.randomUUID() built-in (per RESEARCH.md "Don't Hand-Roll" table).
    - `export function createDefaultSession(id: string): V2Session` — creates an empty V2Session with the given id, version 2, all Record fields as {}, candidate as {}, customSeq 0.

    Step 5 — Run `npm run ci` (biome ci + tsc --noEmit). Fix any Biome import-order violations with `npm run check`. Run `npm test` — should pass (existing 59 tests, vitest-chrome now installed). Record any failures.

    No fenced code blocks in this action. All type names, method names, and import paths are stated inline above as prose identifiers.
  </action>
  <verify>
    <automated>npm run ci && npm test</automated>
  </verify>
  <done>
    - npm install exits 0 for all three packages; package.json lists vitest-chrome in devDependencies, zustand and valibot in dependencies
    - vitest.config.ts coverage.include contains both 'src/scoring/**' and 'src/storage/**'
    - src/test/setup.ts has three lines: jest-dom import, vitest-chrome import, Object.assign globalThis
    - src/storage/types.ts exports V1Schema, CandidateSchema, V2SessionSchema, V2ManifestSchema, V2Session (type), V2Manifest (type), createDefaultManifest, createDefaultSession
    - npm run ci exits 0 (no biome violations, no tsc errors)
    - npm test exits 0 (all pre-existing tests pass with vitest-chrome now in global scope)
  </done>
</task>

<task type="tdd">
  <name>Task 2 (TDD): v1-to-v2 fixture, migration function, and pipeline runner</name>
  <files>
    src/storage/migrations/fixtures/v1-snapshot.json,
    src/storage/migrations/v1-to-v2.test.ts,
    src/storage/migrations/v1-to-v2.ts,
    src/storage/migrations/index.ts
  </files>
  <read_first>
    - src/storage/types.ts (just created in Task 1) — V1Schema interface and V2SessionSchema/V2ManifestSchema for test assertions
    - src/scoring/scoring.test.ts lines 1-40 — test file structure: describe/it imports, inline fixture pattern, module-level const
    - .planning/phases/03-storage-layer-migration-bootstrap/03-RESEARCH.md "Example 2: Frozen fixture in migration test" and V1 Schema section
    - .planning/phases/03-storage-layer-migration-bootstrap/03-PATTERNS.md sections for v1-to-v2.ts, v1-to-v2.test.ts, and migrations/index.ts
  </read_first>
  <behavior>
    - migrateV1ToV2 given frozen V1 fixture: returns object with manifest.version === 2 and session.version === 2
    - migrateV1ToV2 given frozen V1 fixture: session.questionScore equals fixture.questionScore (nullish coalescing — {} if missing)
    - migrateV1ToV2 given frozen V1 fixture: session.topicOverride equals fixture.topicOverride
    - migrateV1ToV2 given frozen V1 fixture: session.candidate equals fixture.candidate
    - migrateV1ToV2 given frozen V1 fixture: session.customQuestions equals fixture.customQuestions
    - migrateV1ToV2 given frozen V1 fixture: does NOT mutate the input — JSON.stringify(fixture) before === JSON.stringify(fixture) after
    - migrateV1ToV2 given frozen V1 fixture: v.safeParse(V2SessionSchema, result.session).success === true (valibot validation passes)
    - migrateV1ToV2 given frozen V1 fixture: v.safeParse(V2ManifestSchema, result.manifest).success === true
    - migrateV1ToV2 given V1 with missing questionScore field: session.questionScore === {}
    - runMigrations given raw data with no version field: returns {manifest, session} via migrateV1ToV2
    - runMigrations given raw data with version === 2: returns null (caller handles — already migrated)
  </behavior>
  <action>
    Follow RED → GREEN → REFACTOR. Commit at each gate.

    RED PHASE — create fixture and failing tests first:

    Step 1 — Create src/storage/migrations/fixtures/v1-snapshot.json. This is an anonymized V1Schema object matching the V1Schema interface from types.ts. Use representative non-empty data:
    - version: 4 (prototype's SCHEMA_VERSION)
    - sections: null
    - sectionOpen: {"javascript": true, "typescript": false}
    - cardOpen: {"js-async": true}
    - topicOverride: {"js-async": 7}
    - questionScore: {"js-async-0": 8, "js-async-1": 6, "js-async-2": 9}
    - cardComment: {"js-async": "Strong understanding of Promises"}
    - questionComment: {"js-async-0": "Explained event loop well"}
    - questionNoteOpen: {"js-async": false}
    - candidate: {"name": "Test Candidate", "email": "test@example.com", "role": "Frontend Engineer", "date": "2026-01-15", "interviewer": "Reviewer A", "details": "Strong candidate overall"}
    - customQuestions: {"js-async": [{"id": 1, "text": "Custom async question", "level": "intermediate"}]}
    - customSeq: 1
    - filters: ["javascript"]
    - levels: ["intermediate", "advanced"]
    - search: ""
    - hideReviewed: false
    - sidebarCollapsed: false
    - sidebarGroups: {"search": true, "difficulty": true, "sections": true, "actions": false}

    Step 2 — Create src/storage/migrations/v1-to-v2.test.ts. Import {describe, expect, it} from 'vitest'. Import * as v from 'valibot'. Import {migrateV1ToV2} from './v1-to-v2.js'. Import {V2SessionSchema, V2ManifestSchema} from '../types.js'. Import v1FixtureRaw from './fixtures/v1-snapshot.json' with { type: 'json' } (use 'with', not 'assert' — 'assert' is deprecated ES2025). Cast to V1Schema type via `as import('../types.js').V1Schema`. Const FROZEN_V1 = Object.freeze(v1FixtureRaw) as V1Schema.

    Write all tests listed in the behavior block above. Use describe('migrateV1ToV2', ...) with nested it() for each behavior. For the "does not mutate" test: const before = JSON.stringify(FROZEN_V1); migrateV1ToV2(FROZEN_V1); expect(JSON.stringify(FROZEN_V1)).toBe(before). For valibot tests: const r = v.safeParse(V2SessionSchema, result.session); expect(r.success).toBe(true). Add a separate describe('runMigrations', ...) section with the two runMigrations tests — these will fail until migrations/index.ts is created.

    Also import {runMigrations} from './index.js' for the runMigrations tests.

    Step 3 — Commit RED gate: git commit with message `test(03-01): add failing migration tests (TDD RED)`. The tests for migrateV1ToV2 must fail because v1-to-v2.ts does not exist yet.

    GREEN PHASE:

    Step 4 — Create src/storage/migrations/v1-to-v2.ts. Signature: `export function migrateV1ToV2(raw: Readonly<V1Schema>): { manifest: V2Manifest; session: V2Session }`. Import types with .js extensions. Use crypto.randomUUID() for session ID (no import needed — Web Crypto built-in). Implement per RESEARCH.md Pattern 3 and PATTERNS.md v1-to-v2.ts section: map raw.questionScore ?? {} for each field, build V2Manifest with version 2, build V2Session with version 2. Add JSDoc block comment above the function. No try/catch — let errors surface to bootstrap()'s catch block.

    Step 5 — Create src/storage/migrations/index.ts. Export: `export function runMigrations(raw: unknown): { manifest: V2Manifest; session: V2Session } | null`. Implement as an array of migration entries: `const MIGRATIONS: Array<{ fromVersion: number; fn: (r: unknown) => { manifest: V2Manifest; session: V2Session } }> = [{ fromVersion: 1, fn: (r) => migrateV1ToV2(r as V1Schema) }]`. For raw data with version field === 2, return null. For any other data (no version or version !== 2), find the matching migration entry and call fn. Treat missing/undefined version as needing v1 migration. This design allows future phases to push new entries without modifying bootstrap.ts.

    Step 6 — Run `npx vitest run src/storage/migrations/`. All tests must pass.

    Step 7 — Run `npm run ci` and fix any Biome violations with `npm run check`.

    Step 8 — Commit GREEN gate: git commit with message `feat(03-01): implement v1-to-v2 migration and pipeline runner (TDD GREEN)`.

    REFACTOR PHASE:

    Step 9 — Review for clarity only. No logic changes. If JSDoc comments or naming needs polish, apply. Run `npm test` to confirm still green. Commit if refactored.
  </action>
  <verify>
    <automated>npx vitest run src/storage/migrations/ && npm run ci</automated>
  </verify>
  <done>
    - src/storage/migrations/fixtures/v1-snapshot.json exists with all V1Schema fields populated
    - src/storage/migrations/v1-to-v2.ts exports migrateV1ToV2 with Readonly<V1Schema> parameter
    - src/storage/migrations/v1-to-v2.test.ts has >= 9 test cases covering field mapping, no-mutation, valibot validation, and missing-field nullish coalescing
    - src/storage/migrations/index.ts exports runMigrations with array-based migration chain
    - All migration tests pass: npx vitest run src/storage/migrations/ exits 0
    - npm run ci exits 0 (Biome + tsc)
    - RED commit exists before GREEN commit in git log
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valibot schema → V1 fixture | V1 fixture is a static JSON file; object.freeze() prevents mutation; test-only path |
| migration output → valibot validator | migrateV1ToV2 output is validated by V2SessionSchema/V2ManifestSchema in tests; safeParse never throws |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | v1-snapshot.json fixture | mitigate | Object.freeze() applied in test; mutation detected by JSON.stringify comparison test |
| T-03-01-02 | Tampering | npm package installs (vitest-chrome, zustand, valibot) | accept | All three packages verified in RESEARCH.md Package Legitimacy Audit; vitest-chrome=Approved, zustand=Approved (SUS false positive), valibot=Approved (SUS false positive); no postinstall scripts; canonical repos confirmed |
| T-03-01-03 | Information Disclosure | V1Schema candidate fields in fixture | mitigate | Fixture uses anonymized placeholder data: "Test Candidate", "test@example.com" — no real PII |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | Package legitimacy audit in RESEARCH.md covers all three packages with Approved verdicts; no [SLOP] packages; SUS flags are false positives from recency heuristic |
</threat_model>

<verification>
After both tasks complete:

1. `npm test` exits 0 — all pre-existing tests pass + new migration tests pass
2. `npm run ci` exits 0 — no Biome violations, no TypeScript errors
3. `grep -c "src/storage" vitest.config.ts` returns >= 1
4. `grep -c "Object.assign(globalThis" src/test/setup.ts` returns 1
5. `grep -c "vitest-chrome" package.json` returns >= 1
6. `grep -c "valibot" package.json` returns >= 1
7. `grep -c "zustand" package.json` returns >= 1
8. `npx vitest run src/storage/migrations/` exits 0 with all tests passing
</verification>

<success_criteria>
- All three packages installed and visible in package.json
- vitest-chrome chrome global available in test files (no "chrome is not defined" errors)
- src/storage/types.ts exports V1Schema, V2Session, V2Manifest, valibot schemas, and factory functions
- migrateV1ToV2 passes all fixture-pinned tests including no-mutation and valibot output validation
- runMigrations returns null for v2 data and delegates to migrateV1ToV2 for legacy data
- Coverage config extended to include src/storage/**
- npm run ci exits 0
</success_criteria>

<output>
Create `.planning/phases/03-storage-layer-migration-bootstrap/03-01-SUMMARY.md` when done.
</output>
