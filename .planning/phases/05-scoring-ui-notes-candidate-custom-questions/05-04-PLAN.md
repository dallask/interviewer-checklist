---
phase: 05-scoring-ui-notes-candidate-custom-questions
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/app.ts
  - src/store/app.test.ts
  - src/app/main.tsx
autonomous: true
gap_closure: true
requirements: [SCORE-06]

must_haves:
  truths:
    - "resetAll() clears selectedDifficulties, selectedSections, searchQuery, and hideMarked in addition to scoring data"
    - "resetAll tests assert that all four filter fields are cleared after a reset"
    - "npm run ci exits 0 (biome + tsc clean)"
  artifacts:
    - path: "src/store/app.ts"
      provides: "resetAll() that clears filters per SCORE-06"
      contains: "selectedDifficulties: new Set()"
    - path: "src/store/app.test.ts"
      provides: "filter-reset assertions in the resetAll describe block"
      contains: "selectedDifficulties"
    - path: "src/app/main.tsx"
      provides: "Biome-compliant type cast on session read"
  key_links:
    - from: "src/store/app.ts"
      to: "src/store/app.test.ts"
      via: "resetAll() action asserted in test suite"
      pattern: "resetAll"
---

<objective>
Fix two CI-blocking gaps found during phase 5 verification:

1. SCORE-06 filter reset (Gap 1): `resetAll()` does not clear `selectedDifficulties`, `selectedSections`, `searchQuery`, or `hideMarked`, contradicting the ResetConfirmDialog copy ("and active filters") and REQUIREMENTS.md SCORE-06. The fix is a one-line addition to the `set({...})` call and new test assertions.

2. Biome formatting violation (Gap 2): `src/app/main.tsx` line 44 has a type cast (`as V3Session | undefined`) on a line that exceeds biome's column limit. `npm run ci` exits non-zero until this is auto-fixed.

Purpose: Bring the phase to a fully verified state where npm run ci exits 0 and SCORE-06's filter-reset contract is met.
Output: Updated src/store/app.ts, src/store/app.test.ts, src/app/main.tsx; all tests pass; npm run ci exits 0.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix resetAll() to clear filter state and add test assertions (SCORE-06, Gap 1)</name>
  <files>src/store/app.ts, src/store/app.test.ts</files>

  <read_first>
    - src/store/app.ts lines 235–244: current `resetAll()` implementation — only clears scores/overrides/notes/topicNotes/customQuestions/candidate; the `set({...})` call is missing the four filter fields
    - src/store/app.test.ts lines 283–316: the existing `resetAll` describe block with current assertions and the `resetAll preserves activeSessionId` test — new filter assertions go inside the same describe block, BEFORE the `preserves activeSessionId` test
    - src/store/app.ts lines 51–66: the AppState type declaration — confirms field names: `selectedDifficulties: Set<Difficulty>`, `selectedSections: Set<string>`, `searchQuery: string`, `hideMarked: boolean`
  </read_first>

  <behavior>
    - Test: before resetAll(), set selectedDifficulties to new Set(['expert']), selectedSections to new Set(['be']), searchQuery to 'foo', hideMarked to true; after resetAll(), expect selectedDifficulties to deep-equal new Set(), selectedSections to deep-equal new Set(), searchQuery to equal '', hideMarked to equal false
    - Test: resetAll() still preserves activeSessionId (existing test must continue to pass unchanged)
    - Test: resetAll() still clears scores, overrides, notes, topicNotes, customQuestions, candidate (existing assertions must pass unchanged)
  </behavior>

  <action>
    STEP 1 — Write failing tests first (RED):

    In src/store/app.test.ts, inside the `describe('ScoringActions', ...)` block, add a new `it` block immediately before the existing `it('resetAll preserves activeSessionId', ...)` test:

    ```
    it('resetAll clears selectedDifficulties, selectedSections, searchQuery, and hideMarked', () => {
      useAppStore.setState({
        selectedDifficulties: new Set(['expert']),
        selectedSections: new Set(['be']),
        searchQuery: 'foo',
        hideMarked: true,
      });
      useAppStore.getState().resetAll();
      const state = useAppStore.getState();
      expect(state.selectedDifficulties).toEqual(new Set());
      expect(state.selectedSections).toEqual(new Set());
      expect(state.searchQuery).toBe('');
      expect(state.hideMarked).toBe(false);
    });
    ```

    Run `npx vitest run src/store/app.test.ts --reporter=dot` — it MUST exit non-zero (new test fails because resetAll does not yet clear filters).

    STEP 2 — Fix the implementation (GREEN):

    In src/store/app.ts, in the `resetAll()` action (lines ~235–244), add the four filter fields to the `set({...})` call:

    ```
    resetAll: () =>
      set({
        scores: {},
        overrides: {},
        notes: {},
        topicNotes: {},
        customQuestions: [],
        candidate: null,
        selectedDifficulties: new Set(),
        selectedSections: new Set(),
        searchQuery: '',
        hideMarked: false,
        // activeSessionId is NOT reset — session identity must persist across resets
      }),
    ```

    Run `npx vitest run src/store/app.test.ts --reporter=dot` — MUST exit 0 with all tests passing.

    Run `npx vitest run --reporter=dot` — full suite MUST exit 0.
  </action>

  <verify>
    <automated>npx vitest run src/store/app.test.ts --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - src/store/app.ts resetAll() sets selectedDifficulties: new Set(), selectedSections: new Set(), searchQuery: '', hideMarked: false alongside the existing score/notes/candidate clears
    - src/store/app.test.ts contains a test asserting all four filter fields equal their cleared defaults after resetAll()
    - The existing 'resetAll preserves activeSessionId' test still passes
    - npx vitest run src/store/app.test.ts exits 0 with all tests passing
    - npx vitest run (full suite) exits 0
  </acceptance_criteria>

  <done>resetAll() fulfills SCORE-06: "Reset all clears scores, notes, custom questions, candidate details, and filters." All filter state returns to default on reset. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix biome formatting violation in main.tsx (Gap 2)</name>
  <files>src/app/main.tsx</files>

  <read_first>
    - src/app/main.tsx lines 42–46: the session hydration block introduced in the post-SUMMARY commit; line 44 contains `const session = sessionRaw[...] as V3Session | undefined;` which exceeds biome's column limit and needs to be split across lines
  </read_first>

  <action>
    Run the biome auto-formatter on the file:

      npx biome format --write src/app/main.tsx

    Then verify the result:

      npx biome check src/app/main.tsx

    This should exit 0. If biome splits line 44 into a multiline form such as:

      const session = sessionRaw[`session:${activeSessionId}`] as
        | V3Session
        | undefined;

    that is the expected output — do not revert it. Do NOT manually edit the line; let biome produce its canonical format.

    After formatting, run the full CI check:

      npm run ci

    MUST exit 0.
  </action>

  <verify>
    <automated>npx biome check src/app/main.tsx && npm run ci</automated>
  </verify>

  <acceptance_criteria>
    - npx biome check src/app/main.tsx exits 0 (no formatting errors)
    - npm run ci exits 0 (biome + tsc both clean)
    - src/app/main.tsx is unchanged in behavior — only whitespace/line-wrap changed by biome format
  </acceptance_criteria>

  <done>npm run ci exits 0. No formatting violations in src/app/main.tsx. The phase's done-criteria "npm run ci exits 0" is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store state → reset action | resetAll() is called after user confirms intent in a dialog; no external input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | resetAll() filter fields | accept | Filter fields are client-side UI state with no security impact; incorrect values produce visual-only inconsistency |
| T-05-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; biome is already in devDependencies |
</threat_model>

<verification>
After both tasks complete:

1. `npx vitest run --reporter=dot` — all tests pass (count >= 330, 0 failures)
2. `npm run ci` — exits 0 (biome + tsc clean)
3. Read src/store/app.ts resetAll() — confirm it includes selectedDifficulties, selectedSections, searchQuery, hideMarked
4. Read src/store/app.test.ts — confirm new test asserting filter clearing is present
</verification>

<success_criteria>
- resetAll() clears all four filter fields (selectedDifficulties, selectedSections, searchQuery, hideMarked) per SCORE-06
- Tests assert filter clearing behavior
- npm run ci exits 0 with no biome or tsc errors
- Full Vitest suite passes (all 330+ tests)
</success_criteria>

<output>
Create `.planning/phases/05-scoring-ui-notes-candidate-custom-questions/05-04-SUMMARY.md` when done
</output>
