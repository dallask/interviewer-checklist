---
phase: 06-multiple-named-sessions-switcher
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/SessionSwitcherModal.tsx
  - src/components/SessionSwitcherModal.test.tsx
  - src/components/SessionRow.tsx
  - src/components/SessionRow.test.tsx
  - src/components/DeleteSessionConfirmDialog.tsx
  - src/components/DeleteSessionConfirmDialog.test.tsx
  - src/components/UndoToast.tsx
  - src/components/UndoToast.test.tsx
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03

must_haves:
  truths:
    - "SessionSwitcherModal renders all sessions from manifest.sessions with compact text rows"
    - "The session list scrolls after 8 rows (max-h-[352px] overflow-y-auto) per locked CONTEXT.md decision"
    - "Active session row has blue tint background and checkmark indicator"
    - "SessionRow shows inline rename input (replacing the name button) when ✎ is clicked; Enter/blur commits; Escape cancels; empty name cancels rename"
    - "Blur on rename input does NOT commit if focus moved to another element within the same <li> (Pitfall 5 guard)"
    - "Rename input has maxLength={50} per locked CONTEXT.md decision"
    - "DeleteSessionConfirmDialog follows ResetConfirmDialog pattern: native <dialog>, manual focus trap, 'Keep session'/'Delete session' buttons"
    - "UndoToast mounts from App.tsx root (not inside SessionSwitcherModal); reads undoBuffer from store; renders null when undoBuffer is null"
    - "UndoToast has role='status' aria-live='polite' aria-atomic='true' and full-width fixed bottom-0 positioning"
    - "All new modals use dialogRef.current?.showModal() (never <dialog open> prop)"
    - "All new components use named exports and .js extensions on relative imports"
  artifacts:
    - path: src/components/SessionSwitcherModal.tsx
      provides: "Session list modal with header, scrollable list, New session footer"
      contains: "aria-labelledby=\"session-switcher-title\""
    - path: src/components/SessionRow.tsx
      provides: "Single session row with inline rename, duplicate, delete icon buttons"
      contains: "maxLength={50}"
    - path: src/components/DeleteSessionConfirmDialog.tsx
      provides: "Confirm dialog for session deletion"
      contains: "aria-labelledby=\"delete-session-dialog-title\""
    - path: src/components/UndoToast.tsx
      provides: "Fixed bottom toast with Undo button, auto-dismissed by store timer"
      contains: "role=\"status\""
    - path: src/components/SessionSwitcherModal.test.tsx
      provides: "Component tests for SESS-02 modal behaviors"
    - path: src/components/SessionRow.test.tsx
      provides: "Component tests for rename flow"
    - path: src/components/DeleteSessionConfirmDialog.test.tsx
      provides: "Component tests for SESS-03 confirm flow"
    - path: src/components/UndoToast.test.tsx
      provides: "Component tests for SESS-03 undo toast"
  key_links:
    - from: src/components/SessionSwitcherModal.tsx
      to: src/store/app.ts
      via: "useAppStore selectors for manifest, activeSessionId, switchSession, createSession"
      pattern: "useAppStore"
    - from: src/components/DeleteSessionConfirmDialog.tsx
      to: src/store/app.ts
      via: "useAppStore deleteSession action called on confirm"
      pattern: "deleteSession"
    - from: src/components/UndoToast.tsx
      to: src/store/app.ts
      via: "useAppStore undoBuffer selector + undoDeleteSession + setUndoBuffer"
      pattern: "undoBuffer"
---

<objective>
Build the four new UI components for session management following the native `<dialog>` + manual focus trap pattern established in Phase 5. Each component is accompanied by unit tests written first (RED) then made to pass (GREEN).

Purpose: Deliver the visible session switcher UI — all of SESS-02 (create, rename, duplicate, delete in the modal) and SESS-03 (delete confirm + undo toast). These components consume the store actions delivered in Plan 01.

Output: Four new component files + four new test files, all green.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/CandidateModal.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ResetConfirmDialog.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/StorageToast.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/CandidateModal.test.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ResetConfirmDialog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionSwitcherModal + SessionRow components with tests</name>
  <files>
    src/components/SessionSwitcherModal.tsx,
    src/components/SessionSwitcherModal.test.tsx,
    src/components/SessionRow.tsx,
    src/components/SessionRow.test.tsx
  </files>
  <read_first>
    - src/components/CandidateModal.tsx — exact focus trap + focus restore useEffect pattern to copy verbatim; Props interface shape; dialog element className pattern
    - src/components/CandidateModal.test.tsx — mock pattern (vi.mock for store + storage adapter); test structure to replicate
    - 06-PATTERNS.md — SessionSwitcherModal section (imports, Props, focus trap, dialog element, DeleteSessionConfirmDialog child, session list) and SessionRow section (Props, inline rename state, blur Pitfall 5 guard, icon button classes)
    - 06-UI-SPEC.md — Component Inventory: SessionSwitcherModal (exact JSX structure, max-h-[352px], role=listbox, aria-activedescendant) and SessionRow (default/active/rename-in-progress states, opacity-0/group-hover pattern, active row blue tint + checkmark, icon button aria-labels)
    - 06-CONTEXT.md — locked decisions: 8 rows before scroll, inline click-to-edit rename, 50-char max, "Session N" auto-name, duplicate copies clicked session not active session
  </read_first>
  <behavior>
    SessionSwitcherModal tests:
    - Renders heading "Sessions" when sessions exist in store
    - Renders one row per session in manifest.sessions
    - Active session row has aria-selected="true" on the li
    - "New session" button calls createSession action
    - Close × button calls dialog.close()
    - Focus restore: after dialog close event, focus returns to #open-session-switcher

    SessionRow tests:
    - Default: renders session name in a button with aria-label="Switch to {name}"
    - Active: li has aria-selected="true", checkmark span is visible (not text-transparent)
    - ✎ click: editing state activates; input appears with current name as value; maxLength=50
    - Rename commit (Enter): calls onRename with trimmed value; exits editing state
    - Rename cancel (Escape): calls no actions; exits editing state; original name shown
    - Blur Pitfall 5: onBlur when relatedTarget is within same li — does NOT commit
    - Blank name on blur: calls no actions (cancelRename); reverts to original name
    - ⧉ click: calls onDuplicate()
    - × click: calls onDelete()
    - Icon buttons hidden at rest (opacity-0 class); opacity-100 on group-hover/focus-within
  </behavior>
  <action>
    Create `src/components/SessionSwitcherModal.test.tsx` and `src/components/SessionRow.test.tsx` with failing tests first (RED). Then create the implementation files.

    **SessionSwitcherModal.tsx:**
    - Named export `SessionSwitcherModal`. Props: `{ dialogRef: RefObject<HTMLDialogElement | null> }`.
    - Copy the focus trap + focus restore useEffect verbatim from CandidateModal.tsx (lines 31–63); change focus restore target to `document.getElementById('open-session-switcher')?.focus()`.
    - Internal state: `pendingDelete: { id: string; name: string } | null` (controls which session's DeleteSessionConfirmDialog is open); `deleteDialogRef = useRef<HTMLDialogElement>(null)`.
    - Store selectors: `manifest = useAppStore(s => s.manifest)`, `activeSessionId = useAppStore(s => s.activeSessionId)`, `createSession = useAppStore(s => s.createSession)`, `switchSession = useAppStore(s => s.switchSession)`, `renameSession = useAppStore(s => s.renameSession)`, `duplicateSession = useAppStore(s => s.duplicateSession)`.
    - Dialog element (per UI-SPEC): `aria-labelledby="session-switcher-title"`, `className="fixed inset-0 m-auto w-full max-w-md bg-white dark:bg-gray-900 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700 p-0 flex flex-col"`. Never `<dialog open>`.
    - Header: h2 id="session-switcher-title" text "Sessions"; close button aria-label="Close sessions" calls `dialogRef.current?.close()`.
    - Session list: `<ul role="listbox" aria-label="Sessions" aria-activedescendant={\`session-row-${activeSessionId}\`} className="flex-1 overflow-y-auto max-h-[352px] py-1">`. Map manifest?.sessions → SessionRow. Pass `isActive`, `onSwitch` (calls switchSession + dialog.close()), `onRename`, `onDuplicate`, `onDelete` (sets pendingDelete + opens deleteDialogRef).
    - Footer: "New session" button calls `void createSession()` then `dialogRef.current?.close()`.
    - Mount `<DeleteSessionConfirmDialog>` as child with deleteDialogRef + pendingDelete data + onDeleted callback that closes the switcher modal.
    - Import `SessionRow` and `DeleteSessionConfirmDialog` with `.js` extensions.

    **SessionRow.tsx:**
    - Named export `SessionRow`. Props: `{ session: SessionMeta; isActive: boolean; onSwitch: () => void; onRename: (name: string) => void; onDuplicate: () => void; onDelete: () => void }` where `type SessionMeta = V2Manifest['sessions'][number]`.
    - Internal state: `editing: boolean`, `draft: string`.
    - `startRename`: sets draft = session.name, editing = true, then `setTimeout(() => inputRef.current?.focus(), 0)`.
    - `commitRename(e: FocusEvent)`: Pitfall 5 guard: if `e.currentTarget.closest('li')?.contains(e.relatedTarget as Node)` return early; if draft.trim() empty call cancelRename; else call onRename(trimmed) + setEditing(false).
    - `cancelRename`: sets draft = session.name, setEditing(false).
    - `handleKeyDown(e: KeyboardEvent)`: Enter → `e.currentTarget.blur()` (triggers blur → commit); Escape → cancelRename().
    - li element: `id={\`session-row-${session.id}\`}` `role="option"` `aria-selected={isActive}` className with conditional blue tint for active: use static class map `isActive ? 'bg-blue-50 dark:bg-blue-900/20' : 'hover:bg-gray-50 dark:hover:bg-gray-800/50'` plus `"flex items-center gap-2 px-4 min-h-[44px] group"`.
    - Active checkmark span: `className={\`w-4 shrink-0 text-xs ${isActive ? 'text-blue-600 dark:text-blue-400' : 'text-transparent'}\`}` with `aria-hidden="true"`. Never dynamic color construction — use ternary between two complete static strings.
    - When editing=false: render name button `flex-1 text-sm font-normal text-left text-gray-900 dark:text-gray-100 truncate focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none focus-visible:rounded` + icon buttons div with `opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 motion-safe:transition-opacity flex items-center gap-1`.
    - When editing=true: render input with `ref={inputRef}` `type="text"` `maxLength={50}` `value={draft}` `onChange` `onBlur={commitRename}` `onKeyDown={handleKeyDown}` `aria-label="Rename session"` (icon buttons hidden in edit mode).
    - Icon button aria-labels: `aria-label={\`Rename ${session.name}\`}`, `aria-label={\`Duplicate ${session.name}\`}`, `aria-label={\`Delete ${session.name}\`}`. Delete button uses red text classes.
    - All buttons: `focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none`.
    - Async actions void-cast: `onClick={() => { void onSwitch(); }}` — onSwitch itself is not async, but the caller must not return a Promise from onClick.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/components/SessionSwitcherModal.test.tsx src/components/SessionRow.test.tsx --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All SessionSwitcherModal and SessionRow tests pass
    - SessionSwitcherModal dialog element has NO `open` prop (linted by test asserting showModal() call path)
    - SessionRow has maxLength={50} on rename input
    - SessionRow blur handler includes the Pitfall 5 relatedTarget guard
    - Static class maps used (no template literals with variable color names)
    - Named exports only; all relative imports end in .js
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>SessionSwitcherModal and SessionRow components render correctly per UI-SPEC; all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DeleteSessionConfirmDialog + UndoToast components with tests</name>
  <files>
    src/components/DeleteSessionConfirmDialog.tsx,
    src/components/DeleteSessionConfirmDialog.test.tsx,
    src/components/UndoToast.tsx,
    src/components/UndoToast.test.tsx
  </files>
  <read_first>
    - src/components/ResetConfirmDialog.tsx — exact focus trap useEffect + focus restore pattern; "Keep"/"Reset" button structure; async handleReset pattern (copy → adapt to handleDelete)
    - src/components/ResetConfirmDialog.test.tsx — mock pattern to replicate for DeleteSessionConfirmDialog.test.tsx
    - src/components/StorageToast.tsx — conditional render pattern (`if (!visible) return null`) to adapt for UndoToast (`if (!undoBuffer) return null`)
    - src/components/StorageToast.test.tsx — toast test pattern to replicate
    - 06-PATTERNS.md — DeleteSessionConfirmDialog section (Props including onDeleted callback, focus restore to #open-session-switcher, handleDelete ordering: dialog.close → deleteSession → onDeleted) and UndoToast section (role/aria-live/aria-atomic, inverted color scheme, setUndoBuffer on × click, auto-dismiss owned by store action)
    - 06-UI-SPEC.md — DeleteSessionConfirmDialog (exact dialog structure, max-w-sm, focus trap, copy: "Delete session?", body text quoting sessionName, button copy "Keep session"/"Delete session") and UndoToast (role="status" aria-live="polite", fixed bottom-0, inverted colors, Undo button in text-blue-400 dark:text-blue-600, auto-dismiss note)
    - 06-CONTEXT.md — delete flow: "An undo option will appear briefly after deletion"; undo only while popup is open
  </read_first>
  <behavior>
    DeleteSessionConfirmDialog tests:
    - Renders h2 "Delete session?" with aria-labelledby matching the dialog's aria-labelledby
    - Body text contains the sessionName in quotes
    - "Keep session" button click calls dialog.close() without calling deleteSession
    - "Delete session" button click: calls deleteSession(sessionId) then dialog.close() then onDeleted()
    - Focus trap: Tab wraps between "Keep session" and "Delete session" buttons
    - Escape: dialog.close() (native behavior, no state change)
    - Focus restore: after close event, document.getElementById('open-session-switcher')?.focus() called

    UndoToast tests:
    - Returns null when undoBuffer is null
    - Renders when undoBuffer is non-null; shows the session name in the message
    - "Undo" button click calls undoDeleteSession()
    - × dismiss button click calls setUndoBuffer(null)
    - Has role="status" aria-live="polite" aria-atomic="true"
    - Has className containing "fixed bottom-0"
  </behavior>
  <action>
    Create `src/components/DeleteSessionConfirmDialog.test.tsx` and `src/components/UndoToast.test.tsx` with failing tests (RED). Then create implementation files.

    **DeleteSessionConfirmDialog.tsx:**
    - Named export `DeleteSessionConfirmDialog`. Props: `{ dialogRef: RefObject<HTMLDialogElement | null>; sessionId: string; sessionName: string; onDeleted: () => void }`.
    - Copy the focus trap + focus restore useEffect verbatim from ResetConfirmDialog.tsx; change focus restore target to `document.getElementById('open-session-switcher')?.focus()`.
    - Store selector: `deleteSession = useAppStore(s => s.deleteSession)`.
    - `handleDelete` async: `dialogRef.current?.close()` → `await deleteSession(sessionId)` → `onDeleted()`. Void-cast in button onClick.
    - Dialog: `aria-labelledby="delete-session-dialog-title"`, `className="fixed inset-0 m-auto w-full max-w-sm bg-white dark:bg-gray-900 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700 p-6"`. Never `<dialog open>`.
    - h2 id="delete-session-dialog-title" text "Delete session?", className `text-base font-semibold text-gray-900 dark:text-gray-100 mb-2`.
    - Body p: `"{sessionName}" will be permanently deleted. An undo option will appear briefly after deletion.` Use `className="text-sm font-normal text-gray-600 dark:text-gray-400 mb-6"`.
    - Buttons div: `flex gap-4 justify-end`.
    - "Keep session" button: id="cancel-delete-session"; gray background classes (copy from ResetConfirmDialog cancel button); calls `dialogRef.current?.close()`.
    - "Delete session" button: id="confirm-delete-session"; red button classes `bg-red-600 text-white rounded hover:bg-red-700 dark:bg-red-500 dark:hover:bg-red-600 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none text-sm font-normal px-4 py-2`; calls `void handleDelete()`.

    **UndoToast.tsx:**
    - Named export `UndoToast`. No props.
    - Store selectors: `undoBuffer = useAppStore(s => s.undoBuffer)`, `undoDeleteSession = useAppStore(s => s.undoDeleteSession)`, `setUndoBuffer = useAppStore(s => s.setUndoBuffer)`.
    - Guard: `if (!undoBuffer) return null;`.
    - Outer div: `role="status"` `aria-live="polite"` `aria-atomic="true"` `className="fixed bottom-0 left-0 right-0 z-50 bg-gray-900 dark:bg-gray-100 text-gray-100 dark:text-gray-900 flex items-center justify-between py-2 px-4 motion-safe:animate-[slide-up_150ms_ease-out]"`.
    - Message span: `className="text-xs font-normal"` content: `"{undoBuffer.sessionMeta.name}" deleted`.
    - Buttons div: `flex gap-2 items-center`.
    - "Undo" button: calls `void undoDeleteSession()`; className `text-xs font-normal text-blue-400 dark:text-blue-600 hover:underline focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none ml-4 shrink-0`.
    - × dismiss button: aria-label="Dismiss"; calls `setUndoBuffer(null)`; className `text-gray-400 hover:text-white dark:hover:text-gray-900 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none`.
    - Note: auto-dismiss is handled by the 10s setTimeout in the `deleteSession` store action (sets undoBuffer to null). UndoToast itself does NOT set up any timer. When undoBuffer becomes null the component re-renders to null and disappears.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/components/DeleteSessionConfirmDialog.test.tsx src/components/UndoToast.test.tsx --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All DeleteSessionConfirmDialog and UndoToast tests pass
    - DeleteSessionConfirmDialog dialog has no `open` prop; focus restore targets #open-session-switcher
    - UndoToast returns null when undoBuffer is null (verified by test)
    - UndoToast has role="status" aria-live="polite" aria-atomic="true"
    - UndoToast does NOT set up its own setTimeout (the store action owns the timer)
    - Named exports; .js extensions on all relative imports
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>DeleteSessionConfirmDialog and UndoToast components implemented with full test coverage; all four new component test suites green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → session name input | User types session name in rename input; value reaches store via onRename callback |
| store undoBuffer → UndoToast render | Deleted session name from undoBuffer rendered in toast message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Session name XSS via rename input | accept | React JSX auto-escapes string values; no dangerouslySetInnerHTML in any component; local-only extension |
| T-06-02-02 | Tampering | `<dialog open>` prop bypass allowing multiple dialogs to stack | mitigate | All new dialogs use only `dialogRef.current?.showModal()`; never `open` prop; pattern enforced verbatim from CandidateModal/ResetConfirmDialog |
| T-06-02-03 | Information Disclosure | UndoToast showing deleted session name in DOM after modal close | accept | Deleted session name is user's own data; rendered only while user's popup is open; no cross-origin exposure |
| T-06-02-04 | DoS | Focus trap preventing user from leaving modal | mitigate | Focus trap useEffect copied verbatim from Phase 5 CandidateModal (verified working); Escape key closes dialog via native `<dialog>` behavior |
| T-06-02-05 | Tampering | inline rename blur committing before user finishes (Pitfall 5) | mitigate | commitRename checks `e.currentTarget.closest('li')?.contains(e.relatedTarget as Node)` before committing; covered by test |
| T-06-02-SC | Tampering | npm install | accept | No new packages; all UI built with existing Tailwind + native dialog |
</threat_model>

<verification>
Full suite after Wave 2: `cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test`

Expected: all tests pass including all 4 new component test files + app.test.ts from Wave 1.
</verification>

<success_criteria>
- SessionSwitcherModal.tsx: native dialog, focus trap + restore to #open-session-switcher, max-h-[352px] list, role=listbox, aria-activedescendant, New session footer button
- SessionRow.tsx: inline rename with maxLength=50, Pitfall 5 blur guard, active row blue tint (static class map), checkmark span (aria-hidden), icon button aria-labels, opacity-0/group-hover reveal
- DeleteSessionConfirmDialog.tsx: copy of ResetConfirmDialog pattern, "Keep session"/"Delete session" buttons, focus restore to #open-session-switcher
- UndoToast.tsx: role=status aria-live=polite aria-atomic=true, fixed bottom-0, returns null when undoBuffer=null, no self-managed timer
- 8 new test files total (4 component + store extension from plan 01) all green
- npm test exits 0
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Role |
|----------|------|------|
| Session switcher modal | src/components/SessionSwitcherModal.tsx | SESS-02 modal UI |
| Session row | src/components/SessionRow.tsx | SESS-02 session list row with inline rename |
| Delete confirm dialog | src/components/DeleteSessionConfirmDialog.tsx | SESS-03 delete confirmation |
| Undo toast | src/components/UndoToast.tsx | SESS-03 soft-delete undo |
| Modal tests | src/components/SessionSwitcherModal.test.tsx | SESS-02 component coverage |
| Row tests | src/components/SessionRow.test.tsx | SESS-02 rename flow coverage |
| Delete dialog tests | src/components/DeleteSessionConfirmDialog.test.tsx | SESS-03 confirm flow coverage |
| Toast tests | src/components/UndoToast.test.tsx | SESS-03 undo toast coverage |

<output>
Create `.planning/phases/06-multiple-named-sessions-switcher/06-02-SUMMARY.md` when done.
</output>
