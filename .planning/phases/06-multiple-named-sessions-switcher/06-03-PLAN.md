---
phase: 06-multiple-named-sessions-switcher
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/components/ActionsGroup.tsx
  - src/components/ActionsGroup.test.tsx
  - src/app/App.tsx
  - src/app/main.tsx
  - src/app/styles.css
autonomous: false
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "ActionsGroup shows the active session name (truncated) above the 'Switch session' button per locked CONTEXT.md decision"
    - "The 'Switch session' button has id='open-session-switcher' and calls sessionSwitcherRef.current?.showModal()"
    - "SessionSwitcherModal is mounted inside ActionsGroup alongside CandidateModal and ResetConfirmDialog"
    - "UndoToast is mounted in App.tsx at the root level (after <StorageToast />) so it overlays the full viewport when the switcher modal is closed"
    - "main.tsx hydrates manifest into the Zustand store after bootstrap (useAppStore.setState({ manifest: initialState.manifest }))"
    - "styles.css contains the @keyframes slide-up animation used by motion-safe:animate-[slide-up_150ms_ease-out] on UndoToast"
    - "ActionsGroup.test.tsx covers: session label renders activeSessionName, 'Switch session' button is present with correct id, showModal is called on click"
    - "The extension loads in Chrome, the 'Switch session' button appears in the sidebar ActionsGroup, and session management works end to end"
  artifacts:
    - path: src/components/ActionsGroup.tsx
      provides: "Session label + Switch session trigger + SessionSwitcherModal mount"
      contains: "id=\"open-session-switcher\""
    - path: src/app/App.tsx
      provides: "UndoToast mounted at root level"
      contains: "UndoToast"
    - path: src/app/main.tsx
      provides: "manifest hydrated from bootstrap into Zustand store"
      contains: "manifest: initialState.manifest"
    - path: src/app/styles.css
      provides: "slide-up keyframe animation"
      contains: "@keyframes slide-up"
  key_links:
    - from: src/components/ActionsGroup.tsx
      to: src/components/SessionSwitcherModal.tsx
      via: "sessionSwitcherRef passed as dialogRef prop"
      pattern: "sessionSwitcherRef"
    - from: src/app/App.tsx
      to: src/components/UndoToast.tsx
      via: "import + JSX mount at root"
      pattern: "UndoToast"
    - from: src/app/main.tsx
      to: src/store/app.ts
      via: "useAppStore.setState({ manifest: initialState.manifest })"
      pattern: "initialState\\.manifest"
---

<objective>
Wire all Phase 6 components into the live application: session label + switcher trigger in ActionsGroup, UndoToast at App root, and manifest hydration in main.tsx. Add the slide-up CSS keyframe. Update ActionsGroup.test.tsx. Finish with a human smoke test in Chrome to verify session management works end-to-end.

Purpose: Complete SESS-01 through SESS-04 by connecting the store actions (plan 01) and UI components (plan 02) into the running extension. The human checkpoint verifies that the full session workflow — create, rename, duplicate, delete with undo — works in the actual Chrome extension environment.

Output: Modified ActionsGroup.tsx, App.tsx, main.tsx, styles.css; updated ActionsGroup.test.tsx; human-verified smoke test.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/06-multiple-named-sessions-switcher/06-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.test.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/main.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/styles.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ActionsGroup, App.tsx, main.tsx, styles.css</name>
  <files>
    src/components/ActionsGroup.tsx,
    src/components/ActionsGroup.test.tsx,
    src/app/App.tsx,
    src/app/main.tsx,
    src/app/styles.css
  </files>
  <read_first>
    - src/components/ActionsGroup.tsx — current file (69 lines): existing useRef declarations, candidateDialogRef + resetDialogRef pattern, JSX structure, button order. Session label + switcher trigger insert BEFORE "Expand all" button; separator `<hr>` after switcher button BEFORE "Expand all".
    - src/components/ActionsGroup.test.tsx — current test file: read the full file to understand existing test structure to extend (not replace)
    - src/app/App.tsx — current file (87 lines): StorageToast mount at line 84; UndoToast mounts immediately AFTER it (line 85); import added at top
    - src/app/main.tsx — current file (63 lines): bootstrap() at line 18 returns initialState; after the existing `useAppStore.setState({ ...uiState, ... })` block (lines 30–39), add manifest hydration: `useAppStore.setState({ manifest: initialState.manifest })`
    - src/app/styles.css — current file (7 lines): add @keyframes slide-up block at the end
    - 06-PATTERNS.md — ActionsGroup extension pattern (session label, switcher button, separator, SessionSwitcherModal mount; exact className strings); App.tsx (UndoToast mount after StorageToast); main.tsx (manifest hydration)
    - 06-UI-SPEC.md — ActionsGroup component spec (exact classes for session label p tag, switcher button, hr separator); UndoToast CSS keyframe
    - 06-CONTEXT.md — locked: session label above switcher button, 50-char name, separator between switcher and expand-all
  </read_first>
  <behavior>
    ActionsGroup.test.tsx additions (extend existing tests — do not replace):
    - "renders active session name label with aria-label='Active session'"
    - "renders 'Switch session' button with id='open-session-switcher'"
    - "clicking 'Switch session' calls showModal() on the session switcher dialog ref"
    - (Keep existing tests; add new ones in a separate describe block or alongside existing ones)
  </behavior>
  <action>
    **src/components/ActionsGroup.tsx** — modify the existing file:
    1. Add new imports at top: `import { SessionSwitcherModal } from './SessionSwitcherModal.js';`
    2. Add new store selectors inside the component: `const manifest = useAppStore(s => s.manifest)`, `const activeSessionId = useAppStore(s => s.activeSessionId)`.
    3. Add `const sessionSwitcherRef = useRef<HTMLDialogElement>(null);` alongside existing dialog refs.
    4. Compute `const activeSessionName = manifest?.sessions.find(s => s.id === activeSessionId)?.name ?? '';`.
    5. In JSX, insert BEFORE the "Expand all" button (currently first child of the div):
       - `<p className="text-xs font-normal text-gray-500 dark:text-gray-400 px-1 truncate" aria-label="Active session">{activeSessionName}</p>`
       - `<button type="button" id="open-session-switcher" onClick={() => sessionSwitcherRef.current?.showModal()} className="w-full text-sm px-3 py-2 text-left text-gray-900 dark:text-gray-100 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none">Switch session</button>`
       - `<hr className="border-gray-200 dark:border-gray-700 my-1" />`
    6. Mount `<SessionSwitcherModal dialogRef={sessionSwitcherRef} />` at the bottom of the JSX div alongside the existing CandidateModal and ResetConfirmDialog.

    **src/components/ActionsGroup.test.tsx** — extend (do NOT replace existing tests):
    - Add mock for `'../components/SessionSwitcherModal.js'` returning a stub component.
    - Add store mock return values for `manifest` (a minimal V2Manifest object with one session matching activeSessionId) and `activeSessionId`.
    - Write the three new test cases listed in the behavior block.

    **src/app/App.tsx** — add import and mount UndoToast:
    - Add `import { UndoToast } from '../components/UndoToast.js';` at the top (keep all existing imports).
    - In JSX, add `<UndoToast />` on the line immediately after `<StorageToast />` (currently line 84).
    - No other changes to App.tsx.

    **src/app/main.tsx** — add manifest hydration:
    - After the existing `useAppStore.setState({ ...uiState, selectedDifficulties: ..., selectedSections: ..., activeSessionId })` block (the block ending around line 39), add a new standalone call: `useAppStore.setState({ manifest: initialState.manifest });`
    - Import: `V2Manifest` type is already available via `src/storage/types.ts`; add `import type { V2Manifest } from '../storage/types.js'` if not already present (check existing imports — if it is not there, add it).
    - No other changes to main.tsx.

    **src/app/styles.css** — append the keyframe at the end of the file:
    ```css
    @keyframes slide-up {
      from { transform: translateY(100%); }
      to   { transform: translateY(0); }
    }
    ```

    After all changes, run: `npm test` (full suite). Fix any failures before committing. TypeScript strict check: `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm test exits 0 — all tests pass including new ActionsGroup.test.tsx cases
    - ActionsGroup.tsx contains `id="open-session-switcher"` and `aria-label="Active session"` on the session label p
    - App.tsx contains `<UndoToast />` after `<StorageToast />`
    - main.tsx contains `manifest: initialState.manifest` in a setState call
    - styles.css contains `@keyframes slide-up`
    - npx tsc --noEmit exits 0
    - No existing tests were removed or broken
  </acceptance_criteria>
  <done>All 5 files modified; full npm test suite green; no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human smoke test — session management end-to-end in Chrome</name>
  <what-built>
    Phase 6 is fully wired: store actions, UI components, ActionsGroup trigger, UndoToast at root, manifest hydration in main.tsx. Run `npm run build` and load the dist/ directory as an unpacked extension to verify the complete session workflow.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/dallask/Projects/dallask/interviewer-checklist && npm run build` — build must succeed with no errors.
    2. Open Chrome → Extensions → Load unpacked → select `dist/`.
    3. Click the toolbar icon to open the extension tab.
    4. In the sidebar ActionsGroup, confirm:
       - A session name label (e.g. "Session 1") appears above a "Switch session" button.
    5. Click "Switch session" → confirm the "Sessions" modal opens with:
       - Current session listed with a blue checkmark on its row.
       - "New session" button in the footer.
       - Close (×) button in the header.
    6. Click "New session" → modal closes; session label in sidebar updates to "Session 2"; Zustand state has 2 sessions.
    7. Re-open the modal → two sessions listed; Session 2 is active (checkmark on row 2).
    8. Click on "Session 1" row → modal closes; sidebar label changes to "Session 1"; content area scores should be empty (Session 1 data).
    9. Score any question in Session 1. Switch back to Session 2. Confirm scores are empty (Session 2 is fresh). Switch back to Session 1. Confirm scores are still present (SESS-04 flush-before-switch verified).
    10. Click ✎ on "Session 2" row → inline input appears with "Session 2" pre-filled; type "Interview A" + Enter → row shows "Interview A"; modal session label updated.
    11. Click ⧉ on "Interview A" row → new row "Interview A (copy)" appears; it is NOT activated.
    12. Click × on "Interview A (copy)" → delete confirm dialog opens with text '"Interview A (copy)" will be permanently deleted'. Click "Keep session" → dialog closes, session still in list.
    13. Click × on "Interview A (copy)" again → confirm dialog → click "Delete session" → both dialogs close; UndoToast appears at bottom with '"Interview A (copy)" deleted' + "Undo" button.
    14. Wait ~10 seconds → toast auto-disappears; session gone permanently.
    15. Repeat delete on a session → click "Undo" within 10 seconds → session reappears in list; toast disappears.
    16. Close and reopen the extension tab → session list persists (manifest written to chrome.storage.local).
  </how-to-verify>
  <resume-signal>Type "approved" if all 16 steps pass. Describe any failures if issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → sessionSwitcherRef.showModal() | Trigger button opens modal imperatively |
| chrome.storage → main.tsx manifest hydration | Manifest read from storage on bootstrap; hydrated into store before render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | activeSessionName rendered before manifest hydrated | mitigate | main.tsx hydrates manifest via useAppStore.setState({ manifest }) before createRoot; component shows empty string fallback (`?? ''`) until hydrated; no race condition |
| T-06-03-02 | Tampering | UndoToast z-index covered by modal backdrop | accept | UndoToast is mounted in App.tsx AFTER modal closes (toast appears post-close per CONTEXT.md flow); z-50 class renders it above all other content |
| T-06-03-03 | Tampering | slide-up animation running without prefers-reduced-motion guard | mitigate | `motion-safe:animate-[slide-up_150ms_ease-out]` — Tailwind motion-safe variant gates animation on `prefers-reduced-motion: no-preference`; UI-08 requirement respected |
| T-06-03-04 | Information Disclosure | Deleted session name visible in DOM via UndoToast after switcher modal closes | accept | Session name is user's own data; local extension only; no cross-origin exposure possible |
| T-06-03-SC | Tampering | npm install | accept | No new packages introduced; no install tasks in this plan |
</threat_model>

<verification>
Final phase gate:
```
cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test && npx tsc --noEmit
```
Both must exit 0 before requesting `/gsd-verify-work`.
</verification>

<success_criteria>
- ActionsGroup.tsx: session name label (aria-label="Active session") + "Switch session" button (id="open-session-switcher") appear in the sidebar; SessionSwitcherModal mounted with sessionSwitcherRef
- App.tsx: UndoToast mounted at root level after StorageToast
- main.tsx: manifest: initialState.manifest hydrated into store after bootstrap
- styles.css: @keyframes slide-up defined
- All 4 requirements (SESS-01, SESS-02, SESS-03, SESS-04) covered across Plans 01–03
- Full npm test suite green
- Human smoke test approved (all 16 verification steps pass)
</success_criteria>

## Artifacts this phase produces

| Artifact | Path | Role |
|----------|------|------|
| ActionsGroup (wired) | src/components/ActionsGroup.tsx | SESS-02 sidebar trigger + session label |
| ActionsGroup tests (extended) | src/components/ActionsGroup.test.tsx | SESS-02 trigger test coverage |
| App.tsx (wired) | src/app/App.tsx | SESS-03 UndoToast root mount |
| main.tsx (wired) | src/app/main.tsx | SESS-01 manifest hydration |
| styles.css (extended) | src/app/styles.css | UndoToast slide-up animation |

<output>
Create `.planning/phases/06-multiple-named-sessions-switcher/06-03-SUMMARY.md` when done.
</output>
