---
phase: 09-polish-print-keyboard-a11y-welcome-updates
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - vite.config.ts
  - src/app/welcome.html
  - src/app/welcome-main.tsx
  - src/app/Welcome.tsx
  - src/components/ContentTree.tsx
  - src/components/QuestionCard.tsx
  - src/components/TopicRow.tsx
  - src/components/SidebarGroup.tsx
  - src/components/ActionsGroup.tsx
  - src/app/App.tsx
autonomous: false
requirements:
  - POLISH-01
  - POLISH-05

must_haves:
  truths:
    - "A welcome.html tab opens on first install"
    - "Welcome page shows the page title, subtitle, pin-to-toolbar nudge, two audience flow cards, and CTA buttons"
    - "Welcome page calls chrome.storage.local.set({ hasSeenWelcome: true }) on mount"
    - "npm run build succeeds with the second Vite entry point"
    - "Printing hides score sliders, action buttons, and the session switcher controls"
    - "Printing shows a candidate print header above the question list"
    - "QuestionCard.tsx and TopicRow.tsx have correct print:hidden on interactive-only controls"
  artifacts:
    - path: "src/app/welcome.html"
      provides: "Second Vite HTML entry — standalone React welcome page"
      contains: "welcome-main.tsx"
    - path: "src/app/welcome-main.tsx"
      provides: "React root mount for welcome page"
      contains: "Welcome"
    - path: "src/app/Welcome.tsx"
      provides: "Welcome page component with pin nudge, audience cards, CTA buttons"
      contains: "hasSeenWelcome"
    - path: "vite.config.ts"
      provides: "Second rollupOptions.input entry for src/app/welcome"
      contains: "welcome"
  key_links:
    - from: "Welcome.tsx mount"
      to: "chrome.storage.local"
      via: "chrome.storage.local.set({ hasSeenWelcome: true })"
      pattern: "hasSeenWelcome"
    - from: "vite.config.ts rollupOptions.input"
      to: "src/app/welcome.html"
      via: "'src/app/welcome': 'src/app/welcome.html'"
      pattern: "welcome"
---

<objective>
Build the welcome page as a second Vite entry point and apply remaining print CSS to interactive controls in QuestionCard, TopicRow, and the content area.

Purpose: POLISH-01 first-run onboarding. Also closes the remaining print CSS items (POLISH-05) for interactive controls that are only visible on screen.

Output: src/app/welcome.html + welcome-main.tsx + Welcome.tsx; vite.config.ts second entry; print:hidden on score sliders, action buttons, custom question controls; build verified before committing.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/PROJECT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/09-polish-print-keyboard-a11y-welcome-updates/09-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/09-polish-print-keyboard-a11y-welcome-updates/09-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/09-polish-print-keyboard-a11y-welcome-updates/09-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/09-polish-print-keyboard-a11y-welcome-updates/09-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/vite.config.ts
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/app.html
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/app/main.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicRow.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ContentTree.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Welcome page files + vite.config.ts second entry (per D-POLISH-01)</name>
  <files>
    vite.config.ts,
    src/app/welcome.html,
    src/app/welcome-main.tsx,
    src/app/Welcome.tsx
  </files>
  <action>
--- vite.config.ts ---

Add a second entry to rollupOptions.input following the exact existing pattern (per RESEARCH.md Pattern 5 and PATTERNS.md):

  build: {
    rollupOptions: {
      input: {
        'src/app/app': 'src/app/app.html',
        'src/app/welcome': 'src/app/welcome.html',  // ADD THIS
      },
    },
    ...existing keys unchanged...
  }

Do not change any other vite.config.ts keys.

CRXJS KNOWN RISK (per RESEARCH.md Pitfall 4 and Assumption A1): The second rollupOptions.input entry may break the build with CRXJS 2.6.1 (issue #876). The checkpoint task (Task 3) verifies this. If the build fails at the checkpoint, the fallback is to revert the rollupOptions.input change and instead create a plain static welcome.html in public/ using inline CSS (no React, no Tailwind) — a simpler self-contained page with no build dependency.

--- src/app/welcome.html ---

Create src/app/welcome.html following the exact app.html pattern (per PATTERNS.md analog: app.html lines 1-14):

  <!doctype html>
  <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <title>Welcome — Interviewer Checklist</title>
      <!-- theme.ts runs BEFORE welcome-main.tsx to prevent dark-mode FOUC -->
      <script type="module" src="./theme.ts"></script>
    </head>
    <body>
      <div id="root"></div>
      <script type="module" src="./welcome-main.tsx"></script>
    </body>
  </html>

--- src/app/welcome-main.tsx ---

Create src/app/welcome-main.tsx following the exact main.tsx pattern:

  import { StrictMode } from 'react';
  import { createRoot } from 'react-dom/client';
  import { Welcome } from './Welcome.js';
  import './styles.css';

  const root = document.getElementById('root');
  if (!root) throw new Error('Welcome page: root element not found');
  createRoot(root).render(
    &lt;StrictMode&gt;
      &lt;Welcome /&gt;
    &lt;/StrictMode&gt;
  );

No store bootstrap needed — welcome page is read-only and does not use Zustand.

--- src/app/Welcome.tsx ---

Create src/app/Welcome.tsx following the UI-SPEC section 1 contract exactly.

Structure:
- Import './styles.css' (Tailwind base styles)
- Import useEffect from 'react'
- No store import needed

Component logic:
- useEffect on mount: chrome.storage.local.set({ hasSeenWelcome: true }) — mark welcome as seen
- Read const { version } = chrome.runtime.getManifest() synchronously for display

JSX (per UI-SPEC section 1 verbatim — use exact copy text from UI-SPEC):

Root wrapper: div className "min-h-screen bg-white dark:bg-gray-900 text-gray-900 dark:text-gray-100"

Skip link (a11y per App.tsx pattern):
  &lt;a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:px-4 focus:py-2 focus:bg-white dark:focus:bg-gray-900 focus:rounded focus:ring-2 focus:ring-blue-500"&gt;Skip to main content&lt;/a&gt;

Main content area:
  &lt;main id="main-content" className="max-w-2xl mx-auto px-6 py-16"&gt;

Inside main:

1. Page title: &lt;h1 className="text-3xl font-semibold text-gray-900 dark:text-gray-100 mb-2"&gt;Interviewer Checklist&lt;/h1&gt;

2. Subtitle: &lt;p className="text-base font-normal text-gray-600 dark:text-gray-400 mb-8"&gt;A structured interview scoring tool — browser-based, private, no account needed.&lt;/p&gt;

3. Version display: &lt;p className="text-xs font-normal text-gray-400 dark:text-gray-500 mb-8"&gt;v{version}&lt;/p&gt;

4. Pin-to-toolbar section:
   &lt;section aria-labelledby="pin-heading" className="mb-8"&gt;
     &lt;h2 id="pin-heading" className="text-xl font-semibold mb-3"&gt;Pin to your toolbar&lt;/h2&gt;
     &lt;p className="text-base font-normal text-gray-600 dark:text-gray-400"&gt;Click the puzzle-piece icon in Chrome's toolbar, find Interviewer Checklist, then click the pin icon.&lt;/p&gt;
   &lt;/section&gt;
   (No SVG illustration — text instruction is sufficient for v1; image assets are not available)

5. Audience cards:
   &lt;div className="grid grid-cols-1 md:grid-cols-2 gap-6 my-8"&gt;
     &lt;section aria-labelledby="for-interviewers" className="bg-gray-50 dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700"&gt;
       &lt;h2 id="for-interviewers" className="text-xl font-semibold mb-2"&gt;For interviewers&lt;/h2&gt;
       &lt;p className="text-base font-normal text-gray-600 dark:text-gray-400"&gt;Run structured technical interviews. Score questions 0–10 with difficulty weighting, capture notes, and generate an AI feedback prompt.&lt;/p&gt;
     &lt;/section&gt;
     &lt;section aria-labelledby="for-candidates" className="bg-gray-50 dark:bg-gray-800 rounded-lg p-6 border border-gray-200 dark:border-gray-700"&gt;
       &lt;h2 id="for-candidates" className="text-xl font-semibold mb-2"&gt;For candidates&lt;/h2&gt;
       &lt;p className="text-base font-normal text-gray-600 dark:text-gray-400"&gt;Follow along during your interview. See which areas are being assessed and review the scoring rubric afterward.&lt;/p&gt;
     &lt;/section&gt;
   &lt;/div&gt;

6. CTA buttons:
   &lt;div className="flex flex-col sm:flex-row gap-4 items-start"&gt;
     &lt;button
       type="button"
       onClick={() => {
         const url = chrome.runtime.getURL('src/app/app.html');
         chrome.tabs.create({ url });
       }}
       className="bg-blue-600 text-white text-base font-semibold px-6 py-4 rounded hover:bg-blue-700 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none min-h-[44px]"
     &gt;Open the extension&lt;/button&gt;
     &lt;button
       type="button"
       onClick={() => {
         // Switch to the demo session and open the main app
         chrome.storage.local.set({ activeSessionOverride: 'demo' });
         const url = chrome.runtime.getURL('src/app/app.html');
         chrome.tabs.create({ url });
       }}
       className="text-blue-600 dark:text-blue-400 text-base font-normal underline focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none min-h-[44px] self-center"
     &gt;View demo session&lt;/button&gt;
   &lt;/div&gt;

Note on "View demo session" CTA: The activeSessionOverride key written here is read by main.tsx on bootstrap to switch the active session to 'demo' before mounting the store. This is the simplest pattern — add handling in src/app/main.tsx: after bootstrap() resolves, check chrome.storage.local.get('activeSessionOverride'); if present, call useAppStore.getState().switchSession('demo') and chrome.storage.local.remove('activeSessionOverride'). Read main.tsx before implementing to confirm where to add this.

Export function Welcome().
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/app/welcome.html','src/app/welcome-main.tsx','src/app/Welcome.tsx'].forEach(f=>{if(!fs.existsSync(f)) throw new Error(f+' missing'); console.log(f,'OK')})"</automated>
  </verify>
  <done>src/app/welcome.html, welcome-main.tsx, and Welcome.tsx all exist. vite.config.ts has 'src/app/welcome' in rollupOptions.input. Welcome.tsx sets hasSeenWelcome on mount and shows all UI-SPEC content (title, subtitle, pin nudge, 2 audience cards, 2 CTA buttons).</done>
</task>

<task type="auto">
  <name>Task 2: Remaining print CSS — QuestionCard + TopicRow interactive controls (per D-POLISH-05)</name>
  <files>
    src/components/QuestionCard.tsx,
    src/components/TopicRow.tsx,
    src/components/ContentTree.tsx
  </files>
  <action>
Apply print:hidden to interactive-only elements in the question tree that should not appear in print output. Read each file before editing to understand current class names.

--- QuestionCard.tsx ---

Add "print:hidden" to the className of each of these elements (per UI-SPEC section 4 print layout):
- The score slider (input type="range") and its label/wrapper div
- The "Add notes" / "Hide notes" toggle button
- The "Delete" button (for custom questions) if present
- The "Add to this topic" custom question form trigger button if rendered in QuestionCard

The notes textarea itself: Plan 09-02 Task 1 already added print:h-auto + conditional hidden prop based on printMode. In this task, confirm the textarea already has print:h-auto from 09-02 — if 09-02 has not yet run, add it here. There should be no duplication if both plans run in parallel (Wave 2 means they CAN run in parallel but SHOULD NOT be executed simultaneously by the same agent — the executor runs them sequentially).

Add a print-only candidate header in the QuestionCard's parent or in ContentTree.tsx: There is no single "top of list" position inside QuestionCard — the candidate print header belongs in ContentTree.tsx or App.tsx. Add a hidden-on-screen, visible-on-print div with candidate name and role in ContentTree.tsx (before the virtualizer output begins):

  Read ContentTree.tsx to find where the virtualizer renders. Before the virtualizer's outer div, add:
  &lt;div aria-hidden="true" className="hidden print:block print:mb-4"&gt;
    &lt;h1 className="text-xl font-semibold"&gt;{candidateName || 'Interview Session'}&lt;/h1&gt;
    &lt;p className="text-sm text-gray-600"&gt;{candidateRole}{candidateRole && ' — '}{candidateDate}&lt;/p&gt;
  &lt;/div&gt;
  
  Read candidate from useAppStore((s) => s.candidate). The fields are candidate?.name, candidate?.role, candidate?.date.

--- TopicRow.tsx ---

Add "print:hidden" to:
- The expand/collapse topic arrow button
- Any "Add topic note" toggle button
- The custom question form trigger if it lives in TopicRow

The topic notes textarea: Plan 09-02 Task 1 handles the hidden prop change (printMode flag). Confirm print:h-auto is also applied to the TopicRow textarea. If 09-02 has not applied it yet, add it here.

Do NOT add print:hidden to topic headings, section headings, question text, score values (the mark band text displays), or notes textarea content. These should all be visible on print.
  </action>
  <verify>
    <automated>grep -c "print:hidden" src/components/QuestionCard.tsx src/components/TopicRow.tsx src/components/ContentTree.tsx 2>&1</automated>
  </verify>
  <done>QuestionCard.tsx has print:hidden on score slider, notes toggle button, and custom-question delete/add controls. TopicRow.tsx has print:hidden on topic expand/collapse button and topic notes toggle. ContentTree.tsx has a hidden print-only candidate header. grep shows at least 1 print:hidden in each file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - vite.config.ts now has a second rollupOptions.input entry: 'src/app/welcome': 'src/app/welcome.html'
    - src/app/welcome.html, welcome-main.tsx, and Welcome.tsx exist
    - Print CSS applied to QuestionCard, TopicRow, ContentTree
    - All three plan 09-02 and 09-03 tasks are complete
  </what-built>
  <how-to-verify>
    Step 1 — Verify the build succeeds with the second entry:
      npm run build
    Expected: build completes without errors. If it fails with a CRXJS/Rollup error:
      FALLBACK ACTION: Revert the vite.config.ts second entry (remove 'src/app/welcome' line), move welcome.html to public/welcome.html as a standalone static page (no React, no Tailwind — use inline CSS), update manifest.json web_accessible_resources to "public/welcome.html", and rebuild. Then re-run `npm run build` and confirm it passes.

    Step 2 — If build succeeded with React entry, load the extension:
      - Open chrome://extensions → click "Reload" on the extension
      - Open a fresh incognito window to simulate first install (or temporarily clear the hasSeenWelcome storage flag via chrome://extensions → Storage → clear)
      - Alternatively: inspect the welcome page directly at chrome-extension://[ID]/src/app/welcome.html
      Expected: Welcome page shows "Interviewer Checklist" heading, subtitle, "Pin to your toolbar" section, two audience flow cards ("For interviewers" / "For candidates"), and two buttons ("Open the extension" / "View demo session").

    Step 3 — Verify print layout:
      - Open the main app tab (chrome-extension://[ID]/src/app/app.html)
      - Open Print dialog (Cmd+P / Ctrl+P) — use Print Preview
      Expected: Sidebar is hidden. Score sliders are hidden. Only question text, mark bands, and (if content exists) notes are visible. Candidate header "Interview Session" appears at top.

    Step 4 — Run full test suite:
      npm test
    Expected: All tests pass.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass. Describe issues if any step fails, especially: (a) CRXJS build error requiring the public/ fallback, (b) welcome page missing elements, (c) print preview showing unexpected elements.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Welcome.tsx → chrome.storage.local | Writes hasSeenWelcome=true on mount; sets activeSessionOverride for demo session handoff |
| Welcome.tsx → chrome.tabs.create | URL built with chrome.runtime.getURL() — extension-controlled, not user-supplied |
| Vite build → CRXJS plugin | Second rollupOptions.input entry may trigger CRXJS issue #876 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | Welcome.tsx CTA tab open URL | mitigate | URL constructed with chrome.runtime.getURL('src/app/app.html'); extension-controlled; no user input in URL |
| T-09-10 | Tampering | activeSessionOverride storage key | accept | Stores 'demo' string only; main.tsx validates session ID exists before switching; no privilege impact |
| T-09-11 | Denial of Service | CRXJS build failure from second entry | mitigate | Checkpoint task verifies build; documented fallback (static public/welcome.html) if CRXJS rejects |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; zero install surface |
</threat_model>

<verification>
1. `npm run build` — exits 0 (no Rollup/CRXJS errors)
2. `ls src/app/welcome.html src/app/welcome-main.tsx src/app/Welcome.tsx` — all three exist
3. `grep -c "hasSeenWelcome" src/app/Welcome.tsx` — at least 1 (set on mount)
4. `grep -c "print:hidden" src/components/QuestionCard.tsx` — at least 1
5. `npm test` — full suite green
6. Human checkpoint: welcome page renders correctly in Chrome; print preview hides interactive controls
</verification>

<success_criteria>
- npm run build exits 0 (CRXJS handles second entry OR fallback static welcome.html is in place)
- Welcome.tsx renders: page title, subtitle, pin nudge, two audience cards, CTA buttons per UI-SPEC
- Welcome.tsx sets hasSeenWelcome=true on mount
- QuestionCard and TopicRow have print:hidden on interactive-only controls
- ContentTree renders a hidden-on-screen print-only candidate header
- Full test suite passes
- Human verifies welcome page appearance and print preview layout in Chrome
</success_criteria>

<output>
Create `.planning/phases/09-polish-print-keyboard-a11y-welcome-updates/09-03-SUMMARY.md` when done
</output>
