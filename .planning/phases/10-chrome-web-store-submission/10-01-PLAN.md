---
phase: 10-chrome-web-store-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PRIVACY.md
  - docs/cws-submission.md
  - cws-assets/CWS-SCREENSHOTS.md
  - cws-assets/CWS-SMOKE-TEST.md
  - .gitignore
autonomous: true
requirements: [CWS-01, CWS-02, CWS-03, CWS-04]
must_haves:
  truths:
    - "Repo contains a privacy policy file that documents local-only storage, no transmission, no analytics, removal on uninstall"
    - "Repo contains ready-to-paste CWS dashboard copy: short description (<=132 chars), long description (<=16384 chars), permissions justification for `storage`, single-purpose description, data-usage answers"
    - "Repo contains a screenshot capture checklist that specifies 3+ required 1280x800 screenshots (populated scoring view, sidebar with filters, AI prompt modal)"
    - "Repo contains a fresh-profile smoke-test checklist covering install, welcome page, scoring loop, sessions, YAML, AI modal, dark mode, keyboard shortcuts, and print"
    - ".gitignore excludes cws-assets/screenshots/ (binary assets) but tracks the checklists"
  artifacts:
    - path: "PRIVACY.md"
      provides: "Privacy policy text covering CWS-01 mandates"
      min_lines: 30
      contains: "chrome.storage.local"
    - path: "docs/cws-submission.md"
      provides: "Ready-to-paste CWS dashboard copy for CWS-02 + listing description"
      min_lines: 60
      contains: "storage"
    - path: "cws-assets/CWS-SCREENSHOTS.md"
      provides: "Screenshot capture checklist for CWS-03"
      min_lines: 20
      contains: "1280"
    - path: "cws-assets/CWS-SMOKE-TEST.md"
      provides: "Fresh-profile smoke-test checklist for CWS-04"
      min_lines: 20
      contains: "fresh"
    - path: ".gitignore"
      provides: "Exclusion for cws-assets/screenshots/ binary files"
      contains: "cws-assets/screenshots"
  key_links:
    - from: "PRIVACY.md"
      to: "docs/cws-submission.md"
      via: "permissions justification cross-references privacy policy intent"
      pattern: "storage"
    - from: "cws-assets/CWS-SCREENSHOTS.md"
      to: "cws-assets/screenshots/"
      via: "checklist specifies target filenames inside the screenshots directory"
      pattern: "cws-assets/screenshots"
---

<objective>
Generate all written CWS submission artifacts so that the user can publish the privacy policy, paste dashboard copy, capture screenshots, and run the fresh-profile smoke test deterministically. Implements decisions D-01, D-02, D-03, D-04, D-05, D-06, D-08 from 10-CONTEXT.md. Closes the writing portion of CWS-01, CWS-02, CWS-03, and CWS-04 by producing the source-of-truth markdown the next plans consume.

Purpose: Phase 10 has zero code changes. All review artifacts are markdown documents that the user copies to the CWS Developer Dashboard or runs manually in Chrome. This plan produces every such document in one autonomous pass so the user never has to ad-hoc draft CWS copy at submission time.

Output:
  - PRIVACY.md (repo root) — covers CWS-01 mandates
  - docs/cws-submission.md — ready-to-paste CWS dashboard copy (CWS-02 + listing)
  - cws-assets/CWS-SCREENSHOTS.md — capture checklist for 3+ shots at 1280x800 (CWS-03)
  - cws-assets/CWS-SMOKE-TEST.md — fresh-profile smoke-test checklist (CWS-04)
  - .gitignore updated to exclude binary screenshots while tracking checklists
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-chrome-web-store-submission/10-CONTEXT.md

# Source-of-truth files for description copy and permission accuracy
@manifest.json
@CHANGELOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write PRIVACY.md at repo root (CWS-01, per D-01 + D-03)</name>
  <files>PRIVACY.md</files>
  <action>
Create PRIVACY.md at the repository root as the canonical privacy policy that the user will publish to a stable HTTPS URL in Plan 10-03. The document MUST be self-contained markdown (no external dependencies) and MUST explicitly cover every CWS-01 mandate from 10-CONTEXT.md decisions D-01 and D-03.

Required sections in this order:

1. Title: "Privacy Policy — Interviewer Checklist" with last-updated date 2026-06-18 and the extension version (read from manifest.json — currently 1.0.0).

2. "Summary" — one short paragraph stating the extension stores all data locally in `chrome.storage.local`, transmits nothing, uses no analytics or telemetry, and removes all data on uninstall.

3. "Data we collect" — bullet list naming exactly what the extension persists: interview scoring sessions, candidate details (name, email, role, date, interviewer, free-text notes), per-question and per-topic notes, custom questions, user preferences (dark mode, sidebar collapsed state, last-seen version, hasSeenWelcome flag), session manifest metadata. State that this data is entered by the user and never read from elsewhere.

4. "Where data is stored" — explicit statement that all data is stored in `chrome.storage.local`, which is an extension-partitioned local store inside the user's own Chrome profile. No other browser surface (no `chrome.storage.sync`, no `localStorage`, no IndexedDB, no cookies).

5. "Data transmission" — explicit statement: "This extension does not transmit any data to any external service. It performs no network requests at runtime. There are no analytics, no telemetry, no error reporting, no crash reporting, and no remote configuration." Cite the locked permission set: only `"storage"` is requested, no `host_permissions`, no `scripting`.

6. "Third-party services" — single line: "None."

7. "Data sharing" — single line: "None. The extension does not share data with anyone because it does not transmit data."

8. "Data removal" — explain that uninstalling the extension from Chrome (chrome://extensions) removes the extension's `chrome.storage.local` partition automatically, deleting every stored session and preference. Note that the user may also clear data manually via the in-app Reset All action (which preserves the session structure) or by deleting individual sessions from the session switcher.

9. "Permissions" — table or bullet list with one row: `storage` — "Save interview scoring sessions, candidate details, custom questions, and user preferences locally to `chrome.storage.local`. Required for the extension to retain any state across browser restarts."

10. "Children's privacy" — single sentence: "This extension is a tool for adult job interviewers and candidates and is not directed at children under 13."

11. "Changes to this policy" — single sentence: "If material changes are made, this document will be updated and the date at the top will reflect the change. There is no remote update mechanism — users see updates via the in-app update banner."

12. "Contact" — single line stating the contact channel is the GitHub repository's Issues page. The user will fill in the exact URL when they publish the policy in Plan 10-03; insert the placeholder `<https://github.com/<owner>/<repo>/issues>` so the user knows where to substitute. Do not invent a real URL.

Use the exact phrasing from 10-CONTEXT.md `<specifics>` where given: "This extension stores all data locally in `chrome.storage.local`. No data is transmitted to any external service. No analytics, tracking, or telemetry are used. All data is removed when the extension is uninstalled." may appear verbatim in the Summary section.

Keep the document under 200 lines. No marketing copy, no emojis. Plain English suitable for CWS reviewers.
  </action>
  <verify>
    <automated>test -f PRIVACY.md && grep -q "chrome.storage.local" PRIVACY.md && grep -q "No data is transmitted" PRIVACY.md && grep -q "uninstalled" PRIVACY.md && grep -qi "no analytics" PRIVACY.md && grep -q "^## Permissions" PRIVACY.md && [ $(grep -v '^#' PRIVACY.md | grep -c "storage") -ge 3 ]</automated>
  </verify>
  <done>
PRIVACY.md exists at repo root, is < 200 lines, contains all 12 required sections, and explicitly covers the four CWS-01 mandates (local-only storage, no transmission, no analytics, removal on uninstall). The exact phrasing required by 10-CONTEXT.md specifics is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write docs/cws-submission.md and CWS asset checklists (CWS-02, CWS-03, CWS-04, per D-02 + D-04 + D-06 + D-08)</name>
  <files>docs/cws-submission.md, cws-assets/CWS-SCREENSHOTS.md, cws-assets/CWS-SMOKE-TEST.md, .gitignore</files>
  <action>
Produce three checklist/copy documents and update .gitignore. Create the `docs/` and `cws-assets/` directories if they do not already exist (use mkdir as part of writing the files).

=== A. docs/cws-submission.md (CWS-02 + D-02 + D-08) ===

Single source of truth that the user will copy-paste into the Chrome Web Store Developer Dashboard at submission time. Organize as one section per dashboard field so the user can find each block by name. Required sections in this order:

1. Title: "Chrome Web Store Submission Copy — Interviewer Checklist" with extension version (1.0.0) and last-updated date 2026-06-18.

2. "How to use this document" — one short paragraph instructing the user that each section below corresponds to a specific field in the CWS Developer Dashboard (Store listing or Privacy practices tab) and should be copied as-is.

3. "Short description (single line, max 132 characters)" — exactly one line of marketing copy fitting in 132 chars or fewer. Use the manifest.json description as the seed but tighten for store search: convey that it is a fully-local, weighted-scoring tool for tech interviews with YAML and AI prompt export. After the line, in italics, add the literal character count (e.g. `_128 / 132 characters_`). Verify the count yourself before writing. Do NOT exceed 132.

4. "Detailed description (max 16384 characters)" — a multi-paragraph long description. Structure:
   - One-paragraph value statement (what it does, who it is for: interviewers and self-assessing candidates).
   - "Features" bulleted list derived from CHANGELOG.md 1.0.0 entries (question bank, weighted scoring, notes, candidate details, custom questions, named sessions, YAML import/export, AI feedback prompt, print stylesheet, keyboard shortcuts, welcome tour, dark mode).
   - "Privacy" paragraph that mirrors PRIVACY.md Summary: local storage only, nothing transmitted, no analytics, data removed on uninstall.
   - "Permissions" paragraph naming the single `storage` permission and its purpose.
   - "Open source" line if applicable (link to GitHub repo placeholder).
   Keep total under 16384 chars; aim for ~1500–3000 chars (readable, not padded).

5. "Single-purpose description (CWS data-handling field)" — exactly one short paragraph (2–4 sentences) stating the extension's single purpose: "Run difficulty-weighted technical interview scoring sessions entirely inside the user's browser, with YAML export and an AI-feedback prompt builder, all without any backend." Cite that this is the only purpose of the extension.

6. "Permissions justification — `storage`" — required for CWS-02. Use the exact phrasing from 10-CONTEXT.md specifics: "The `storage` permission is required to save interview scoring sessions, candidate details, custom questions, and user preferences (dark mode, sidebar state). All data remains local to the user's browser via `chrome.storage.local`." Add one sentence stating no other Chrome permissions are requested.

7. "Host permissions justification" — single line: "Not requested. This extension declares no `host_permissions` and makes no network requests."

8. "Remote code justification" — single line: "No remote code is used. The extension bundles all code at build time; the production `dist/` contains no `eval`, no inline scripts, and no references to remote scripts. The build is verified by `scripts/check-dist.js`."

9. "Data usage declarations" — table mapping each CWS data-usage category to the user's answer:
   - Personally identifiable information (name, email): YES — candidate name and email are stored locally by the user in the Candidate Details modal. NOT collected by the extension, NOT transmitted, NOT shared, NOT sold, NOT used for advertising, NOT used for tracking, NOT used outside the extension's stated purpose.
   - Health, financial, location, authentication, personal communications, web browsing activity: NO for each.
   - Certifications (three required checkboxes): All three certified TRUE (no transfer to third parties, no use unrelated to stated purpose, no use for creditworthiness/lending).

10. "Privacy policy URL" — placeholder line: `https://<user-publishes-this-in-plan-10-03>` with a one-line note: "Plan 10-03 task 1 records the live URL here before dashboard upload."

11. "Category and language" — recommended CWS category (Developer Tools or Productivity — flag as a user-confirmation point in Plan 10-03) and language (English).

12. "Submission checklist" — short bulleted recap of every CWS dashboard field the user must fill, mapped to which section above provides the copy.

=== B. cws-assets/CWS-SCREENSHOTS.md (CWS-03 + D-04 + D-05) ===

Capture checklist describing exactly three required screenshots at 1280x800. Required sections:

1. Title and a one-paragraph intro stating CWS requires at least three screenshots, all at 1280×800 pixels (the CWS recommended size), in PNG format, captured in real Chrome (not a screenshot tool window-chrome-included). Reference D-04 + D-05.

2. "How to capture" — step-by-step:
   - Load the production build (`dist/` from Plan 10-02 task 1) as an unpacked extension in a fresh Chrome profile (or any Chrome profile after a clean test).
   - Open the extension page in a full window.
   - Resize the Chrome content area to exactly 1280×800. Recommend using the Chrome DevTools device toolbar (Cmd/Ctrl+Shift+M) set to Responsive at 1280×800, then take a full-page screenshot via DevTools (`...` menu → "Capture screenshot") — this produces a pixel-perfect 1280×800 PNG without window chrome.
   - Save each PNG into `cws-assets/screenshots/` with the exact filenames listed below.

3. "Required shots" — a numbered table or list with exactly three rows. Each row specifies: filename, what must be visible, and how to set up the state. The three shots are:

   a. `01-scoring-view-populated.png` — Populated scoring view. Must show: at least one expanded section, multiple visible question cards with non-zero score sliders, at least one topic showing a live difficulty-weighted mark with a color band, and the sidebar visible on the left. State setup: start from the seeded demo session (welcome page → "Start demo session"), score 6–8 questions at varying levels, then capture.

   b. `02-sidebar-with-filters.png` — Sidebar with filters active. Must show: expanded Search, Difficulty (at least one level selected), and Sections (at least 2 sections selected) groups; the result count visible; the content area on the right showing the filtered tree. State setup: type a non-trivial search term ("react" or "system design"), check at least one difficulty level, select at least two sections.

   c. `03-ai-prompt-modal.png` — AI prompt modal open with generated content. Must show: the modal as a centered overlay, the textarea pre-populated with the candidate-feedback prompt, the Copy button visible, and at least the candidate name from the demo session present in the prompt. State setup: from the populated session, open Actions → AI Prompt.

4. "What MUST NOT appear" — bullets: no empty-state screens, no DevTools panel, no browser window chrome, no other tabs visible, no personally-identifying real data (use the seeded demo data only).

5. "After capture" — bullet steps: verify each file is exactly 1280×800 (e.g. `file cws-assets/screenshots/*.png` and `identify` if ImageMagick is available, or open in Preview/an image viewer and check the info pane), commit the PNGs to git (the .gitignore exception is handled in part D below — actually the screenshots directory is gitignored, so the user must `git add -f cws-assets/screenshots/*.png` to force-add them, OR we add a `!cws-assets/screenshots/*.png` exception in .gitignore so they can be added normally. Choose the exception approach: instruct user that they can `git add` normally).

=== C. cws-assets/CWS-SMOKE-TEST.md (CWS-04 + D-06) ===

Fresh-profile smoke-test checklist. Required sections:

1. Title and one-paragraph intro: explain that CWS-04 requires the extension to load cleanly in a Chrome profile that is NOT the developer profile. Recommend creating a fresh Chrome profile via chrome://profile-picker OR using a separate browser like Chrome Canary, OR using `--user-data-dir=/tmp/cws-smoke-profile` from the command line. Reference D-06.

2. "Pre-flight" — bullet checks: confirm `dist.zip` and `dist/` exist from Plan 10-02 task 1, no other Interviewer Checklist extension is installed in the test profile.

3. "Install" — bullet steps: open chrome://extensions in the test profile, enable Developer mode, click "Load unpacked", select the `dist/` directory, confirm no error banner appears on the extension card.

4. "First-run welcome flow (POLISH-01)" — verify: a new tab opens automatically with the welcome page; page shows the title, version string, "Pin to your toolbar" section, and two audience cards; clicking "Start demo session" navigates to the main app with the seeded session loaded.

5. "Toolbar action and shortcut (FOUND-03, POLISH-02)" — verify: clicking the toolbar icon opens the app in a full-page tab; the keyboard shortcut Alt+Shift+I (Cmd+Shift+I on macOS) also opens the app.

6. "Core scoring loop (SCORE-01..06)" — verify: a 0–10 slider on at least one question updates the live topic mark; setting a manual topic override replaces the computed mark; typing in per-question and per-topic notes persists across a page reload; opening Candidate Details and saving name/email/role/date persists across reload; adding a custom question shows a "custom" badge and participates in scoring; Reset All (with confirmation) clears scores/notes/customs.

7. "Sessions (SESS-01..04)" — verify: opening the session switcher shows the active session; creating a new session, renaming, duplicating, and deleting each work; deleting shows the undo toast; switching sessions does not corrupt data (set scores in A, switch to B, switch back, scores in A intact).

8. "YAML import/export (YAML-01..03)" — verify: Export produces a structural YAML file; importing that file into a new session shows the preview modal then applies correctly; importing a legacy progress-only YAML matches by stable IDs.

9. "AI prompt (AI-01, AI-02)" — verify: AI prompt modal opens, prompt embeds candidate details and marks; Copy button copies the full text to clipboard.

10. "UI shell (UI-01..08)" — verify: sidebar collapse/expand works; sidebar becomes overlay on narrow viewport; search debounces and shows result count; difficulty + section multi-select filters work; dark mode toggle persists; tab order and focus rings visible; prefers-reduced-motion suppresses animation.

11. "Print (POLISH-05)" — verify: Cmd/Ctrl+P preview shows all collapsed cards expanded, sidebar and controls hidden.

12. "Keyboard shortcuts (POLISH-03)" — verify: `/` focuses search, `\` toggles sidebar, `Esc` clears search / closes the active modal; shortcuts do not fire while focus is inside a textarea/input.

13. "Storage / no-network (FOUND-02, CWS-01)" — verify: open DevTools → Network tab, exercise the app for 60 seconds, confirm zero non-extension network requests; open chrome://extensions → Inspect service worker → Application → Storage to confirm only `chrome.storage.local` is populated.

14. "Uninstall (CWS-01)" — verify: removing the extension from chrome://extensions clears the extension's storage automatically (reinstall → no prior data visible).

15. "Sign-off" — empty space for the user to record the date and Chrome version of the passing run.

Each check should be a checkbox `- [ ]`.

=== D. .gitignore update ===

Read current .gitignore. Append a new section:

```
# Chrome Web Store submission assets
cws-assets/screenshots/*
!cws-assets/screenshots/.gitkeep
```

This ignores binary screenshots by default but allows a `.gitkeep` file to keep the directory tracked. Also create `cws-assets/screenshots/.gitkeep` as an empty file. Note: do NOT add `dist.zip` — the existing `*.zip` line already covers it.

In CWS-SCREENSHOTS.md "After capture" section, instruct the user to add specific PNGs with `git add -f cws-assets/screenshots/*.png` (force flag bypasses the pattern). This is intentional — keeps accidental binary commits opt-in.
  </action>
  <verify>
    <automated>test -f docs/cws-submission.md && test -f cws-assets/CWS-SCREENSHOTS.md && test -f cws-assets/CWS-SMOKE-TEST.md && test -f cws-assets/screenshots/.gitkeep && grep -q "Permissions justification" docs/cws-submission.md && grep -q "storage" docs/cws-submission.md && grep -q "1280" cws-assets/CWS-SCREENSHOTS.md && grep -qi "fresh" cws-assets/CWS-SMOKE-TEST.md && grep -q "cws-assets/screenshots" .gitignore && SHORT_LINE=$(awk '/Short description/,/Detailed description/' docs/cws-submission.md | grep -v '^#' | grep -v '^_' | grep -v '^$' | head -1) && [ ${#SHORT_LINE} -le 132 ]</automated>
  </verify>
  <done>
docs/cws-submission.md exists with all 12 sections including a short description verified at <=132 characters and the exact CWS-02 permissions-justification phrasing. cws-assets/CWS-SCREENSHOTS.md specifies three 1280x800 PNGs by filename and capture state. cws-assets/CWS-SMOKE-TEST.md covers install, welcome, scoring, sessions, YAML, AI, UI shell, print, keyboard, network, and uninstall. .gitignore appends the screenshots exclusion with .gitkeep allow. cws-assets/screenshots/.gitkeep exists.
  </done>
</task>

</tasks>

<verification>
- All four artifacts exist at the expected paths.
- PRIVACY.md grep matches confirm CWS-01 mandates are explicit (chrome.storage.local, no transmission, no analytics, uninstall removal).
- docs/cws-submission.md short description verified <=132 chars by character count.
- docs/cws-submission.md contains the exact CWS-02 permissions-justification phrasing from 10-CONTEXT.md specifics.
- CWS-SCREENSHOTS.md lists exactly three required shots at 1280x800 with target filenames inside `cws-assets/screenshots/`.
- CWS-SMOKE-TEST.md uses `- [ ]` checkboxes and covers install, welcome, scoring, sessions, YAML, AI, UI shell, print, keyboard shortcuts, network, and uninstall.
- .gitignore appends the screenshots directory exclusion without removing existing entries (existing `*.zip` line preserved).
- No code under `src/` is modified — Phase 10 has zero code changes.
</verification>

<success_criteria>
- [ ] PRIVACY.md exists at repo root and explicitly addresses all four CWS-01 mandates
- [ ] docs/cws-submission.md exists with short description (<=132 chars), detailed description, single-purpose, permissions justification (CWS-02 exact phrasing), data-usage answers, and submission checklist
- [ ] cws-assets/CWS-SCREENSHOTS.md exists with three required shots at 1280x800 specified by filename and state
- [ ] cws-assets/CWS-SMOKE-TEST.md exists with `- [ ]` checkboxes for install, welcome, scoring, sessions, YAML, AI, UI shell, print, keyboard, network, uninstall
- [ ] .gitignore excludes cws-assets/screenshots/* with a .gitkeep allow exception; cws-assets/screenshots/.gitkeep exists
- [ ] No files under `src/` modified
</success_criteria>

<output>
Create `.planning/phases/10-chrome-web-store-submission/10-01-SUMMARY.md` when done. List every file created and confirm each requirement (CWS-01, CWS-02, CWS-03, CWS-04) is partially or fully satisfied by this plan (final fulfillment for CWS-01 and CWS-05 occurs in Plan 10-03; CWS-03 and CWS-04 are fulfilled by manual user execution in Plan 10-02 using the checklists from this plan).
</output>
