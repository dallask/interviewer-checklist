---
phase: 11
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/storage/migrations/index.ts
  - src/storage/migrations/v3-to-v4.ts
  - src/storage/migrations/fixtures/v3-session-fixture.ts
  - src/storage/migrations/v3-to-v4.test.ts
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "V4SessionSchema valibot parse succeeds on a V4 object produced by migrateV3ToV4()"
    - "migrateV3ToV4 re-keys score keys from '${topicId}-N' to '${topicId}-qN'"
    - "migrateV3ToV4 re-keys note keys with same pattern; overrides and topicNotes are NOT re-keyed"
    - "migrateV3ToV4 materializes sections from DEFAULT_SECTIONS with isDefault:true on all entities"
    - "Custom question IDs and topicIds pass through migrateV3ToV4 unchanged"
    - "runMigrations routes version:3 to migrateV3ToV4; version:4 returns null (skip)"
  artifacts:
    - path: "src/storage/migrations/v3-to-v4.ts"
      provides: "migrateV3ToV4() pure function"
      exports: ["migrateV3ToV4"]
    - path: "src/storage/migrations/fixtures/v3-session-fixture.ts"
      provides: "Frozen V3Session test fixtures"
      exports: ["V3_SESSION_EMPTY", "V3_SESSION_POPULATED", "V3_SESSION_NULL_CANDIDATE"]
    - path: "src/storage/migrations/v3-to-v4.test.ts"
      provides: "Unit tests covering all migrateV3ToV4 behaviors"
    - path: "src/storage/types.ts"
      provides: "V4SessionSchema, V4Session, V4Section, V4Topic, V4Question types, createDefaultV4Session()"
      exports: ["V4SessionSchema", "V4SectionSchema", "V4TopicSchema", "V4QuestionSchema", "V4Session", "V4Section", "V4Topic", "V4Question", "createDefaultV4Session"]
    - path: "src/storage/migrations/index.ts"
      provides: "runMigrations() extended to route V3→V4 and return null for V4"
  key_links:
    - from: "src/storage/migrations/v3-to-v4.ts"
      to: "src/data/bank/index.ts"
      via: "DEFAULT_SECTIONS import"
      pattern: "import.*DEFAULT_SECTIONS.*from.*bank/index"
    - from: "src/storage/migrations/v3-to-v4.ts"
      to: "src/storage/types.ts"
      via: "V3Session and V4Session type imports"
      pattern: "import type.*V3Session.*V4Session.*from.*types"
    - from: "src/storage/migrations/index.ts"
      to: "src/storage/migrations/v3-to-v4.ts"
      via: "migrateV3ToV4 import"
      pattern: "import.*migrateV3ToV4.*from.*v3-to-v4"
---

<objective>
Define the V4 schema contract and implement the migrateV3ToV4() pure function with its full unit test coverage. This is the foundation plan — all other Phase 11 plans depend on the types and migration function produced here.

Purpose: Establish V4Session as the canonical session format with materialized sections, stable question IDs (per D-04), and isDefault flags (per D-02). Wire migrateV3ToV4 into the runMigrations() dispatch array and replace the V3 early-return with a V4 early-return.

Output:
- V4SessionSchema (valibot) + V4Session/V4Section/V4Topic/V4Question TypeScript types in types.ts
- createDefaultV4Session() factory function in types.ts
- migrateV3ToV4() pure function in v3-to-v4.ts
- V3 session fixtures in fixtures/v3-session-fixture.ts
- Unit test suite v3-to-v4.test.ts with 9 describe blocks
- Updated runMigrations() in migrations/index.ts
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/REQUIREMENTS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add V4 valibot schemas, TypeScript types, and factory function to types.ts</name>
  <files>src/storage/types.ts</files>
  <read_first>
    - src/storage/types.ts — read in full; insert after line 141 (end of V3SessionSchema) and after line 153 (end of V3 type exports); mirror existing schema layout exactly
    - src/storage/migrations/fixtures/v2-session-fixture.ts — fixture pattern reference
  </read_first>
  <behavior>
    - V4QuestionSchema: id (string), text (string), level (union of 4 literals), isDefault (boolean)
    - V4TopicSchema: id (string), name (string), desc (string), tag (string), isDefault (boolean), questions (array of V4QuestionSchema)
    - V4SectionSchema: id (string), label (string), icon (string), isDefault (boolean), topics (array of V4TopicSchema)
    - V4SessionSchema: version (literal 4), id (string), sections (array of V4SectionSchema), scores (record string → nullable number), overrides (record string → nullable number), notes (record string → string), topicNotes (record string → string), customQuestions (array of CustomQuestionSchema — reuse existing), candidate (nullable CandidateDetailsSchema — reuse existing)
    - v.safeParse(V4SessionSchema, {version:3,...}) returns success:false
    - v.safeParse(V4SessionSchema, valid V4 object) returns success:true
    - createDefaultV4Session(id: string): V4Session returns {version:4, id, sections:[], scores:{}, overrides:{}, notes:{}, topicNotes:{}, customQuestions:[], candidate:null}
  </behavior>
  <action>
    Insert V4 schema block immediately after the closing of V3SessionSchema (after line 141). Follow the existing section-comment style: `// V4 types`. Define V4QuestionSchema, V4TopicSchema, V4SectionSchema, V4SessionSchema in that order — each referencing the prior schema for nested arrays. Reuse `CustomQuestionSchema` and `CandidateDetailsSchema` (already defined above) for the two fields that stay unchanged.

    Insert TypeScript type exports after the `export type V3Session = ...` line (after line 153). Add `V4Question`, `V4Topic`, `V4Section`, `V4Session` as `v.InferOutput<typeof Xxx>` exports.

    Insert `createDefaultV4Session(id: string): V4Session` factory after `createDefaultV3Session()`. The `sections` field must be `[]` (empty array) — it is populated only during migration, not on new session creation. Do NOT copy sections from DEFAULT_SECTIONS in the factory.

    Do NOT modify any existing schemas (V2SessionSchema, V3SessionSchema, CandidateDetailsSchema, CustomQuestionSchema). Do NOT remove createDefaultV3Session — it remains for migration tests.

    Per D-02: every V4 entity (section, topic, question) carries `isDefault: boolean`. Per D-04: the stable ID format `${topicId}-q${idx}` is used for default questions — this is encoded in the migration function, not the schema (schema accepts any string id).
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/storage/types.ts 2>/dev/null || npm test -- --reporter=verbose src/storage/migrations/v3-to-v4.test.ts</automated>
  </verify>
  <done>
    src/storage/types.ts exports V4QuestionSchema, V4TopicSchema, V4SectionSchema, V4SessionSchema, V4Question, V4Topic, V4Section, V4Session, createDefaultV4Session. v.safeParse(V4SessionSchema, {version:3}) returns {success:false}. v.safeParse(V4SessionSchema, createDefaultV4Session('x')) returns {success:true} (empty sections array is valid per schema).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create v3-session-fixture.ts, migrateV3ToV4() module, and full unit test suite</name>
  <files>
    src/storage/migrations/fixtures/v3-session-fixture.ts
    src/storage/migrations/v3-to-v4.ts
    src/storage/migrations/v3-to-v4.test.ts
    src/storage/migrations/index.ts
  </files>
  <read_first>
    - src/storage/migrations/fixtures/v2-session-fixture.ts — exact fixture pattern (3-fixture shape, Object.freeze, Readonly)
    - src/storage/migrations/v2-to-v3.ts — direct template for migrateV3ToV4 structure (Readonly parameter, no try/catch, pure function)
    - src/storage/migrations/v2-to-v3.test.ts — test file structure (9 describe blocks pattern), immutability test at lines 137-143, schema validation at lines 145-168
    - src/storage/migrations/index.ts — current MIGRATIONS array and runMigrations dispatch logic (lines 1-57)
    - src/data/bank/types.ts — Section/Topic/Question interfaces (needed for materializeSections parameter type)
    - src/data/bank/index.ts — DEFAULT_SECTIONS export (confirm import path)
  </read_first>
  <behavior>
    Fixture file:
    - V3_SESSION_EMPTY: version:3, id:'test-v3-session', all records empty, customQuestions:[], candidate:null
    - V3_SESSION_POPULATED: version:3, id:'populated-v3-session', scores:{'twig-0':8,'twig-1':6}, overrides:{twig:7}, notes:{'twig-0':'Good answer','twig-1':''}, topicNotes:{twig:'Overall solid'}, customQuestions:[{id:'custom-twig-1714000000000-0',topicId:'twig',text:'Custom Q',level:'novice'}], candidate:{name:'Test Candidate',email:'test@example.com',role:'Engineer',date:'2026-06-18',interviewer:'Interviewer',details:''}
    - V3_SESSION_NULL_CANDIDATE: version:3, id:'null-candidate-session', scores:{'js-0':5}, candidate:null

    migrateV3ToV4 behaviors:
    - Output version is 4
    - scores: 'twig-0' → 'twig-q0', 'twig-1' → 'twig-q1' (integer-suffix keys re-keyed per D-04)
    - notes: same re-keying as scores ('twig-0' → 'twig-q0')
    - overrides: key 'twig' preserved unchanged (topicId key — no suffix to re-key per research A2)
    - topicNotes: key 'twig' preserved unchanged (topicId key — no suffix to re-key per research A3)
    - customQuestions: custom-twig-1714000000000-0 passes through unchanged (custom-* does not match /-(\d+)$/ alone)
    - candidate: null preserved; populated object preserved
    - sections: non-empty array from DEFAULT_SECTIONS deep-copy; every section has isDefault:true; every topic has isDefault:true; every question has isDefault:true
    - sections question IDs follow pattern '${topicId}-q${idx}' (e.g., first question of topic 'twig' is 'twig-q0')
    - Frozen V3_SESSION_POPULATED is not mutated (JSON.stringify before == after)

    runMigrations update:
    - runMigrations({version:3,...}) returns a V4Session (routes through MIGRATIONS entry fromVersion:3)
    - runMigrations({version:4,...}) returns null (already at latest version)
    - runMigrations({version:2,...}) still returns V3Session (existing behavior unchanged)

    v3-to-v4.test.ts describe blocks:
    1. 'migrateV3ToV4 — score key re-keying'
    2. 'migrateV3ToV4 — notes key re-keying'
    3. 'migrateV3ToV4 — overrides and topicNotes NOT re-keyed'
    4. 'migrateV3ToV4 — sections materialization'
    5. 'migrateV3ToV4 — customQuestions pass-through'
    6. 'migrateV3ToV4 — version and id'
    7. 'migrateV3ToV4 — candidate handling'
    8. 'migrateV3ToV4 — immutability'
    9. 'V4SessionSchema — valibot validation'
  </behavior>
  <action>
    Create src/storage/migrations/fixtures/v3-session-fixture.ts with `import type { V3Session } from '../../types.js'` and the three Object.freeze fixtures described in PATTERNS.md §v3-session-fixture pattern. Use real topic ID 'twig' in V3_SESSION_POPULATED because twig is a real topicId in DEFAULT_SECTIONS (from frontendSection/twigTopic) — this enables assertions on real materialized sections.

    Create src/storage/migrations/v3-to-v4.ts following the exact template contract from v2-to-v3.ts: `Readonly<V3Session>` parameter, no try/catch, pure function. Implement three internal functions:
    - `materializeSections(bank: readonly Section[]): V4Section[]` — deep-copies DEFAULT_SECTIONS using explicit map/spread (NOT JSON.stringify). Maps sec.items (array of Topic) to V4Topic. Maps topic.questions to V4Question with id=`${topic.id}-q${idx}` (per D-04), text=q.q, level=q.level, isDefault:true. Sets isDefault:true on every section and topic.
    - `remapScoreKeys<T>(record: Record<string, T>): Record<string, T>` — regex `/^(.+)-(\d+)$/` on each key; matches become `${match[1]}-q${match[2]}`; non-matches pass through. Called for BOTH scores and notes.
    - Export `migrateV3ToV4(session: Readonly<V3Session>): V4Session` — returns {version:4, id:session.id, sections:materializeSections(DEFAULT_SECTIONS), scores:remapScoreKeys(session.scores), overrides:session.overrides, notes:remapScoreKeys(session.notes), topicNotes:session.topicNotes, customQuestions:session.customQuestions, candidate:session.candidate}.

    ANTI-PATTERN GUARD: Do NOT call remapScoreKeys on session.overrides or session.topicNotes — they use topicId keys like 'twig' which do not end in /-(\d+)$/ and must not be modified. Do NOT use JSON.parse(JSON.stringify(...)) for deep copy — DEFAULT_SECTIONS is readonly and this loses types.

    Update src/storage/migrations/index.ts:
    1. Add import: `import { migrateV3ToV4 } from './v3-to-v4.js'`
    2. Add to MIGRATIONS array as third entry: `{ fromVersion: 3, fn: (r) => migrateV3ToV4(r as V3Session) }`
    3. Update MIGRATIONS array type union to include `| V4Session`
    4. Replace the `if (version === 3) { return null; }` block with a dispatch through the MIGRATIONS entry (same pattern as the version===2 block above it)
    5. Add `if (version === 4) { return null; }` guard after the version===3 dispatch block
    6. Update runMigrations return type to include `| V4Session`
    7. Add V4Session to the import from '../types.js'

    Create src/storage/migrations/v3-to-v4.test.ts following the v2-to-v3.test.ts template exactly. Mirror the 9 describe blocks listed in behavior. For the sections materialization block: assert `sections.length > 0`, assert `sections[0].isDefault === true`, assert `sections[0].topics[0].isDefault === true`, assert `sections[0].topics[0].questions[0].isDefault === true`, assert `sections[0].topics[0].questions[0].id` matches `/^.+-q\d+$/`. For the score re-keying block: assert result.scores['twig-q0'] === 8 and result.scores['twig-q1'] === 6 (using V3_SESSION_POPULATED). Assert 'twig-0' not in result.scores. For notes: same pattern with 'twig-0' → 'twig-q0'. The custom-twig key in customQuestions passes through remapScoreKeys unchanged because 'custom-twig-1714000000000-0' does not match /-(\d+)$/ at end (it ends in '-0' BUT the prefix contains multiple dashes — confirm with the regex: /^(.+)-(\d+)$/ DOES match 'custom-twig-1714000000000-0' as prefix='custom-twig-1714000000000' and suffix='0'. This means custom-* IDs WILL be re-keyed if passed through remapScoreKeys. IMPORTANT: customQuestions array is NOT passed through remapScoreKeys — only session.scores and session.notes records are. The customQuestions array itself passes through unchanged. For score keys belonging to custom questions (e.g., 'custom-twig-1714000000000-0' in session.scores), these WILL be re-keyed to 'custom-twig-1714000000000-q0' by remapScoreKeys. The RESEARCH.md anti-patterns section states "Existing custom-question IDs are NOT rewritten" — this refers to the customQuestions[].id field, not score map keys. Verify behavior by checking what V3_SESSION_POPULATED contains: it has no custom question scores in the scores map, so this edge case does not arise in the test fixtures. If needed, assert that a custom question score key 'custom-topicId-ts-0' in session.scores WOULD be re-keyed to 'custom-topicId-ts-q0' by remapScoreKeys.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/storage/migrations/v3-to-v4.test.ts</automated>
  </verify>
  <done>
    All 9 describe blocks pass. npm test runs green with no pre-existing test regressions. migrateV3ToV4(V3_SESSION_POPULATED).scores['twig-q0'] === 8. migrateV3ToV4(V3_SESSION_POPULATED).sections.length > 0 with isDefault:true on all entities. runMigrations({version:3, ...V3_SESSION_EMPTY}) returns a V4Session. runMigrations({version:4, ...}) returns null.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| `V4QuestionSchema` | `src/storage/types.ts` | valibot schema export |
| `V4TopicSchema` | `src/storage/types.ts` | valibot schema export |
| `V4SectionSchema` | `src/storage/types.ts` | valibot schema export |
| `V4SessionSchema` | `src/storage/types.ts` | valibot schema export |
| `V4Question` | `src/storage/types.ts` | TypeScript type export |
| `V4Topic` | `src/storage/types.ts` | TypeScript type export |
| `V4Section` | `src/storage/types.ts` | TypeScript type export |
| `V4Session` | `src/storage/types.ts` | TypeScript type export |
| `createDefaultV4Session` | `src/storage/types.ts` | factory function export |
| `migrateV3ToV4` | `src/storage/migrations/v3-to-v4.ts` | pure migration function export |
| `remapScoreKeys` (internal) | `src/storage/migrations/v3-to-v4.ts` | unexported helper |
| `materializeSections` (internal) | `src/storage/migrations/v3-to-v4.ts` | unexported helper |
| `V3_SESSION_EMPTY` | `src/storage/migrations/fixtures/v3-session-fixture.ts` | frozen fixture export |
| `V3_SESSION_POPULATED` | `src/storage/migrations/fixtures/v3-session-fixture.ts` | frozen fixture export |
| `V3_SESSION_NULL_CANDIDATE` | `src/storage/migrations/fixtures/v3-session-fixture.ts` | frozen fixture export |
| `reKeyImportResultToV4` | `src/utils/yamlImport.ts` | exported utility function (Plan 03) |
| `MigrationErrorBanner` | `src/components/MigrationErrorBanner.tsx` | React component (Plan 02) |
| `migrationFailedCount` | `src/store/app.ts` (AppState) | AppState field (Plan 02) |
| `migrationFailedIds` | `src/store/app.ts` (AppState) | AppState field (Plan 02) |
| `sections` | `src/store/app.ts` (AppState) | AppState field (Plan 02) |
| `createDefaultV4Session` call in `bootstrapDefaults` | `src/storage/bootstrap.ts` | updated call site (Plan 02) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome.storage.local → migrateV3ToV4 | Raw stored blobs (written by v1.0 app) cross into the migration function |
| V3SessionSchema parse | valibot validates raw blob before migration; invalid blobs do not reach migrateV3ToV4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | migrateV3ToV4 input | mitigate | valibot v.safeParse(V3SessionSchema, raw) in bootstrap validates the blob BEFORE passing to migrateV3ToV4; malformed blobs fail the parse and skip migration |
| T-11-02 | Denial of Service | materializeSections | accept | DEFAULT_SECTIONS is bundled and bounded (9 sections × N topics × M questions); user cannot inject sections in V3 — no V3 sections field exists |
| T-11-SC | Tampering | npm installs | accept | No new packages installed in this plan; existing stack only |
</threat_model>

<verification>
npm test -- --reporter=verbose src/storage/migrations/v3-to-v4.test.ts
npm test (full 515+ suite stays green)
</verification>

<success_criteria>
- V4SessionSchema exported from src/storage/types.ts; v.safeParse(V4SessionSchema, {version:3}) fails; v.safeParse(V4SessionSchema, V4 object from migrateV3ToV4) succeeds
- migrateV3ToV4(V3_SESSION_POPULATED).scores['twig-q0'] === 8 (not 'twig-0')
- migrateV3ToV4(V3_SESSION_POPULATED).notes['twig-q0'] === 'Good answer'
- migrateV3ToV4(V3_SESSION_POPULATED).overrides['twig'] === 7 (unchanged)
- migrateV3ToV4(V3_SESSION_POPULATED).sections.length > 0 with isDefault:true on first section
- migrateV3ToV4(V3_SESSION_EMPTY).sections.length === same as DEFAULT_SECTIONS.length
- runMigrations({version:3,...}) returns V4Session; runMigrations({version:4,...}) returns null
- All 515+ existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/11-v4-session-migration-legacy-compat/11-01-SUMMARY.md` when done
</output>
