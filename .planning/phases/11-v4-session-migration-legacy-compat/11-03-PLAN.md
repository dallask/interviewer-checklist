---
phase: 11
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/utils/yamlImport.ts
  - src/components/ActionsGroup.tsx
  - src/storage/bootstrap.test.ts
  - src/utils/yamlImport.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "parseLegacy() YAML import produces scores with 'q'-prefixed keys after reKeyImportResultToV4 is applied in ActionsGroup"
    - "parseStructural() YAML import produces scores and notes with 'q'-prefixed keys after reKeyImportResultToV4 is applied"
    - "custom question IDs in customQuestions[] pass through ActionsGroup unchanged (array not re-keyed)"
    - "bootstrap.test.ts has a Scenario E: V3 session in storage → bootstrap returns V4 session with sections materialized"
    - "bootstrap.test.ts has a Scenario F: failed-migration session excluded from sessions map, failedSessionIds contains the ID"
    - "yamlImport.test.ts asserts legacy import scores use 'twig-q0' key not 'twig-0'"
    - "yamlImport.test.ts asserts structural import scores use 'q'-prefixed keys"
  artifacts:
    - path: "src/utils/yamlImport.ts"
      provides: "reKeyImportResultToV4() exported utility function"
      exports: ["reKeyImportResultToV4"]
    - path: "src/components/ActionsGroup.tsx"
      provides: "YAML import call site applies reKeyImportResultToV4 before setImportPreview"
    - path: "src/storage/bootstrap.test.ts"
      provides: "Scenarios E (V3 migration) and F (failed migration) test coverage"
    - path: "src/utils/yamlImport.test.ts"
      provides: "V4 key expectations for legacy and structural import paths"
  key_links:
    - from: "src/components/ActionsGroup.tsx"
      to: "src/utils/yamlImport.ts"
      via: "reKeyImportResultToV4 import and call after parseLegacy/parseStructural"
      pattern: "reKeyImportResultToV4"
    - from: "src/utils/yamlImport.ts"
      to: "src/storage/migrations/v3-to-v4.ts"
      via: "NO direct import — reKeyImportResultToV4 inlines the same regex pattern independently"
      pattern: "reKeyImportResultToV4"
---

<objective>
Add the V4 key-remapping step to the YAML import path (DATA-02) and extend the integration test suites. After this plan, both legacy and structural YAML imports produce V4-compatible score/note keys, and bootstrap.test.ts covers the new migration scenarios introduced in Plan 02.

Purpose: Honor D-08 (YAML imports produce V4 shape) and D-09 (no pre-v4 snapshot for imports). Per RESEARCH.md Pattern 4 / PATTERNS.md §yamlImport.ts: reKeyImportResultToV4() lives in yamlImport.ts as an exported helper; ActionsGroup calls it after parsing.

Output:
- src/utils/yamlImport.ts — reKeyImportResultToV4() utility exported
- src/components/ActionsGroup.tsx — YAML import call site applies re-key before setImportPreview
- src/storage/bootstrap.test.ts — Scenarios E + F
- src/utils/yamlImport.test.ts — V4 key assertions for legacy + structural paths
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/11-v4-session-migration-legacy-compat/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reKeyImportResultToV4 to yamlImport.ts and wire it in ActionsGroup.tsx</name>
  <files>
    src/utils/yamlImport.ts
    src/components/ActionsGroup.tsx
  </files>
  <read_first>
    - src/utils/yamlImport.ts — read in full; understand ImportResult interface (lines 20-28) and the emptyResult() factory; find the export list to know where to append reKeyImportResultToV4
    - src/components/ActionsGroup.tsx — read in full; find the handleImportFileChange handler (lines 96-131) where parseLegacy/parseStructural are called and setImportPreview is set; this is the exact insertion point
    - .planning/phases/11-v4-session-migration-legacy-compat/11-PATTERNS.md §yamlImport.ts pattern — exact reKeyImportResultToV4 implementation shape
    - .planning/phases/11-v4-session-migration-legacy-compat/11-RESEARCH.md §Pattern 4 — YAML import V4 feed options (use Option A: convert in ActionsGroup)
    - .planning/phases/11-v4-session-migration-legacy-compat/11-RESEARCH.md §Pitfall 4 — key format mismatch
  </read_first>
  <behavior>
    reKeyImportResultToV4(result: ImportResult): ImportResult:
    - result.scores: keys matching /^(.+)-(\d+)$/ are re-keyed to '${match[1]}-q${match[2]}'; non-matching keys pass through
    - result.notes: same re-keying as scores
    - result.overrides: NOT re-keyed (topicId keys have no integer suffix — they have no digits at end)
    - result.topicNotes: NOT re-keyed (same reason as overrides)
    - result.customQuestions: array passed through unchanged (the ids in the array are NOT re-keyed by this function)
    - result.sessionName: passed through unchanged
    - result.candidate: passed through unchanged
    - modifiedCount, addedCount, unmatchedCount: passed through unchanged
    - reKeyImportResultToV4 returns a new ImportResult (does not mutate input)

    ActionsGroup.tsx behavior after change:
    - After parseLegacy returns preview, preview.result.scores use 'q'-prefixed keys (e.g., 'twig-q0' not 'twig-0')
    - After parseStructural returns preview, same key format change applies
    - importPreview passed to ImportPreviewModal contains re-keyed scores
    - No change to modifiedCount/addedCount/unmatchedCount values (counts computed before re-keying)
  </behavior>
  <action>
    In src/utils/yamlImport.ts, append reKeyImportResultToV4 as an exported function after the parseStructural function. The function takes `result: ImportResult` and returns `ImportResult`. Implement a private helper `remap<T>(record: Record<string, T>): Record<string, T>` inside the function body that applies `/^(.+)-(\d+)$/` to re-key integer-suffix keys. Call remap on result.scores and result.notes. Return `{ ...result, scores: remap(result.scores), notes: remap(result.notes) }` — explicitly spreading without modifying overrides/topicNotes/customQuestions.

    Per D-08 and RESEARCH.md Pattern 4, the parsers themselves (parseLegacy, parseStructural) do NOT change — they remain V3-key-based producers. The re-keying is solely the caller's responsibility.

    Do NOT add the regex pattern as a named constant shared with v3-to-v4.ts — the two files are in different directories and the regex is trivial; inline it in yamlImport.ts independently.

    In src/components/ActionsGroup.tsx:
    1. Add `reKeyImportResultToV4` to the import from '../utils/yamlImport.js' (alongside the existing imports from that file).
    2. In handleImportFileChange, find the block where `preview` is set (lines 116-127 approximately). After `const preview = ...`, before `setImportPreview(preview)`, insert:
       `const v4Preview = { ...preview, result: reKeyImportResultToV4(preview.result) };`
       Then change `setImportPreview(preview)` to `setImportPreview(v4Preview)`.
    3. Do NOT change anything else in ActionsGroup — the handleImportConfirm handler calls importSession(importPreview.result) which now contains V4-format keys, which is correct.

    Also update the handleExportYaml function in ActionsGroup.tsx to build a V4Session export object (version:4 + sections from store) instead of V3Session. Read the existing handleExportYaml block. It currently constructs a V3Session literal `{ version: 3, id: ..., scores, ... }` and passes it to exportSession(). After Plan 02, the store has `sections` in state. Update the session object to `{ version: 4 as const, id: activeSessionId, sections: state.sections, scores, overrides, notes, topicNotes, customQuestions, candidate }` and update the import type from `V3Session` to `V4Session`. Read `sections` from the store (add `const sections = useAppStore((s) => s.sections)` alongside the other state reads). Note: exportSession in yamlExport.ts still expects a V3Session today — check if it accepts an extended type or needs updating. If exportSession's parameter type needs updating, that is Phase 14 scope (YAML-04). For now, the export function can keep using the old V3 fields (scores/overrides/notes/topicNotes/customQuestions/candidate) — just do NOT change the export format, only the storage write is V4. Keep the V3Session type cast in handleExportYaml if changing the export format would break the yamlExport signature. This is a boundary case: Plan 03 only changes the IMPORT side per D-08; export schema changes are Phase 14 scope. If exportSession() only uses the sparse maps (which are unchanged), the existing call can stay as-is by passing only the V3 fields — no update to handleExportYaml is required for correctness. Verify yamlExport.ts signature to confirm.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose src/utils/yamlImport.test.ts</automated>
  </verify>
  <done>
    reKeyImportResultToV4 is exported from yamlImport.ts. ActionsGroup applies reKeyImportResultToV4 before setImportPreview. yamlImport.test.ts (existing tests) still pass. TypeScript compiles. New test assertions (added in Task 2) confirm 'twig-q0' keys in import results.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend bootstrap.test.ts with V4 migration scenarios and yamlImport.test.ts with V4 key assertions</name>
  <files>
    src/storage/bootstrap.test.ts
    src/utils/yamlImport.test.ts
  </files>
  <read_first>
    - src/storage/bootstrap.test.ts — read in full; understand existing four scenarios (A, B, C, D) and the vitest-chrome mock patterns; find where new scenarios E and F should be appended
    - src/utils/yamlImport.test.ts — read in full; understand the existing test structure and fixture format for legacy and structural YAML; find where to append V4 key assertions
    - src/storage/migrations/fixtures/v3-session-fixture.ts — V3_SESSION_POPULATED fixture (produced in Plan 01; use twig-0/twig-1 score keys)
    - src/storage/migrations/v2-to-v3.test.ts — vitest-chrome mock pattern reference (beforeEach vi.clearAllMocks, chrome.storage.local.get mock)
  </read_first>
  <behavior>
    bootstrap.test.ts Scenario E (new): V3 session in storage → migration on bootstrap:
    - Given VALID_V2_MANIFEST in storage + a V3 session blob (use V3_SESSION_POPULATED or inline a V3 fixture)
    - When bootstrap() is called
    - Then: returned sessions[SESSION_ID].version === 4
    - Then: returned sessions[SESSION_ID].sections is a non-empty array
    - Then: returned failedSessionIds is []
    - Then: chrome.storage.local.set was called with a pre-v4 snapshot key matching /^snapshot:.+:pre-v4-\d+$/
    - Then: chrome.storage.local.set was called with the migrated V4 session under 'session:<id>'

    bootstrap.test.ts Scenario F (new): migration failure → session excluded:
    - Given VALID_V2_MANIFEST in storage + an intentionally broken blob that passes V3SessionSchema parse but causes migrateV3ToV4 to throw (or produces invalid V4 — simulate by mocking v.safeParse or providing a weird blob)
    - Simpler approach: provide a blob that passes v.safeParse(V3SessionSchema) validation but returns an output that fails v.safeParse(V4SessionSchema) after migration. Achieve this by using a V3 fixture and then spying on migrateV3ToV4 to throw. OR: provide a malformed blob that fails V3SessionSchema parse AND V4SessionSchema parse AND V2SessionSchema parse (corrupt blob) — in this case the session gets createDefaultV4Session (not a failure case). For a true failure case, spy on migrateV3ToV4 to throw.
    - Simplest approach: mock the vitest module `vi.mock('./migrations/v3-to-v4.js', () => ({ migrateV3ToV4: vi.fn().mockImplementation(() => { throw new Error('migration failed'); }) }))` and verify failedSessionIds contains the session ID. OR: create a blob that has {version:3} and valid V3 fields but intentionally corrupt the result by having migrateV3ToV4 return invalid output — use a spy. Pick the spy approach as it is cleanest.
    - Then: returned sessions does NOT contain SESSION_ID (excluded per D-06 / Pitfall 2)
    - Then: failedSessionIds contains SESSION_ID

    bootstrap.test.ts Scenario G (new): already-V4 session → passed through without re-migration:
    - Given VALID_V2_MANIFEST + a V4 session blob in storage (construct using createDefaultV4Session with sections:[])
    - When bootstrap() is called
    - Then: returned sessions[SESSION_ID] is the V4 session unchanged
    - Then: chrome.storage.local.set was NOT called with a pre-v4 snapshot key (no snapshot for already-V4)

    yamlImport.test.ts new assertions (append to existing describe blocks or add new describe blocks):
    - New describe block 'reKeyImportResultToV4 — score key re-keying':
      - Given result.scores = { 'twig-0': 8, 'twig-1': 6 }
      - reKeyImportResultToV4(result).scores['twig-q0'] === 8
      - reKeyImportResultToV4(result).scores['twig-q1'] === 6
      - 'twig-0' NOT in re-keyed scores
    - New describe block 'reKeyImportResultToV4 — notes key re-keying':
      - Given result.notes = { 'twig-0': 'Good' }
      - reKeyImportResultToV4(result).notes['twig-q0'] === 'Good'
    - New describe block 'reKeyImportResultToV4 — overrides and topicNotes NOT re-keyed':
      - Given result.overrides = { 'twig': 7 }
      - reKeyImportResultToV4(result).overrides['twig'] === 7 (unchanged)
    - New describe block 'parseLegacy → reKeyImportResultToV4 integration':
      - Given a legacy YAML object with scores: { 'twig-0': 8 }
      - parseLegacy(yamlObj, DEFAULT_SECTIONS) returns preview with scores key 'twig-0'
      - reKeyImportResultToV4(preview.result).scores['twig-q0'] === 8
      - Confirms the two-step pipeline (parse → re-key) is end-to-end correct
  </behavior>
  <action>
    Extend src/storage/bootstrap.test.ts by appending three new describe blocks (Scenarios E, F, G) after the existing Scenario D block. Import V3_SESSION_POPULATED (or define an inline V3 fixture) and createDefaultV4Session. For Scenario E, configure the vitest-chrome mock to return VALID_V2_MANIFEST for 'manifest' key and the V3 session blob for 'session:SESSION_ID'. Verify the migration happened by checking sessions[SESSION_ID].version === 4 and sessions[SESSION_ID].sections.length > 0 and failedSessionIds is empty. Verify the pre-v4 snapshot was written by asserting chrome.storage.local.set was called with an argument whose keys include a string matching /^snapshot:SESSION_ID:pre-v4-\d+$/.

    For Scenario F, use vi.mock to make migrateV3ToV4 throw. Import the module at the top of the test file (or use vi.mock with a factory). The bootstrap function will catch the error in the per-session try/catch, push SESSION_ID to failedSessionIds, and NOT add SESSION_ID to sessions. Assert sessions[SESSION_ID] is undefined. Assert failedSessionIds contains SESSION_ID.

    For Scenario G, provide a V4 session blob (use createDefaultV4Session(SESSION_ID) with sections:[] or a small sections array). Assert sessions[SESSION_ID].version === 4 (already V4). Assert chrome.storage.local.set was NOT called with a pre-v4 snapshot key.

    Extend src/utils/yamlImport.test.ts by appending the reKeyImportResultToV4 describe blocks and the integration block. Import reKeyImportResultToV4 from '../utils/yamlImport.js' (adjust path to relative). For the integration test with parseLegacy, construct a minimal legacy YAML object: `{ scores: { 'twig-0': 8 }, candidate: null }` and pass it alongside DEFAULT_SECTIONS. Assert the round-trip: parseLegacy output has 'twig-0' in scores; after reKeyImportResultToV4, scores has 'twig-q0'. This confirms the two-step pipeline is correct without ActionsGroup.

    IMPORTANT bootstrap.test.ts edge case: the vitest-chrome mock for chrome.storage.local.set must be set up to capture ALL calls, not just the first. Use `vi.spyOn(chrome.storage.local, 'set')` or check the existing mock setup in bootstrap.test.ts to see how set is tracked. Ensure the beforeEach vi.clearAllMocks() still fires to reset between tests.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    npm test passes with all pre-existing plus new tests green. bootstrap.test.ts has Scenarios E, F, G. yamlImport.test.ts has reKeyImportResultToV4 describe blocks. reKeyImportResultToV4(result with 'twig-0' key).scores['twig-q0'] === 8. Scenario E: V3 session in storage produces V4 output from bootstrap(). Scenario F: failed migration → failedSessionIds contains the session ID, sessions does not contain it. Full test count increases from 515 by the number of new tests.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | File | Kind |
|--------|------|------|
| `reKeyImportResultToV4` | `src/utils/yamlImport.ts` | exported utility function |
| YAML import V4 wiring | `src/components/ActionsGroup.tsx` | updated call site (reKeyImportResultToV4 applied before setImportPreview) |
| Scenario E (V3 migration) | `src/storage/bootstrap.test.ts` | new test describe block |
| Scenario F (failed migration) | `src/storage/bootstrap.test.ts` | new test describe block |
| Scenario G (already-V4 passthrough) | `src/storage/bootstrap.test.ts` | new test describe block |
| reKeyImportResultToV4 unit tests | `src/utils/yamlImport.test.ts` | new describe blocks |
| parseLegacy → reKey integration test | `src/utils/yamlImport.test.ts` | new describe block |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML file content → parseLegacy/parseStructural → reKeyImportResultToV4 | User-provided YAML file crosses into the import pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | reKeyImportResultToV4 input | accept | Function is a pure transform on user-owned data; no security boundary crossed; same score data the user provided in the YAML |
| T-11-07 | Tampering | import result score keys after re-keying | accept | re-keyed keys follow same format as migration-produced keys; no new injection surface created; valibot validates final session shape before storage |
| T-11-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
npm test (full suite — all 515+ existing tests plus new tests must pass)
</verification>

<success_criteria>
- reKeyImportResultToV4 exported from src/utils/yamlImport.ts
- ActionsGroup applies reKeyImportResultToV4 after parseLegacy/parseStructural before setImportPreview
- yamlImport.test.ts: reKeyImportResultToV4({'twig-0': 8}).scores['twig-q0'] === 8
- bootstrap.test.ts Scenario E: V3 session → bootstrap returns version:4 + sections.length > 0 + failedSessionIds:[]
- bootstrap.test.ts Scenario F: migrateV3ToV4 throws → failedSessionIds contains session ID + session excluded from sessions map
- All 515+ pre-existing tests remain green
- DATA-01 and DATA-02 requirements are satisfied end-to-end
</success_criteria>

<output>
Create `.planning/phases/11-v4-session-migration-legacy-compat/11-03-SUMMARY.md` when done
</output>
