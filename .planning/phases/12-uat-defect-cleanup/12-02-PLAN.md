---
phase: 12-uat-defect-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/app.ts
  - src/components/QuestionCard.tsx
  - src/components/TopicRow.tsx
  - src/test/phase-12-defects.test.tsx
autonomous: true
requirements:
  - UI-09
must_haves:
  truths:
    - "hideNotes field exists in AppState with default false and is NOT persisted to chrome.storage.local"
    - "setHideNotes action exists and toggles the hideNotes boolean"
    - "When hideNotes is true, QuestionCard note textarea container gets the hidden CSS class (unless printMode is true)"
    - "When hideNotes is true, TopicRow notes panel gets the hidden CSS class (unless printMode is true)"
    - "Toggling hideNotes to false removes the hidden class and restores all note areas"
    - "Print mode: hideNotes=true does not suppress note areas when printMode=true"
  artifacts:
    - path: "src/store/app.ts"
      provides: "hideNotes boolean state + setHideNotes action"
      contains: "hideNotes"
    - path: "src/components/QuestionCard.tsx"
      provides: "conditional hidden class on note container driven by hideNotes"
      contains: "hideNotes"
    - path: "src/components/TopicRow.tsx"
      provides: "conditional hidden class on notes panel driven by hideNotes"
      contains: "hideNotes"
  key_links:
    - from: "src/store/app.ts"
      to: "src/components/QuestionCard.tsx"
      via: "useAppStore((s) => s.hideNotes)"
      pattern: "hideNotes"
    - from: "src/store/app.ts"
      to: "src/components/TopicRow.tsx"
      via: "useAppStore((s) => s.hideNotes)"
      pattern: "hideNotes"
    - from: "src/store/app.ts subscribe block"
      to: "chrome.storage.local uiState write"
      via: "hideNotes absent from uiState object"
      pattern: "hideNotes"
---

<objective>
Add the `hideNotes` boolean to the Zustand store and wire it to QuestionCard and TopicRow for UI-09.

This plan establishes the state layer and consumer components. The ActionsGroup wiring (the "Hide notes" button trigger) is in Plan 04 because ActionsGroup.tsx is also modified by the UI-10 icon-only conversion, and those changes must be co-located.

Purpose: `hideNotes` is a volatile UI preference (resets on reload) that suppresses all note areas when true. It mirrors the `hideMarked` boolean pattern exactly — same interface shape, same action shape, same default — but unlike `hideMarked` it MUST NOT be written to the `subscribe` block's `uiState` object (per D-07).
Output: Updated store, QuestionCard, TopicRow, and unit tests confirming the hidden-class behavior and non-persistence.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hideNotes state + action to app.ts store (per D-06, D-07)</name>
  <files>src/store/app.ts, src/test/phase-12-defects.test.tsx</files>
  <behavior>
    - Test: useAppStore initial state has hideNotes === false
    - Test: calling setHideNotes(true) sets hideNotes to true
    - Test: calling setHideNotes(false) sets hideNotes to false
    - Test: the subscribe block's uiState write object does NOT contain a hideNotes key (grep-verifiable: subscribe block at line 613 area does not contain "hideNotes")
  </behavior>
  <action>
    In `src/store/app.ts`, make four additions following the exact `hideMarked` pattern (PATTERNS.md "Boolean store toggle"):

    1. In `AppState` interface, after `hideMarked: boolean` (line 65 area), add:
       `/** Whether to suppress all note areas (per-question and per-topic) — UI-only, not persisted */`
       `hideNotes: boolean;`

    2. In `AppActions` interface, after `setHideMarked: (v: boolean) => void` (line 115 area), add:
       `setHideNotes: (v: boolean) => void;`

    3. In `DEFAULT_STATE`, after `hideMarked: false` (line 168 area), add:
       `hideNotes: false,`

    4. In the store implementation, after `setHideMarked: (v) => set({ hideMarked: v })` (line 280 area), add:
       `setHideNotes: (v) => set({ hideNotes: v }),`

    Critical: do NOT touch the `subscribe` block (line 613 area). The `uiState` object written there already lists `hideMarked` and `darkMode`. Do NOT add `hideNotes` to that object. Verify after editing that the subscribe block does not contain "hideNotes" anywhere.

    Add a "UI-09: hideNotes store" describe block to `src/test/phase-12-defects.test.tsx` (creating the file if Plan 01 has not created it yet — the executor on Plan 02 may run in parallel with Plan 01). Import `useAppStore` and test initial value + setter behavior using `act()`. Add a separate test that reads the subscribe block source via grep or asserts store behavior does not persist the value (testing that after calling `setHideNotes(true)` and then simulating a store reset to DEFAULT_STATE, hideNotes returns to false).
  </action>
  <verify>
    <automated>npx vitest run src/test/phase-12-defects.test.tsx --reporter=verbose</automated>
  </verify>
  <done>hideNotes and setHideNotes exist in the store. Initial value is false. The subscribe block uiState write does not contain hideNotes. All new tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire hideNotes to QuestionCard and TopicRow note areas (per D-08)</name>
  <files>src/components/QuestionCard.tsx, src/components/TopicRow.tsx, src/test/phase-12-defects.test.tsx</files>
  <behavior>
    - Test: QuestionCard with hideNotes=true has the note container div with className including "hidden"
    - Test: QuestionCard with hideNotes=true AND printMode=true does NOT have "hidden" on the note container
    - Test: QuestionCard with hideNotes=false does NOT have "hidden" on the note container
    - Test: TopicRow with hideNotes=true has the notes panel with className including "hidden"
    - Test: TopicRow with hideNotes=true AND printMode=true does NOT have "hidden" on the notes panel
  </behavior>
  <action>
    In `src/components/QuestionCard.tsx`:
    - Add `const hideNotes = useAppStore((s) => s.hideNotes);` alongside existing store subscriptions.
    - Locate the outer `<div>` that wraps the note toggle button and note textarea (the notes section container). Apply a conditional className: `${hideNotes && !printMode ? 'hidden' : ''}`.
    - Use the CSS `hidden` class on the outer wrapper div — NOT the HTML `hidden` attribute on the textarea itself. This is required per RESEARCH.md Pitfall 5: the HTML `hidden` attribute cannot be overridden by Tailwind `print:` variants, but a CSS `hidden` class on the wrapper CAN be overridden by `print:` variants if needed. The existing `print:` Tailwind variant on the note textarea is preserved unchanged.
    - The existing `printMode` guard logic for the textarea's own rendering is unchanged.

    In `src/components/TopicRow.tsx`:
    - Add `const hideNotes = useAppStore((s) => s.hideNotes);` alongside existing store subscriptions.
    - Locate the notes panel `<div>` (line 75 area, the outer container for both the "Add topic notes" toggle button and the textarea). Apply `hideNotes` as an additional className condition: `${hideNotes && !printMode ? 'hidden' : ''}`.
    - The existing `hidden` HTML attribute on the textarea itself (line 90 area: `hidden={!topicNotesOpen && !localTopicNote && !printMode}`) remains unchanged — it controls the textarea's own visibility. The `hideNotes` class applies to the outer wrapper div, suppressing both the toggle button and textarea together.
    - Print guard: use `hideNotes && !printMode` so that when `printMode === true`, the notes panel is NOT suppressed regardless of `hideNotes`.

    Extend `src/test/phase-12-defects.test.tsx` with describe blocks for QuestionCard and TopicRow note suppression. Mock `useAppStore` to inject `hideNotes` and `printMode` values. Use `@testing-library/react` render + `getByTestId` or className queries to verify the `hidden` class presence/absence.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All tests pass. QuestionCard note container gets hidden class when hideNotes=true and printMode=false. TopicRow notes panel gets hidden class when hideNotes=true and printMode=false. Neither suppresses during printMode=true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store → chrome.storage.local | Only explicitly-listed uiState fields are written; hideNotes must not cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Information Disclosure | hideNotes in subscribe block | mitigate | hideNotes must NOT appear in the uiState write in the subscribe block (D-07); verified by test assertion and code review |
| T-12-04 | Tampering | XSS via emoji in title/aria-label | accept | React escapes all string props; no raw HTML introduced |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes all tests
- `src/store/app.ts` contains `hideNotes: boolean` in AppState, `setHideNotes` in AppActions, `hideNotes: false` in DEFAULT_STATE, `setHideNotes: (v) => set({ hideNotes: v })` in implementation
- `src/store/app.ts` subscribe block uiState object does NOT contain `hideNotes`
- `src/components/QuestionCard.tsx` and `src/components/TopicRow.tsx` each import `hideNotes` from store and apply `hidden` class conditional with `!printMode` guard
</verification>

<success_criteria>
- hideNotes=true hides all note areas in QuestionCard and TopicRow on screen
- hideNotes=true does not suppress note areas when printMode=true
- hideNotes resets to false on page reload (not persisted)
</success_criteria>

<output>
Create `.planning/phases/12-uat-defect-cleanup/12-02-SUMMARY.md` when done
</output>
