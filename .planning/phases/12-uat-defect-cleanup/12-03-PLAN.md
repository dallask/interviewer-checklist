---
phase: 12-uat-defect-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SidebarGroup.tsx
  - src/components/Sidebar.tsx
  - src/app/App.tsx
autonomous: true
requirements:
  - UI-11
  - UI-12
must_haves:
  truths:
    - "SidebarGroup renders a leading icon span before the label text when the icon prop is provided"
    - "SidebarGroup renders identically to before when no icon prop is provided (backwards-compatible)"
    - "Sidebar section headers Search, Difficulty, Sections, and Actions each show a leading icon"
    - "Sidebar visibility is controlled solely by sidebarOpen on all viewports including desktop (md:)"
    - "Backdrop overlay is shown whenever sidebarOpen is true on all viewports including desktop (md:)"
  artifacts:
    - path: "src/components/SidebarGroup.tsx"
      provides: "optional icon prop rendered before label in section header button"
      contains: "icon?: ReactNode"
    - path: "src/components/Sidebar.tsx"
      provides: "icon props passed to all four SidebarGroup instances; md:relative md:translate-x-0 removed"
      contains: "icon="
    - path: "src/app/App.tsx"
      provides: "backdrop overlay without md:hidden class"
      contains: "bg-black/40 z-40 print:hidden"
  key_links:
    - from: "Sidebar.tsx SidebarGroup calls"
      to: "SidebarGroup.tsx icon prop"
      via: "icon prop string literal (emoji)"
      pattern: "icon="
    - from: "Sidebar.tsx aside className"
      to: "sidebarOpen store value"
      via: "translate-x-0 / -translate-x-full conditional only (md: overrides removed)"
      pattern: "translate-x"
---

<objective>
Two independent UI fixes applied together because they share Sidebar.tsx (making them a single-file conflict):

UI-11 (per D-17, D-18): Add optional `icon?: ReactNode` prop to `SidebarGroup`. Pass emoji icons to all four SidebarGroup instances in Sidebar.tsx: 🔍 Search, 🎯 Difficulty, 📋 Sections, ⚡ Actions.

UI-12 (per D-10, D-11, D-12, D-13): Remove `md:relative md:translate-x-0` from the Sidebar.tsx `<aside>` className so that `sidebarOpen` is the sole visibility signal at all viewport widths. Remove `md:hidden` from the App.tsx backdrop div so the backdrop shows whenever the sidebar is open at any viewport width.

Purpose: Both are minimal class/prop changes with no new dependencies. No store changes — `sidebarOpen: true` default is already correct in DEFAULT_STATE (D-12 confirms no change needed).
Output: Three modified files, no new test file needed (UI-12 desktop toggle behavior is verified manually per the test map in RESEARCH.md; UI-11 icon rendering is verifiable via existing Vitest component tests if SidebarGroup has coverage, but new automated tests for icon rendering are included in the test extension).
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/12-uat-defect-cleanup/12-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add icon prop to SidebarGroup and pass icons in Sidebar.tsx (UI-11, per D-17, D-18)</name>
  <files>src/components/SidebarGroup.tsx, src/components/Sidebar.tsx, src/test/phase-12-defects.test.tsx</files>
  <behavior>
    - Test: SidebarGroup rendered with icon="🔍" renders a span with aria-hidden="true" containing "🔍" before the label text
    - Test: SidebarGroup rendered without icon prop renders no icon span — output is identical to the pre-change behavior
    - Test: icon span has aria-hidden="true" so screen readers skip it
  </behavior>
  <action>
    In `src/components/SidebarGroup.tsx` (the full 35-line file from RESEARCH.md Q6):

    1. Add `icon?: ReactNode` to `SidebarGroupProps` interface (after `label: string`).
    2. Add `icon` to the destructured props of the `SidebarGroup` function.
    3. Inside the header `<button>`, replace `<span>{label}</span>` with a flex wrapper:
       ```
       <span className="flex items-center gap-2">
         {icon && <span aria-hidden="true">{icon}</span>}
         {label}
       </span>
       ```
       The `gap-2` keeps the icon and label visually separated. The `aria-hidden="true"` on the icon span ensures screen readers only announce the label text. The chevron ▾ span and its `rotate-180` animation are unchanged and stay on the right side of the button.

    4. The `groupId` prop remains in the interface but continues to NOT be destructured into the function body — leave this as-is per the RESEARCH.md Q6 note.

    In `src/components/Sidebar.tsx`, locate the four `<SidebarGroup>` call sites (lines 24–58 area) and add the `icon` prop to each:
    - groupId="search" → `icon="🔍"`
    - groupId="difficulty" → `icon="🎯"`
    - groupId="sections" → `icon="📋"`
    - groupId="actions" → `icon="⚡"`

    Emoji string literals are valid `ReactNode` values — no wrapper needed at the call site.

    Add a "UI-11: SidebarGroup icon prop" describe block to `src/test/phase-12-defects.test.tsx`. Render `<SidebarGroup groupId="test" label="Test" icon="🔍" isOpen={false} onToggle={() => {}}><div /></SidebarGroup>` and assert the icon span presence. Render again without `icon` and assert no icon span.
  </action>
  <verify>
    <automated>npx vitest run src/test/phase-12-defects.test.tsx --reporter=verbose</automated>
  </verify>
  <done>SidebarGroup renders icon before label when icon prop is provided. Sidebar.tsx passes 🔍/🎯/📋/⚡ to the four group instances. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove md: breakpoint overrides for sidebar visibility and backdrop (UI-12, per D-10, D-11)</name>
  <files>src/components/Sidebar.tsx, src/app/App.tsx</files>
  <action>
    In `src/components/Sidebar.tsx`, locate the `<aside>` element (line 22 area). The current className string contains `md:relative md:translate-x-0` embedded among the other Tailwind classes. Remove exactly those two tokens: `md:relative` and `md:translate-x-0`.

    The resulting className must still contain (in any order):
    - `w-[280px] flex-shrink-0 bg-gray-100 dark:bg-gray-800 flex flex-col overflow-y-auto`
    - `transition-transform duration-200 ease-in-out motion-reduce:transition-none`
    - `fixed inset-y-0 left-0 z-50 print:hidden`
    - `${sidebarOpen ? 'translate-x-0' : '-translate-x-full'}` (the dynamic classes)

    After this change, the sidebar is `fixed` on all viewports and its translate position is determined solely by `sidebarOpen`. The sidebar default (`sidebarOpen: true` in DEFAULT_STATE, line 161) means desktop users see the sidebar open on first load — intentional per D-12. No DEFAULT_STATE change is needed.

    In `src/app/App.tsx`, locate the backdrop overlay (lines 71–77 area):
    ```
    <div
      className="fixed inset-0 bg-black/40 z-40 md:hidden print:hidden"
      aria-hidden="true"
      onClick={() => setSidebarOpen(false)}
    />
    ```
    Remove only `md:hidden` from the className. The result must be:
    ```
    className="fixed inset-0 bg-black/40 z-40 print:hidden"
    ```

    Do NOT modify the sidebar toggle button in App.tsx — RESEARCH.md Q5 confirms it has no `md:hidden` class and requires no change (per D-13).

    After both changes, run `npx vitest run` to confirm no test regressions (no existing tests cover these class names, but run the suite to verify no side-effects).

    Layout impact (expected, not a regression): on desktop the sidebar is now a floating overlay (not inline in the flex row). The main content fills the full viewport width when the sidebar is open or closed. The backdrop provides a click-to-close target at all viewport widths. This is the intended behavior per D-10/D-11.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Sidebar.tsx aside className no longer contains md:relative or md:translate-x-0. App.tsx backdrop div className no longer contains md:hidden. All existing tests pass. Sidebar toggle works at all viewport widths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser viewport → sidebar layout | Sidebar overlay now shows at all viewports; backdrop provides click-to-close at all widths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | XSS via emoji in icon prop | accept | React escapes all ReactNode string values; emoji characters contain no HTML |
| T-12-06 | Denial of Service | Sidebar overlay on desktop | accept | Backdrop onClick={() => setSidebarOpen(false)} gives users a reliable close path at all viewports; intended design contract per D-10/D-11 |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run --reporter=verbose` passes all tests
- `src/components/SidebarGroup.tsx` contains `icon?: ReactNode` in props interface and renders `{icon && <span aria-hidden="true">{icon}</span>}` in button
- `src/components/Sidebar.tsx` passes icon props to all four SidebarGroup instances AND does NOT contain `md:relative` or `md:translate-x-0`
- `src/app/App.tsx` backdrop className does NOT contain `md:hidden`
- `grep -v '^//' src/components/Sidebar.tsx | grep -c 'md:relative'` returns 0
- `grep -v '^//' src/app/App.tsx | grep -c 'md:hidden'` returns 0
</verification>

<success_criteria>
- Sidebar section headers show leading emoji icons (UI-11)
- Sidebar toggle closes and opens sidebar on desktop viewport (UI-12)
- Backdrop appears when sidebar is open at all viewport widths (UI-12)
- SidebarGroup renders identically to before when icon prop is omitted
</success_criteria>

<output>
Create `.planning/phases/12-uat-defect-cleanup/12-03-SUMMARY.md` when done
</output>
