---
phase: 13-filter-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/app.ts
  - src/components/DifficultyFilter.tsx
  - src/components/DifficultyFilter.test.tsx
  - src/components/SectionFilter.tsx
  - src/components/SectionFilter.test.tsx
autonomous: true
requirements:
  - UI-16
  - UI-17
must_haves:
  truths:
    - "DifficultyFilter renders an 'All levels' row (∞ icon) as the first button; it shows aria-pressed=true when no difficulties are selected and aria-pressed=false when any difficulty is selected (D-01)"
    - "Clicking 'All levels' when one or more difficulties are selected clears selectedDifficulties to an empty Set; clicking when already empty is a no-op (D-01)"
    - "Each difficulty row shows an 8×8px color dot before its label: Novice green, Intermediate blue, Advanced orange, Expert pink (D-06)"
    - "Difficulty labels read Novice / Intermediate / Advanced / Expert — no multiplier text (D-04)"
    - "Each difficulty row and the 'All levels' row shows a question count badge on the right; the badge is computed via useMemo over DEFAULT_SECTIONS with empty dep array (D-02, D-05)"
    - "SectionFilter renders an 'All sections' row (📋 icon) as the first button; it shows aria-pressed=true when no sections are selected and aria-pressed=false when any section is selected (D-01)"
    - "Clicking 'All sections' when one or more sections are selected clears selectedSections to an empty Set; clicking when already empty is a no-op (D-01)"
    - "Each section row renders section.icon as an emoji before the label; the right slot shows a question count (integer), not a score mark (D-07, D-08)"
    - "SectionFilter no longer imports scores, overrides, customQuestions, computeTopicMark, computeSectionMark, BAND_COLORS, or MarkBand (D-07)"
    - "clearDifficulties and clearSections are separate thin actions in AppActions / useAppStore that set the respective Set to new Set() (D-03)"
  artifacts:
    - path: "src/store/app.ts"
      provides: "clearDifficulties and clearSections actions"
      contains: "clearDifficulties"
    - path: "src/components/DifficultyFilter.tsx"
      provides: "UI-16 filter widget"
      contains: "All levels"
    - path: "src/components/SectionFilter.tsx"
      provides: "UI-17 filter widget"
      contains: "All sections"
    - path: "src/components/DifficultyFilter.test.tsx"
      provides: "UI-16 test coverage"
      contains: "All levels"
    - path: "src/components/SectionFilter.test.tsx"
      provides: "UI-17 test coverage"
      contains: "All sections"
  key_links:
    - from: "src/components/DifficultyFilter.tsx"
      to: "src/store/app.ts"
      via: "useAppStore selector"
      pattern: "clearDifficulties"
    - from: "src/components/SectionFilter.tsx"
      to: "src/store/app.ts"
      via: "useAppStore selector"
      pattern: "clearSections"
    - from: "src/components/DifficultyFilter.tsx"
      to: "src/data/bank/index.ts"
      via: "useMemo over DEFAULT_SECTIONS"
      pattern: "DEFAULT_SECTIONS"
    - from: "src/components/SectionFilter.tsx"
      to: "src/data/bank/index.ts"
      via: "useMemo over DEFAULT_SECTIONS"
      pattern: "DEFAULT_SECTIONS"
---

<objective>
Add "All" rows, color/emoji indicators, and live question-count badges to DifficultyFilter and SectionFilter; wire two new clear actions in the Zustand store.

Purpose: Satisfies UI-16 and UI-17 — users can see at a glance how many questions live in each difficulty and section, and can select "all" with a single click.

Output:
- src/store/app.ts — clearDifficulties and clearSections actions added (per D-03)
- src/components/DifficultyFilter.tsx — "All levels" row, color dots, count badges, normalized labels (per D-01, D-04, D-05, D-06, UI-16)
- src/components/SectionFilter.tsx — "All sections" row, section.icon emoji, count badge replacing score mark; scoring imports removed (per D-01, D-02, D-07, D-08, UI-17)
- Both test files updated/extended for new behavior
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/PROJECT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/13-filter-overhaul/13-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/13-filter-overhaul/13-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/13-filter-overhaul/13-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/store/app.ts
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/DifficultyFilter.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionFilter.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/data/bank/types.ts
@/Users/dallask/Projects/dallask/interviewer-checklist/src/data/bank/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clearDifficulties and clearSections store actions (per D-03)</name>
  <files>src/store/app.ts</files>
  <action>
    Make two targeted edits to src/store/app.ts:

    1. In the AppActions interface (around line 116, after `toggleSection: (id: string) => void;`), add:
       clearDifficulties: () => void;
       clearSections: () => void;

    2. In the useAppStore create() body (around line 282-283, immediately after the closing brace of the toggleSection implementation and before setHideMarked), add the two action implementations:
       clearDifficulties: () => set({ selectedDifficulties: new Set() }),
       clearSections: () => set({ selectedSections: new Set() }),

    These follow the exact same pattern as resetAll (store line ~339) which sets selectedDifficulties/selectedSections to new Set(). Do NOT modify toggleDifficulty or toggleSection. The new actions are separate; no sentinel value repurposing (D-03).

    The store subscribe block at line ~627 serializes selectedDifficulties as [...state.selectedDifficulties] — new Set() serializes to [] and rehydrates to new Set(), so no subscribe changes are needed.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -n "clearDifficulties\|clearSections" src/store/app.ts</automated>
  </verify>
  <done>grep finds clearDifficulties and clearSections appearing in both the AppActions interface block and the useAppStore create body (at minimum 4 matches total: 2 in interface, 2 in implementation)</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild DifficultyFilter with "All levels" row, color dots, count badges, and normalized labels (per UI-16, D-01, D-04, D-05, D-06)</name>
  <files>src/components/DifficultyFilter.tsx, src/components/DifficultyFilter.test.tsx</files>
  <action>
    COMPONENT (src/components/DifficultyFilter.tsx):

    Replace the full file content. The new version:

    1. Imports: keep `import type { Difficulty } from '../data/bank/types.js'` and `import { useAppStore } from '../store/app.js'`. Add `import { useMemo } from 'react'` and `import { DEFAULT_SECTIONS } from '../data/bank/index.js'`.

    2. DIFFICULTY_LABELS map — update all four values to plain names per D-04:
       novice: 'Novice'
       intermediate: 'Intermediate'
       advanced: 'Advanced'
       expert: 'Expert'

    3. Add a static DOT_CLASSES record per D-06 (full class strings as literals for Tailwind scanner):
       novice: 'bg-green-500'
       intermediate: 'bg-blue-500'
       advanced: 'bg-orange-500'
       expert: 'bg-pink-500'
       Type: Record<Difficulty, string>

    4. Inside DifficultyFilter(), add a useMemo with empty dep array [] per D-05:
       const questionCounts = useMemo(() => {
         const all = DEFAULT_SECTIONS.flatMap(s => s.items).flatMap(t => t.questions);
         return {
           novice: all.filter(q => q.level === 'novice').length,
           intermediate: all.filter(q => q.level === 'intermediate').length,
           advanced: all.filter(q => q.level === 'advanced').length,
           expert: all.filter(q => q.level === 'expert').length,
         };
       }, []);

    5. Add clearDifficulties selector: const clearDifficulties = useAppStore((s) => s.clearDifficulties);

    6. Return JSX: a wrapping div (className="flex flex-col gap-2") containing:

       a) "All levels" button FIRST (per D-01):
          - type="button"
          - aria-pressed={selectedDifficulties.size === 0}
          - onClick: if (selectedDifficulties.size > 0) clearDifficulties(); (no-op when empty, per D-01)
          - className ternary: when pressed (size===0) use 'bg-blue-600 text-white dark:bg-blue-500'; when unpressed use 'bg-gray-200 text-gray-700 dark:bg-gray-700 dark:text-gray-200'
          - Base layout classes: 'flex items-center gap-2 text-sm px-3 py-2 rounded-full text-left focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none'
          - Inner content: <span aria-hidden="true">∞</span> then text "All levels" then <span className="ml-auto text-xs tabular-nums text-gray-400 dark:text-gray-500"> showing sum of all four questionCounts values

       b) Difficulty row buttons (existing shape, extended per D-06):
          - Same outer layout as current: 'flex items-center gap-2 text-sm px-3 py-2 rounded-full text-left focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none' plus pressed/unpressed ternary (unchanged)
          - Note: current component uses py-1.5; change to py-2 to match "All levels" row height from UI-SPEC
          - Inner content: <span aria-hidden="true" className={`w-2 h-2 rounded-full flex-shrink-0 ${DOT_CLASSES[difficulty]}`} /> then {DIFFICULTY_LABELS[difficulty]} then <span className="ml-auto text-xs tabular-nums text-gray-400 dark:text-gray-500">{questionCounts[difficulty]}</span>

    TEST FILE (src/components/DifficultyFilter.test.tsx):

    Read the existing file first. Then make these targeted changes:

    1. Add clearDifficulties: vi.fn() to the mock setup alongside toggleDifficulty.

    2. Add clearDifficulties to all mockUseAppStore.mockImplementation selector objects.

    3. Update any query using /beginner/i to use /novice/i (label normalization per D-04).

    4. Update any test asserting button count: the component now renders 5 buttons (1 All + 4 difficulties). Find any expect(buttons).toHaveLength(4) and change to toHaveLength(5).

    5. Add new test cases for UI-16 "All levels" row:
       - '"All levels" row has aria-pressed="true" when no difficulties selected': render with selectedDifficulties: new Set(); find button with name /all levels/i; expect toHaveAttribute('aria-pressed', 'true')
       - '"All levels" row has aria-pressed="false" when a difficulty is selected': mock with selectedDifficulties: new Set(['novice']); expect toHaveAttribute('aria-pressed', 'false')
       - 'clicking "All levels" calls clearDifficulties when a difficulty is selected': mock with selectedDifficulties: new Set(['novice']) and clearDifficulties: vi.fn(); fireEvent.click; expect clearDifficulties called once
       - 'clicking "All levels" is a no-op when selectedDifficulties is empty': mock with new Set() and clearDifficulties: vi.fn(); fireEvent.click; expect clearDifficulties NOT called
       - 'each difficulty row renders a color dot span': for each of the four difficulties, render and check getByRole('button', { name: /novice/i }) (etc.) contains a span with class matching bg-green-500 / bg-blue-500 / bg-orange-500 / bg-pink-500
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test -- --reporter=verbose src/components/DifficultyFilter</automated>
  </verify>
  <done>All DifficultyFilter tests pass. The component renders 5 buttons (All + 4 difficulties), "All levels" aria-pressed semantics are correct, clearDifficulties is called correctly, color dots are present, labels read Novice/Intermediate/Advanced/Expert.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild SectionFilter with "All sections" row, section.icon emoji, count badge replacing score mark (per UI-17, D-01, D-07, D-08)</name>
  <files>src/components/SectionFilter.tsx, src/components/SectionFilter.test.tsx</files>
  <action>
    COMPONENT (src/components/SectionFilter.tsx):

    Replace the full file content. The new version:

    1. Imports: keep `import { DEFAULT_SECTIONS } from '../data/bank/index.js'` and `import { useAppStore } from '../store/app.js'`. Add `import { useMemo } from 'react'`.
       REMOVE all of: MarkBand type import, computeSectionMark import, computeTopicMark import, and the BAND_COLORS constant (per D-07). The file must not import from '../scoring/index.js' at all after this change.

    2. Inside SectionFilter(), add a useMemo with empty dep array [] per D-05:
       const sectionCounts = useMemo(
         () => Object.fromEntries(
           DEFAULT_SECTIONS.map(s => [s.id, s.items.reduce((n, t) => n + t.questions.length, 0)])
         ),
         []
       );

    3. Store selectors: keep selectedSections and toggleSection. Add clearSections: const clearSections = useAppStore((s) => s.clearSections).
       REMOVE selectors for scores, overrides, customQuestions (per D-07).

    4. Return JSX: a wrapping div (className="flex flex-col") containing:

       a) "All sections" button FIRST (per D-01):
          - type="button"
          - aria-pressed={selectedSections.size === 0}
          - onClick: if (selectedSections.size > 0) clearSections(); (no-op when empty, per D-01)
          - className ternary: when pressed (size===0) add 'border-l-2 border-blue-600 dark:border-blue-400 bg-blue-50 dark:bg-blue-900/20'; when unpressed add 'border-l-2 border-transparent'
          - Base layout classes: 'w-full flex items-center px-3 py-2 text-sm text-left text-gray-900 dark:text-gray-100 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none'
          - Inner content: <span aria-hidden="true">📋</span> then <span className="flex-1">All sections</span> then <span className="ml-auto text-xs tabular-nums text-gray-400 dark:text-gray-500"> showing Object.values(sectionCounts).reduce((a, b) => a + b, 0)

       b) Section row buttons (existing shape, modified per D-07 and D-08):
          - Layout classes: same as current (unchanged)
          - Inner content CHANGE: prepend <span aria-hidden="true">{section.icon}</span> before the label span; replace the right slot span (which showed markDisplay with colorClass) with a plain count badge: <span className="ml-auto text-xs tabular-nums text-gray-400 dark:text-gray-500">{sectionCounts[section.id] ?? 0}</span>
          - Remove all computeTopicMark / computeSectionMark logic from the .map() body — only isSelected is needed per iteration

    TEST FILE (src/components/SectionFilter.test.tsx):

    Read the existing file first. Then make these targeted changes:

    1. Remove scores, overrides, customQuestions from the mockUseAppStore selector object in beforeEach (D-07 — component no longer reads these). Add clearSections: vi.fn() alongside toggleSection.

    2. Update any test asserting button count from 9 to 10 (1 All + 9 sections).

    3. Remove or replace any assertions that check for '—' text (score placeholders). The right slot now shows integers; assert that count badges contain numeric strings matching /^\d+$/ instead (or assert specific values using the actual DEFAULT_SECTIONS question counts).

    4. Add new test cases for UI-17 "All sections" row:
       - '"All sections" row has aria-pressed="true" when no sections selected': render with selectedSections: new Set(); find button with name /all sections/i; expect toHaveAttribute('aria-pressed', 'true')
       - '"All sections" row has aria-pressed="false" when a section is selected': mock with selectedSections: new Set(['frontend']); expect toHaveAttribute('aria-pressed', 'false')
       - 'clicking "All sections" calls clearSections when a section is selected': mock with selectedSections: new Set(['frontend']) and clearSections: vi.fn(); fireEvent.click; expect clearSections called once
       - 'clicking "All sections" is a no-op when selectedSections is empty': mock with new Set() and clearSections: vi.fn(); fireEvent.click; expect clearSections NOT called
       - 'each section row renders the section.icon emoji': render; query each section button (e.g. getByRole('button', { name: /frontend/i })); verify the emoji from that section's icon field appears in its text content
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test -- --reporter=verbose src/components/SectionFilter</automated>
  </verify>
  <done>All SectionFilter tests pass. The component renders 10 buttons (All + 9 sections), "All sections" aria-pressed semantics are correct, clearSections is called correctly, section.icon emoji appears per row, count badge shows integer (not '—'), scoring imports are absent from the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bank data → component render | DEFAULT_SECTIONS is a compiled static module; values are not user-supplied |
| store Set → component render | selectedDifficulties and selectedSections contain only Difficulty union literals or section ID strings from DEFAULT_SECTIONS; no user-typed input enters these Sets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | section.icon render | accept | section.icon values are static string literals from the compiled bank module — not user-provided data; React's JSX escapes string content by default; no XSS risk |
| T-13-02 | Tampering | clearDifficulties / clearSections | accept | actions accept no arguments and produce only new Set(); no input to validate |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Phase 13 installs no new packages (verified: RESEARCH.md Package Legitimacy Audit section confirms zero new dependencies) |
</threat_model>

<verification>
After all three tasks complete, run the full test suite:

  cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test

Expected: all existing tests pass plus the new DifficultyFilter and SectionFilter tests. Look for:
- No "Unable to find an accessible element" errors from stale /beginner/i queries
- No failing "—" text assertions in SectionFilter tests
- DifficultyFilter: 5 buttons rendered (verified by updated button count test)
- SectionFilter: 10 buttons rendered (verified by updated button count test)
- Full suite green (598+ tests baseline)

Grep checks for anti-patterns:
  grep -n "beginner" src/components/DifficultyFilter.tsx src/components/DifficultyFilter.test.tsx
  (should return zero matches — label normalization complete per D-04)

  grep -n "computeTopicMark\|computeSectionMark\|BAND_COLORS\|MarkBand" src/components/SectionFilter.tsx
  (should return zero matches — scoring imports removed per D-07)
</verification>

<success_criteria>
Phase 13 is complete when:
1. DifficultyFilter renders "All levels" as the first row with ∞ icon; aria-pressed=true when selectedDifficulties is empty, aria-pressed=false when any difficulty is selected (D-01)
2. Clicking "All levels" with non-empty Set calls clearDifficulties; clicking with empty Set is a no-op (D-01)
3. Each difficulty button has a color dot: Novice green, Intermediate blue, Advanced orange, Expert pink (D-06)
4. Difficulty labels read Novice / Intermediate / Advanced / Expert — no multiplier text (D-04)
5. Each difficulty row and "All levels" show a question count badge on the right; computed via useMemo([], []) (D-05)
6. SectionFilter renders "All sections" as the first row with 📋 icon; aria-pressed=true when selectedSections is empty, aria-pressed=false when any section is selected (D-01)
7. Clicking "All sections" with non-empty Set calls clearSections; clicking with empty Set is a no-op (D-01)
8. Each section row shows section.icon emoji before the label (D-08)
9. Each section row right slot shows a question count integer; no score mark / "—" is rendered (D-07)
10. SectionFilter does not import from '../scoring/index.js' (D-07)
11. clearDifficulties and clearSections exist in AppActions interface and useAppStore body (D-03)
12. npm test passes green with 598+ tests
</success_criteria>

<output>
Create `.planning/phases/13-filter-overhaul/13-01-SUMMARY.md` when done
</output>
