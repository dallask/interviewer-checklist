---
phase: 14-editable-bank-yaml-schema-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/storage/types.ts
  - src/store/app.ts
  - src/app/App.tsx
autonomous: true
requirements:
  - BANK-01
  - BANK-02
  - BANK-03
  - BANK-04
  - BANK-05

must_haves:
  truths:
    - "V4SessionSchema includes removedDefaultQuestionIds: optional array<string> with default []"
    - "AppState includes removedDefaultQuestionIds: Set<string>"
    - "Five new store actions exist: addSection, removeSection, addTopic, removeTopic, removeDefaultQuestion"
    - "subscribe block writes removedDefaultQuestionIds as a spread array to storage"
    - "switchSession hydrates removedDefaultQuestionIds as new Set(session.removedDefaultQuestionIds ?? [])"
    - "importSession sets sections and removedDefaultQuestionIds in both overwriteActive branches"
    - "expandAll and collapseAll iterate state.sections instead of DEFAULT_SECTIONS"
    - "App.tsx reads sections from store and passes state.sections to buildFlatRows"
    - "markedTopicIds computation in App.tsx uses state.sections instead of DEFAULT_SECTIONS"
    - "resetAll does NOT clear removedDefaultQuestionIds (per key-constraints)"
    - "DEFAULT_STATE initializes removedDefaultQuestionIds as new Set()"
  artifacts:
    - path: "src/storage/types.ts"
      provides: "V4SessionSchema with removedDefaultQuestionIds field"
      contains: "removedDefaultQuestionIds"
    - path: "src/store/app.ts"
      provides: "5 new bank actions + hydration wiring"
      exports: ["addSection", "removeSection", "addTopic", "removeTopic", "removeDefaultQuestion"]
    - path: "src/app/App.tsx"
      provides: "sections from store wired to buildFlatRows"
      contains: "state.sections"
  key_links:
    - from: "src/store/app.ts (subscribe block)"
      to: "chrome.storage.local"
      via: "storageAdapter.write"
      pattern: "removedDefaultQuestionIds.*\\.\\.\\.state\\.removedDefaultQuestionIds"
    - from: "src/store/app.ts (switchSession)"
      to: "AppState.removedDefaultQuestionIds"
      via: "new Set(session?.removedDefaultQuestionIds ?? [])"
      pattern: "new Set.*removedDefaultQuestionIds"
---

<objective>
Lay the foundational data layer for Phase 14: extend the V4 schema, add 5 bank-mutation store actions, wire persistence (subscribe + hydration), fix App.tsx to read live sections from the store, and fix expandAll/collapseAll to iterate state.sections.

Purpose: Every other plan in this phase depends on these types and actions existing. Without this plan, nothing can be built — components need the actions, buildFlatRows needs the V4Section type, YAML layer needs the store fields.

Output: Updated types.ts, app.ts (schema + actions + wiring), App.tsx (sections from store). Zero UI changes.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-editable-bank-yaml-schema-expansion/14-CONTEXT.md
@.planning/phases/14-editable-bank-yaml-schema-expansion/14-RESEARCH.md
@src/storage/types.ts
@src/store/app.ts
@src/app/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend V4SessionSchema + AppState + DEFAULT_STATE</name>
  <files>src/storage/types.ts, src/store/app.ts</files>
  <behavior>
    - V4SessionSchema parses `{ ..., removedDefaultQuestionIds: [] }` when field is absent (fallback)
    - V4SessionSchema parses `{ ..., removedDefaultQuestionIds: ["frontend-q0-q2"] }` correctly
    - V4Session TypeScript type infers `removedDefaultQuestionIds: string[]`
    - AppState.removedDefaultQuestionIds is typed as `Set<string>`
    - DEFAULT_STATE.removedDefaultQuestionIds is `new Set()`
    - AppActions declares 5 new methods: addSection, removeSection, addTopic, removeTopic, removeDefaultQuestion
  </behavior>
  <action>
    In `src/storage/types.ts`:

    Add `removedDefaultQuestionIds` to `V4SessionSchema` (per D-02, D-05). Use valibot v1 `v.optional(v.array(v.string()), [])` syntax. Insert it between `sections` and `scores` fields. The derived `V4Session` type will automatically include `removedDefaultQuestionIds: string[]` via `v.InferOutput`.

    Also update `createDefaultV4Session` factory: add `removedDefaultQuestionIds: []` to the returned literal so it satisfies the updated schema.

    In `src/store/app.ts`:

    1. Add `removedDefaultQuestionIds: Set<string>` to the `AppState` interface after the `sections` field (per D-02).

    2. Add `DEFAULT_STATE.removedDefaultQuestionIds: new Set<Difficulty>()` — actually `new Set<string>()` — after `sections: []`.

    3. Add 5 new action signatures to `AppActions` (per D-02):
       - `addSection(section: V4Section): void`
       - `removeSection(sectionId: string): void`
       - `addTopic(sectionId: string, topic: V4Topic): void`
       - `removeTopic(topicId: string): void`
       - `removeDefaultQuestion(questionId: string): void`

    Import `V4Topic` from `../storage/types.js` alongside the existing `V4Section` and `V4Session` imports.

    Note on valibot syntax: if `v.optional(v.array(v.string()), [])` does not compile with the project's valibot version, use `v.fallback(v.optional(v.array(v.string())), [])` instead — check `node_modules/valibot/dist/index.d.ts` for the correct overload.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error TS|Cannot find" | head -20</automated>
  </verify>
  <done>TypeScript build passes with no errors related to types.ts or app.ts interface changes. `V4SessionSchema` and `AppState` contain `removedDefaultQuestionIds`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement 5 store actions + fix subscribe/hydration/expandAll/App.tsx</name>
  <files>src/store/app.ts, src/app/App.tsx</files>
  <behavior>
    - addSection appends a V4Section to state.sections array
    - removeSection filters out the section with matching id from state.sections
    - addTopic maps over sections, finds matching sectionId, appends topic to its topics array
    - removeTopic maps over sections filtering out the topic with matching topicId from each section's topics
    - removeDefaultQuestion adds questionId to a new Set copy of removedDefaultQuestionIds
    - subscribe block includes `removedDefaultQuestionIds: [...state.removedDefaultQuestionIds]` in the session write
    - switchSession hydrates `removedDefaultQuestionIds: new Set(session?.removedDefaultQuestionIds ?? [])`
    - importSession sets `sections: data.sections ?? []` and `removedDefaultQuestionIds: new Set(data.removedDefaultQuestionIds ?? [])` in both branches (overwriteActive=true and overwriteActive=false)
    - expandAll reads from `get().sections` (not DEFAULT_SECTIONS) to iterate topics
    - collapseAll reads from `get().sections` (not DEFAULT_SECTIONS) to iterate topics
    - resetAll does NOT include removedDefaultQuestionIds in its set() call
    - App.tsx `markedTopicIds` useMemo iterates `sections` from `useAppStore((s) => s.sections)` using `section.topics` (V4Section uses `.topics`, not `.items`)
    - App.tsx passes `sections` (from store) as first arg to `buildFlatRows` instead of `DEFAULT_SECTIONS`
  </behavior>
  <action>
    In `src/store/app.ts`, inside the `create(...)` block:

    **5 new actions** (per D-01, D-02, RESEARCH Pattern 1 and 2). Add after `deleteCustomQuestion`:

    `addSection`: `set((s) => ({ sections: [...s.sections, section] }))` — appends the V4Section.

    `removeSection`: `set((s) => ({ sections: s.sections.filter((sec) => sec.id !== sectionId) }))` — user-added only; no guard needed since button only appears when `!section.isDefault`.

    `addTopic`: `set((s) => ({ sections: s.sections.map((sec) => sec.id === sectionId ? { ...sec, topics: [...sec.topics, topic] } : sec) }))`.

    `removeTopic`: `set((s) => ({ sections: s.sections.map((sec) => ({ ...sec, topics: sec.topics.filter((t) => t.id !== topicId) })) }))`.

    `removeDefaultQuestion`: `set((s) => { const next = new Set(s.removedDefaultQuestionIds); next.add(questionId); return { removedDefaultQuestionIds: next }; })` (per RESEARCH Pattern 2, analogous to toggleDifficulty).

    **Fix expandAll** (currently line ~242-249): replace `for (const section of DEFAULT_SECTIONS)` with `for (const section of get().sections)` and change `for (const topic of section.items)` to `for (const topic of section.topics)` (V4Section uses `.topics` not `.items`). Also switch `set({ topicOpen })` to use the local object.

    **Fix collapseAll** (currently line ~252-260): same replacement — `get().sections` and `section.topics`.

    **Subscribe block** (line ~692-704): add `removedDefaultQuestionIds: [...state.removedDefaultQuestionIds]` to the V4Session write literal. This must satisfy the updated `V4Session` type.

    **switchSession** (line ~362-394): add `removedDefaultQuestionIds: new Set(session?.removedDefaultQuestionIds ?? [])` to the `set()` call inside switchSession.

    **importSession** (line ~599-638): In BOTH the `overwriteActive=true` branch and the `overwriteActive=false` branch, add:
    - `sections: data.sections ?? []`
    - `removedDefaultQuestionIds: new Set(data.removedDefaultQuestionIds ?? [])`

    `ImportResult` may need these two fields added — check `src/utils/yamlImport.ts` `ImportResult` interface and add `sections?: V4Section[]` and `removedDefaultQuestionIds?: string[]` optional fields there. These will be populated by Plan 03 (YAML import) but must be present for type safety now.

    **resetAll**: confirm `removedDefaultQuestionIds` is NOT in the `set({...})` call (per key-constraints and Open Question 1 resolution from RESEARCH.md). Add a code comment: `// removedDefaultQuestionIds intentionally not cleared — bank shape is separate from scoring state (D-01)`.

    In `src/app/App.tsx`:

    Add `const sections = useAppStore((s) => s.sections);` selector alongside the other selectors.

    Change `const markedTopicIds = useMemo(...)` to iterate `sections` instead of `DEFAULT_SECTIONS`, and use `section.topics` instead of `section.items`. Keep `topic.questions.some(...)` logic identical.

    Change `const rows = buildFlatRows(DEFAULT_SECTIONS, ...)` to `buildFlatRows(sections, ...)`.

    The `DEFAULT_SECTIONS` import from `'../data/bank/index.js'` can now be removed if it is no longer referenced (check for other usages first — if only `buildFlatRows` and `markedTopicIds` used it, remove the import).

    IMPORTANT: `buildFlatRows` currently expects `readonly Section[]` as first arg (legacy bank type). Plan 02 will update that signature to accept `V4Section[]`. For now, cast as `buildFlatRows(sections as any, ...)` with a `// TODO(14-02): remove cast after buildFlatRows is updated` comment, or leave DEFAULT_SECTIONS temporarily and note the build error for Plan 02. The cleanest approach: keep the cast here and remove it in Plan 02.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <done>Build passes. App.tsx no longer imports DEFAULT_SECTIONS for buildFlatRows/markedTopicIds. Subscribe block includes removedDefaultQuestionIds. switchSession and importSession include the field. expandAll/collapseAll iterate get().sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store action input | sectionId / topicId / questionId passed from click handlers — no user-typed text at this layer |
| storage read → Set hydration | `session.removedDefaultQuestionIds` array from chrome.storage.local converted to Set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | removeSection/removeTopic actions | accept | Actions filter by id; no PII; malicious input only affects local user's own session data |
| T-14-02 | Tampering | removedDefaultQuestionIds Set hydration | mitigate | `new Set(session?.removedDefaultQuestionIds ?? [])` — nullish coalescence prevents crash on missing field; array spread prevents prototype pollution |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `npm test` passes — existing app.test.ts suite stays green
3. `grep -n "removedDefaultQuestionIds" src/store/app.ts` shows entries in: DEFAULT_STATE, AppState interface, AppActions interface, subscribe block, switchSession, importSession, resetAll comment
4. `grep -n "removedDefaultQuestionIds" src/storage/types.ts` shows entry in V4SessionSchema and createDefaultV4Session
5. `grep -n "DEFAULT_SECTIONS" src/app/App.tsx` returns empty (import removed) or only non-buildFlatRows usage
</verification>

<success_criteria>
- V4SessionSchema has `removedDefaultQuestionIds` optional field with array default
- AppState has `removedDefaultQuestionIds: Set<string>`
- DEFAULT_STATE has `removedDefaultQuestionIds: new Set()`
- 5 new actions implemented and exported from AppActions
- Subscribe block persists removedDefaultQuestionIds as spread array
- switchSession and importSession hydrate removedDefaultQuestionIds as Set
- expandAll/collapseAll iterate state.sections with .topics accessor
- App.tsx passes store sections to buildFlatRows (with temporary cast if needed)
- resetAll does not clear removedDefaultQuestionIds
- npm run build and npm test pass
</success_criteria>

<output>
Create `.planning/phases/14-editable-bank-yaml-schema-expansion/14-01-SUMMARY.md` when done
</output>
