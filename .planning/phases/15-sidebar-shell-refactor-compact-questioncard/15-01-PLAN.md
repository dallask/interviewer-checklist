---
phase: 15-sidebar-shell-refactor-compact-questioncard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SidebarHeader.tsx
  - src/components/Sidebar.tsx
autonomous: true
requirements:
  - UI-13

must_haves:
  truths:
    - "The sidebar `<aside>` has a sticky top section that does not scroll with filter content"
    - "The sticky header shows a toggle button (closes sidebar) and a candidate-detail button"
    - "The sticky header shows 'Final mark · N/86 topics' with a thin progress bar and a mark badge"
    - "The mark badge uses BAND_COLORS coloring tied to the computed overall mark band"
    - "Moving overflow-y-auto from `<aside>` to an inner div makes sticky positioning work correctly"
  artifacts:
    - path: "src/components/SidebarHeader.tsx"
      provides: "Sticky sidebar header with toggle, candidate button, progress line, and progress bar"
      exports: ["SidebarHeader"]
    - path: "src/components/Sidebar.tsx"
      provides: "Updated sidebar shell with SidebarHeader + inner scrollable div"
      contains: "overflow-y-auto"
  key_links:
    - from: "src/components/SidebarHeader.tsx"
      to: "src/store/app.js"
      via: "useAppStore selectors for sidebarOpen, setSidebarOpen, sections, scores, overrides"
      pattern: "useAppStore"
    - from: "src/components/SidebarHeader.tsx"
      to: "src/scoring/index.js"
      via: "computeOverallMark + getMarkBand for progress display"
      pattern: "computeOverallMark"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/SidebarHeader.tsx"
      via: "import and render as first child before scrollable div"
      pattern: "SidebarHeader"
---

<objective>
Create `SidebarHeader.tsx` — a sticky top section for the sidebar — and refactor `Sidebar.tsx` to use it. The header does not scroll with sidebar content; it permanently displays the sidebar close toggle, a candidate-detail button, the session final mark with topic progress, and a thin progress bar.

Purpose: Delivers UI-13 — users see live session progress without scrolling the sidebar. The "Final mark · N/86 topics" line and progress bar give a persistent session status anchor.

Output: `src/components/SidebarHeader.tsx` (new), `src/components/Sidebar.tsx` (modified layout).
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicMarkDisplay.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/scoring/scoring.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SidebarHeader component (UI-13)</name>
  <files>src/components/SidebarHeader.tsx</files>
  <action>
    Create `src/components/SidebarHeader.tsx` that renders a sticky top header for the sidebar. It is `sticky top-0 z-10` within the sidebar's inner scrollable container so it does not scroll away.

    Props interface: `SidebarHeaderProps` with `onCandidateClick: () => void` — a callback that opens CandidateModal (passed from Sidebar, which owns the CandidateModal ref after this refactor). No other props; all data comes from `useAppStore`.

    Store selectors needed: `sidebarOpen` (boolean), `setSidebarOpen` (action), `sections` (V4Section[]), `scores` (ScoreMap), `overrides` (Record), `customQuestions` (CustomQuestion[]).

    Progress computation: use `computeOverallMark` and `computeTopicMark` from `../scoring/index.js`. For each topic across all sections, call `computeTopicMark(topic, scores, overrides[topic.id] ?? null)` to get `TopicResult`. Pass the full `TopicResult[]` to `computeOverallMark` to get `{ mark, band, scoredTopics, totalTopics }`. Note: `customQuestions` filtered by `topicId` must be appended to each topic's `questions` array before passing to `computeTopicMark` — this matches the pattern in `TopicMarkDisplay.tsx`.

    Band colors: declare a static `BAND_COLORS` record with complete string literals (never dynamic construction):
    - `none: 'text-gray-400 dark:text-gray-500'`
    - `low: 'text-red-500 dark:text-red-400'`
    - `mid: 'text-yellow-500 dark:text-yellow-400'`
    - `good: 'text-green-600 dark:text-green-400'`
    - `high: 'text-emerald-600 dark:text-emerald-400'`

    Progress percent: `totalTopics > 0 ? Math.round((scoredTopics / totalTopics) * 100) : 0`, clamped to [0, 100].

    HTML structure (per UI-SPEC `## Component Inventory > SidebarHeader`):
    - Outer wrapper: `<div className="sticky top-0 z-10 bg-gray-100 dark:bg-gray-800 border-b border-gray-200 dark:border-gray-700 flex flex-col print:hidden">`
    - Row 1 (toggle + candidate): `<div className="flex items-center gap-2 px-3 py-2 min-h-[44px]">`. Toggle button: `type="button"`, `aria-expanded={sidebarOpen}`, `aria-label="Close sidebar"`, `className="p-2 min-h-[44px] min-w-[44px] text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none rounded"`, `onClick={() => setSidebarOpen(false)}`, content `☰`. Candidate button: `type="button"`, `id="open-candidate-modal"`, `aria-label="Candidate details"`, `title="Candidate details"`, `className="ml-auto p-2 min-h-[44px] min-w-[44px] text-gray-600 dark:text-gray-300 hover:text-gray-900 dark:hover:text-gray-100 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none rounded"`, `onClick={onCandidateClick}`, content `👤`.
    - Row 2 (progress text): `<div aria-label="Session progress" className="px-3 pb-1 flex items-center justify-between gap-2">`. Left span: `<span className="text-xs font-normal text-gray-500 dark:text-gray-400 truncate">Final mark · {scoredTopics}/{totalTopics} topics</span>`. Right badge: `<span className={\`text-xs font-semibold tabular-nums \${BAND_COLORS[band]}\`}>{mark !== null ? mark.toFixed(1) : '—'}</span>`.
    - Row 3 (progress bar): `<div className="h-1 bg-gray-200 dark:bg-gray-700 mx-3 mb-2 rounded-full"><div className="h-1 bg-blue-500 rounded-full transition-all" style={{ width: \`\${progressPercent}%\` }} /></div>`.

    Import `computeOverallMark`, `computeTopicMark` from `'../scoring/index.js'` and `type { MarkBand }` from `'../scoring/index.js'`. Import `useAppStore` from `'../store/app.js'`. No import of `CandidateModal` here — that moves to Sidebar.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>File exists at `src/components/SidebarHeader.tsx`, TypeScript compiles without errors referencing this file, and the component renders the sticky header structure per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Sidebar.tsx — sticky header + inner scrollable div + CandidateModal ownership (UI-13)</name>
  <files>src/components/Sidebar.tsx</files>
  <action>
    Refactor `src/components/Sidebar.tsx` to implement the new sidebar shell structure per UI-SPEC `## Layout Refactor Notes`. This is a structural layout change — not a visual change to SidebarGroup contents.

    Key constraint (UI-SPEC): `overflow-y-auto` must be on the inner scrollable `<div>`, NOT on the `<aside>`. Currently `overflow-y-auto` is on `<aside>` — moving it is what enables `sticky top-0` on `SidebarHeader` to work. Remove `overflow-y-auto` from the `<aside>` className.

    Add `useRef` import from `react`. Declare `const candidateDialogRef = useRef<HTMLDialogElement>(null)` inside the component.

    Import `CandidateModal` from `'./CandidateModal.js'` and import `SidebarHeader` from `'./SidebarHeader.js'`.

    Remove `CandidateModal` from `ActionsGroup` — this refactor relocates its ownership. In this task: add the `CandidateModal` render to `Sidebar`, mount it as `<CandidateModal dialogRef={candidateDialogRef} />` as a sibling after `</aside>` (outside the aside, as a fragment sibling). Pass `onCandidateClick={() => candidateDialogRef.current?.showModal()}` to `SidebarHeader`.

    Remove the `CandidateModal` import + render + `candidateDialogRef` declaration from `ActionsGroup.tsx` in the same task (they are now duplicated — keep the one in Sidebar, remove from ActionsGroup to avoid two modals). Also remove the `id="open-candidate-modal"` button from ActionsGroup since `SidebarHeader` now renders that button with the same `id`.

    New `Sidebar` JSX structure:
    ```
    <>
      <aside aria-label="Filters" className={`w-[280px] flex-shrink-0 bg-gray-100 dark:bg-gray-800 flex flex-col transition-transform duration-200 ease-in-out motion-reduce:transition-none fixed inset-y-0 left-0 z-50 print:hidden ${sidebarOpen ? 'translate-x-0' : '-translate-x-full'}`}>
        <SidebarHeader onCandidateClick={() => candidateDialogRef.current?.showModal()} />
        <div className="flex-1 overflow-y-auto flex flex-col">
          <SidebarGroup groupId="search" ...>
          <SidebarGroup groupId="difficulty" ...>
          <SidebarGroup groupId="sections" ...>
          <SidebarGroup groupId="actions" ...>
        </div>
        <SidebarFooter />
      </aside>
      <CandidateModal dialogRef={candidateDialogRef} />
    </>
    ```

    Also remove the candidate-related button from `ActionsGroup.tsx` (the `id="open-candidate-modal"` button and the `candidateDialogRef`/`CandidateModal` render). The `CandidateModal.tsx` `handleClose` function references `document.getElementById('open-candidate-modal')?.focus()` for focus return — this still works because `SidebarHeader` now renders the button with that ID.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Sidebar.tsx renders SidebarHeader as first child, inner div has overflow-y-auto, CandidateModal is mounted in Sidebar (not ActionsGroup), TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → SidebarHeader | Scores and overrides from store are already validated upstream; computed mark is display-only, no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; no slopcheck needed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Sidebar renders with `SidebarHeader` as first child inside `<aside>` before the scrollable inner div
3. `overflow-y-auto` is on the inner div, not on `<aside>`
4. `CandidateModal` is rendered in `Sidebar`, not in `ActionsGroup`
5. The `id="open-candidate-modal"` button exists in `SidebarHeader` (not ActionsGroup)
</verification>

<success_criteria>
- `src/components/SidebarHeader.tsx` exists and exports `SidebarHeader`
- `SidebarHeader` shows sticky header with close toggle, candidate button, progress line, and progress bar
- `Sidebar.tsx` inner div has `overflow-y-auto`; `<aside>` does not
- `CandidateModal` removed from `ActionsGroup`, mounted in `Sidebar`
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-01-SUMMARY.md` when done
</output>
