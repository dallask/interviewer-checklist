---
phase: 15-sidebar-shell-refactor-compact-questioncard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AboutModal.tsx
  - src/components/SidebarFooter.tsx
autonomous: true
requirements:
  - UI-14
  - UI-15

must_haves:
  truths:
    - "SidebarFooter shows 'Developed by Ievgen Kyvgyla' with a clickable kivgila.pro link"
    - "SidebarFooter shows an 'About' button that opens AboutModal"
    - "AboutModal is a native <dialog> matching the CandidateModal pattern (showModal, Esc close, backdrop close)"
    - "AboutModal shows app name, version from manifest, description, credits, and a Close button"
    - "AboutModal is accessible: aria-labelledby, focus trap, focus return on close"
  artifacts:
    - path: "src/components/AboutModal.tsx"
      provides: "About dialog with app name, version, description, credits, close button"
      exports: ["AboutModal"]
    - path: "src/components/SidebarFooter.tsx"
      provides: "Extended footer with credit lockup and About button"
      contains: "Ievgen Kyvgyla"
  key_links:
    - from: "src/components/SidebarFooter.tsx"
      to: "src/components/AboutModal.tsx"
      via: "aboutDialogRef passed as prop to AboutModal; button calls aboutDialogRef.current?.showModal()"
      pattern: "aboutDialogRef"
    - from: "src/components/AboutModal.tsx"
      to: "chrome.runtime.getManifest"
      via: "version from chrome.runtime.getManifest().version"
      pattern: "getManifest"
---

<objective>
Extend `SidebarFooter.tsx` with a credit lockup and About button (UI-14), and create `AboutModal.tsx` (UI-15) — a native `<dialog>` modal containing application name, version, description, links, and credits. The modal follows the existing `CandidateModal` pattern exactly.

Purpose: Delivers UI-14 + UI-15 — users can see app credits and find the developer link from the sidebar footer without scrolling. The About modal provides version info and credits in a focused modal overlay.

Output: `src/components/AboutModal.tsx` (new), `src/components/SidebarFooter.tsx` (modified).
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarFooter.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/CandidateModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutModal component (UI-15)</name>
  <files>src/components/AboutModal.tsx</files>
  <action>
    Create `src/components/AboutModal.tsx`. Follow the `CandidateModal.tsx` pattern exactly for dialog mounting, focus trap, focus restore, Esc close, and backdrop click close.

    Props interface: `AboutModalProps` with `dialogRef: RefObject<HTMLDialogElement | null>`.

    Inside the component:
    - Read `const { version } = chrome.runtime.getManifest()` synchronously (same as SidebarFooter).
    - Set up focus trap using `useEffect` + `dialogEl.addEventListener('keydown', handleKeyDown)` — Tab/Shift+Tab cycle within modal. Use WR-02 guard: `if (focusable.length === 0) return`.
    - Focus return on close: `dialogEl.addEventListener('close', handleClose)` where `handleClose` calls `document.querySelector('[data-about-trigger]')?.focus()` — the About button in SidebarFooter will get `data-about-trigger` attribute.
    - Backdrop click close: `onClick` on the `<dialog>` itself — `if (e.target === dialogRef.current) dialogRef.current.close()` — identical to the SESS-05 pattern in SessionSwitcherModal.

    Dialog element: `<dialog ref={dialogRef} aria-labelledby="about-modal-title" className="fixed inset-0 m-auto w-full max-w-lg bg-white dark:bg-gray-900 rounded-lg shadow-xl border border-gray-200 dark:border-gray-700 p-6" onClick={handleBackdropClick}>` — do not pass `open` prop, always use `showModal()` imperatively (T-05-03-04 note from CandidateModal).

    Content structure per UI-SPEC `## Component Inventory > AboutModal`:
    - Title: `<h2 id="about-modal-title" className="text-base font-semibold text-gray-900 dark:text-gray-100 mb-4">Interviewer Checklist</h2>`
    - Version: `<p className="text-sm font-normal text-gray-500 dark:text-gray-400 mb-2">Version {version}</p>`
    - Description: `<p className="text-sm font-normal text-gray-700 dark:text-gray-300 mb-4">A browser-based weighted scoring session for technical interviews.</p>`
    - Credits section: `<div className="mb-4"><p className="text-sm font-normal text-gray-700 dark:text-gray-300">Developed by <a href="https://kivgila.pro" target="_blank" rel="noopener noreferrer" className="text-blue-600 dark:text-blue-400 underline hover:text-blue-800 dark:hover:text-blue-300 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none">Ievgen Kyvgyla</a></p></div>`
    - Close button row: `<div className="flex justify-end"><button type="button" aria-label="Close about modal" onClick={() => dialogRef.current?.close()} className="text-sm font-normal px-4 py-2 text-gray-700 dark:text-gray-300 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none">Close</button></div>`

    Imports: `useEffect` from `'react'`, `type { RefObject }` from `'react'`.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>`src/components/AboutModal.tsx` exists, exports `AboutModal`, TypeScript compiles without errors. Dialog follows CandidateModal pattern (showModal, focus trap, backdrop close, Esc close).</done>
</task>

<task type="auto">
  <name>Task 2: Extend SidebarFooter with credit lockup and About button (UI-14)</name>
  <files>src/components/SidebarFooter.tsx</files>
  <action>
    Extend `src/components/SidebarFooter.tsx` to add the credit lockup and About button above the existing version/changelog row, and mount `AboutModal`.

    Add `useRef` import from `'react'`. Import `AboutModal` from `'./AboutModal.js'`. Declare `const aboutDialogRef = useRef<HTMLDialogElement>(null)`.

    Insert a new credit row above the existing `<div className="px-3 py-2 ...">` version row:
    ```
    <div className="px-3 pt-2 pb-0 text-xs font-normal text-gray-400 dark:text-gray-500 flex items-center gap-1 flex-wrap">
      <span>Developed by </span>
      <a
        href="https://kivgila.pro"
        target="_blank"
        rel="noopener noreferrer"
        className="underline hover:text-gray-600 dark:hover:text-gray-300 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none"
      >
        Ievgen Kyvgyla
      </a>
      <button
        type="button"
        data-about-trigger
        onClick={() => aboutDialogRef.current?.showModal()}
        className="ml-1 underline text-xs font-normal text-gray-400 dark:text-gray-500 hover:text-gray-600 dark:hover:text-gray-300 focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none"
      >
        About
      </button>
    </div>
    ```

    Keep the existing version row and ChangelogViewer unchanged.

    Mount `AboutModal` inside the outer `<div className="mt-auto border-t ...">` wrapper, after the content divs:
    ```
    <AboutModal dialogRef={aboutDialogRef} />
    ```

    The `data-about-trigger` attribute on the About button is used by `AboutModal`'s focus-return handler (`document.querySelector('[data-about-trigger]')?.focus()`).

    Copy (do not extend) credit text per UI-SPEC Copywriting Contract: link text is `"kivgila.pro"` but the `href` is `"https://kivgila.pro"` — the visible text shown on the link element should be `Ievgen Kyvgyla` with full URL as href, matching the spec literal `"Developed by Ievgen Kyvgyla, https://kivgila.pro"`. Preserve the full name as the link text per UI-SPEC credit lockup.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>SidebarFooter shows credit text "Developed by Ievgen Kyvgyla" with link and "About" button. AboutModal is mounted in SidebarFooter. TypeScript compiles without errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SidebarFooter → external link | `href="https://kivgila.pro"` opens in new tab via `target="_blank" rel="noopener noreferrer"` — prevents tab-napping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Information Disclosure | AboutModal credits link | mitigate | `rel="noopener noreferrer"` on all `target="_blank"` links prevents opener access |
| T-15-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `AboutModal.tsx` exists, dialog uses `showModal()` imperatively
3. `SidebarFooter` renders "Developed by Ievgen Kyvgyla" with link and "About" button
4. All `target="_blank"` links have `rel="noopener noreferrer"`
5. About button `data-about-trigger` attribute is present for focus-return
</verification>

<success_criteria>
- `src/components/AboutModal.tsx` exists, follows CandidateModal dialog pattern
- `SidebarFooter.tsx` renders credit lockup above the version row
- Clicking "About" triggers `aboutDialogRef.current?.showModal()`
- TypeScript compiles clean
</success_criteria>

<output>
Create `.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-02-SUMMARY.md` when done
</output>
