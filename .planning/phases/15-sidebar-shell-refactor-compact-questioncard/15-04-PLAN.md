---
phase: 15-sidebar-shell-refactor-compact-questioncard
plan: 04
type: execute
wave: 2
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - src/components/SidebarHeader.test.tsx
  - src/components/AboutModal.test.tsx
  - src/components/SidebarFooter.test.tsx
  - src/components/Sidebar.test.tsx
  - src/components/QuestionCard.test.tsx
autonomous: true
requirements:
  - UI-13
  - UI-14
  - UI-15
  - SCORE-08

must_haves:
  truths:
    - "SidebarHeader test suite covers: sticky header renders, progress text, mark badge, toggle button, candidate button"
    - "AboutModal test suite covers: renders dialog, shows version, shows credits, close button, backdrop click, Esc close"
    - "SidebarFooter tests updated to cover new credit lockup and About button"
    - "Sidebar tests updated to reflect SidebarHeader presence and CandidateModal moved here"
    - "QuestionCard tests updated: score dropdown replaces slider, note icon replaces text toggle button, delete button still present"
    - "All existing passing tests continue to pass — no regressions"
  artifacts:
    - path: "src/components/SidebarHeader.test.tsx"
      provides: "Test suite for SidebarHeader sticky header, progress display, buttons"
    - path: "src/components/AboutModal.test.tsx"
      provides: "Test suite for AboutModal dialog behavior"
    - path: "src/components/SidebarFooter.test.tsx"
      provides: "Updated tests for credit lockup and About button"
    - path: "src/components/Sidebar.test.tsx"
      provides: "Updated tests for SidebarHeader presence"
    - path: "src/components/QuestionCard.test.tsx"
      provides: "Updated tests for dropdown, note icon, compact layout"
  key_links:
    - from: "src/components/SidebarHeader.test.tsx"
      to: "src/store/app.js"
      via: "vi.mock('../store/app.js') with useAppStore mock"
      pattern: "vi.mock"
    - from: "src/components/QuestionCard.test.tsx"
      to: "src/components/QuestionCard.tsx"
      via: "Tests now query combobox role (select) instead of slider role"
      pattern: "getByRole.*combobox"
---

<objective>
Write and update test suites for all Phase 15 components. `SidebarHeader.test.tsx` and `AboutModal.test.tsx` are new; `SidebarFooter.test.tsx`, `Sidebar.test.tsx`, and `QuestionCard.test.tsx` are updated to match the Phase 15 implementations. All existing tests must continue to pass.

Purpose: Ensures Phase 15 implementations are verifiable and the compact card + sidebar shell are tested at the unit level. Protects against regressions in the components modified this phase.

Output: 2 new test files + 3 updated test files.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-UI-SPEC.md
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarHeader.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/AboutModal.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarFooter.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarFooter.test.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.test.tsx
@/Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SidebarHeader.test.tsx and AboutModal.test.tsx</name>
  <files>src/components/SidebarHeader.test.tsx, src/components/AboutModal.test.tsx</files>
  <action>
    Create two new test files.

    **`src/components/SidebarHeader.test.tsx`**

    Mock `../store/app.js` with `vi.mock`. Mock `../scoring/index.js` — mock `computeOverallMark` to return `{ mark: null, band: 'none', scoredTopics: 0, totalTopics: 0 }` by default. Mock `computeTopicMark` to return `{ mark: null, band: 'none', scoredCount: 0, totalCount: 0 }`.

    Use `chrome.runtime.getManifest.mockReturnValue({ name: 'Interviewer Checklist', manifest_version: 3, version: '1.0.0' })` in `beforeEach` (needed if SidebarHeader transitively uses it — if not needed, omit).

    Default store state for `makeState()`:
    ```
    {
      sidebarOpen: true,
      setSidebarOpen: vi.fn(),
      sections: [],
      scores: {},
      overrides: {},
      customQuestions: [],
    }
    ```

    Tests:
    - `renders without crashing` — renders `<SidebarHeader onCandidateClick={vi.fn()} />` without throwing
    - `renders toggle button with aria-label "Close sidebar"` — `screen.getByRole('button', { name: 'Close sidebar' })`
    - `toggle button has aria-expanded=true when sidebarOpen=true` — checks `aria-expanded` attribute
    - `toggle button calls setSidebarOpen(false) when clicked` — `fireEvent.click(toggleBtn)`, check `setSidebarOpen` called with `false`
    - `renders candidate button with aria-label "Candidate details"` — `screen.getByRole('button', { name: 'Candidate details' })`
    - `candidate button calls onCandidateClick on click` — fires click, checks callback was called
    - `renders "Final mark" progress text` — `screen.getByText(/Final mark/)`
    - `mark badge shows "—" when no topics scored` — `screen.getByText('—')`
    - `renders progress bar element` — `document.querySelector('.h-1.bg-blue-500')` or find by role/structure

    **`src/components/AboutModal.test.tsx`**

    Mock `../store/app.js` is not needed (AboutModal has no store access). Mock `chrome.runtime.getManifest` via `vitest-chrome`:
    ```
    import { chrome } from 'vitest-chrome';
    chrome.runtime.getManifest.mockReturnValue({ name: 'Interviewer Checklist', manifest_version: 3, version: '1.1.0' });
    ```

    Tests use a `dialogRef` created with `React.createRef<HTMLDialogElement>()`.

    Tests:
    - `renders without crashing` — renders `<AboutModal dialogRef={ref} />`
    - `dialog has aria-labelledby="about-modal-title"` — check attribute on dialog element
    - `shows "Interviewer Checklist" as title` — `screen.getByRole('heading', { name: 'Interviewer Checklist' })`
    - `shows version from manifest` — `screen.getByText(/1\.1\.0/)`
    - `shows credits "Developed by"` — `screen.getByText(/Developed by/)`
    - `shows link to kivgila.pro` — `screen.getByRole('link', { name: /Ievgen Kyvgyla/ })` has `href="https://kivgila.pro"`
    - `kivgila.pro link has rel="noopener noreferrer"` — check attribute
    - `Close button has aria-label "Close about modal"` — `screen.getByRole('button', { name: 'Close about modal' })`
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/components/SidebarHeader.test.tsx src/components/AboutModal.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and all tests pass. Zero failing tests in SidebarHeader.test.tsx and AboutModal.test.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Update SidebarFooter.test.tsx, Sidebar.test.tsx, and QuestionCard.test.tsx</name>
  <files>src/components/SidebarFooter.test.tsx, src/components/Sidebar.test.tsx, src/components/QuestionCard.test.tsx</files>
  <action>
    Update three existing test files to match Phase 15 changes.

    **`src/components/SidebarFooter.test.tsx`**

    Read the existing file first. Add to the existing `SidebarFooter` describe block:
    - `renders "Developed by" credit text` — `getByText(/Developed by/)`
    - `renders Ievgen Kyvgyla link with href="https://kivgila.pro"` — find link by accessible name containing `Ievgen Kyvgyla`, check href
    - `Ievgen Kyvgyla link has rel="noopener noreferrer"` — check attribute
    - `renders About button` — `getByRole('button', { name: 'About' })`
    - `About button has data-about-trigger attribute` — check attribute on the About button
    Keep all existing tests unchanged — they still test the version line and What's new toggle.

    **`src/components/Sidebar.test.tsx`**

    Read the existing file. Update `makeState()` to include the fields `SidebarHeader` needs: `sections: []`, `scores: {}`, `overrides: {}`, `customQuestions: []`, `setSidebarOpen: vi.fn()`. These may already be present — add only what is missing. Add mock for `../scoring/index.js` (mock `computeOverallMark` → `{ mark: null, band: 'none', scoredTopics: 0, totalTopics: 0 }`, mock `computeTopicMark` → `{ mark: null, band: 'none', scoredCount: 0, totalCount: 0 }`).

    Add tests:
    - `renders SidebarHeader inside aside` — check that a button with `aria-label="Close sidebar"` is present (rendered by SidebarHeader)
    - `inner scrollable div has overflow-y-auto class` — `aside.querySelector('.overflow-y-auto')` is not null
    Keep all 5 existing tests unchanged.

    **`src/components/QuestionCard.test.tsx`**

    Read the existing file. The existing tests query for `role: 'slider'` and test the slider's `aria-valuenow`, `min`, `max`, `step`, and `"— / 10"` / `"0 / 10"` display text — these are all obsolete after the compact redesign (slider replaced by `<select>` dropdown). Also the notes toggle button label changed from `"Add notes"` / `"Hide notes"` to a note icon button with `aria-label="Toggle note for {question}"`.

    Strategy: remove the obsolete slider tests and notes-text-label tests, replace with compact-card equivalents. Keep the custom badge, delete button, and `questionId` derivation tests since those remain valid.

    Obsolete tests to remove (they will fail after the rewrite):
    - `renders a range input with aria-label set to the question text`
    - `range input has min=0, max=10, step=1`
    - `has aria-valuenow=0 when score is null (unscored)`
    - `has aria-valuemin=0 and aria-valuemax=10`
    - `displays "— / 10" when score is null`
    - `displays "0 / 10" when score is 0`
    - `displays "8 / 10" when score is 8`
    - `calls setScore with questionId and numeric value on slider change`
    - `constructs questionId as topicId-qIndex (V4 format)` — keep this but update to use select/combobox
    - `renders a notes toggle button with aria-expanded=false by default` — replace (label changed)
    - `notes toggle button has aria-controls pointing to notes textarea id` — replace (button aria-label changed)
    - `notes toggle changes label to "Hide notes" when clicked` — remove (button no longer changes label)
    - `notes toggle button has aria-expanded=true after click` — replace

    New tests to add (SCORE-08 compact card behavior):

    Score dropdown tests:
    - `renders a score select (combobox) with aria-label "{question} score"` — `screen.getByRole('combobox', { name: /What is JSX\? score/ })`
    - `score select shows "Skip" as default selected option when score is null` — check selected value is `'skip'`
    - `score select calls setScore with null when "Skip" is selected` — `fireEvent.change(select, { target: { value: 'skip' } })`, check `setScore('react-q0', null)`
    - `score select calls setScore with 7 when "7" is selected` — `fireEvent.change(select, { target: { value: '7' } })`, check `setScore('react-q0', 7)`
    - `score select shows value "8" when score is 8 in store` — mock `scores: { 'react-q0': 8 }`, check select value
    - `score select has min-w-[52px] class` — class check
    - `constructs questionId as topicId-qIndex (V4 format) — select aria-label uses it` — render with `topicId: 'twig', index: 4`, check select aria-label contains correct text

    Note icon button tests:
    - `renders note icon button with aria-label "Toggle note for {question}"` — `screen.getByRole('button', { name: /Toggle note for What is JSX\?/ })`
    - `note icon button has aria-expanded=false by default` — attribute check
    - `note icon button has aria-controls="notes-react-q0"` — attribute check
    - `note icon button aria-expanded=true after click` — click, check attribute
    - `clicking note icon button shows the textarea` — click, textarea no longer has `hidden` attr

    Keep existing tests:
    - `does NOT render "custom" badge for regular questions`
    - `renders "custom" badge for custom questions`
    - `custom badge has correct classes including purple colors`
    - `does NOT render delete button for regular questions`
    - `renders delete button with aria-label "Delete custom question" for custom questions`
    - `delete button calls deleteCustomQuestion with customId on click`
    Also update `mockUseAppStore` default state to add `removeDefaultQuestion: vi.fn()` if not already present. Update `makeState` to include `removeDefaultQuestion`.

    The `notes textarea has aria-label "Notes for {question.q}"`, `notes textarea has id notes-{questionId}`, `notes textarea has resize-y class`, `notes textarea has min-h-[80px] class` tests — update: keep aria-label and id tests (unchanged in new layout), update `min-h-[80px]` to `min-h-[64px]` (compact card uses shorter textarea per UI-SPEC).
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx vitest run src/components/SidebarFooter.test.tsx src/components/Sidebar.test.tsx src/components/QuestionCard.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>All three updated test files pass. No regressions in Sidebar.test.tsx, SidebarFooter.test.tsx, or QuestionCard.test.tsx. Zero failing tests across the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test mocks → component under test | Mocks provide controlled store/API state; no trust concern in test environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. `npx vitest run` passes with zero failing tests
2. All 5 test files (2 new + 3 updated) are present
3. `SidebarHeader.test.tsx` covers toggle, candidate button, progress display
4. `AboutModal.test.tsx` covers version, credits, close, link attributes
5. `QuestionCard.test.tsx` no longer tests slider (removed) — tests dropdown, note icon
6. No test file references `role: 'slider'` for QuestionCard
</verification>

<success_criteria>
- `npx vitest run` exits 0 with all tests passing
- 2 new test files: SidebarHeader.test.tsx, AboutModal.test.tsx
- 3 updated test files: SidebarFooter, Sidebar, QuestionCard
- Compact card dropdown and note icon interactions tested
- Sidebar sticky header presence tested
- AboutModal dialog behavior tested
</success_criteria>

<output>
Create `.planning/phases/15-sidebar-shell-refactor-compact-questioncard/15-04-SUMMARY.md` when done
</output>
