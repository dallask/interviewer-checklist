---
phase: 17-difficulty-indicators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/QuestionCard.tsx
  - src/components/QuestionCard.test.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
must_haves:
  truths:
    - "Each QuestionCard outer container has class border-l-4 and the correct difficulty border color class (border-green-500 / border-blue-500 / border-orange-500 / border-pink-500)"
    - "Each QuestionCard screen row contains a badge chip <span> with aria-label='{level} difficulty', uppercase class, and the correct difficulty badge color classes from BADGE_CLASSES"
    - "The badge chip is always rendered — no conditional wrapper — because difficulty is a primary informational signal"
    - "The print-only <div> in QuestionCard also contains the difficulty badge chip span"
    - "All 2007+ existing tests continue to pass after the visual changes"
    - "TypeScript reports 0 errors and Biome reports 0 lint errors"
  artifacts:
    - path: "src/components/QuestionCard.tsx"
      provides: "BORDER_CLASSES and BADGE_CLASSES maps + border-l-4 on outer div + badge chip span in screen row and print row"
      contains: "BORDER_CLASSES"
    - path: "src/components/QuestionCard.test.tsx"
      provides: "Class-presence tests for VIS-01 border (4 per difficulty) and VIS-02 badge chip (4 per difficulty)"
      contains: "border-l-4"
  key_links:
    - from: "src/components/QuestionCard.tsx"
      to: "src/data/bank/types.ts"
      via: "import type { Difficulty }"
      pattern: "import.*Difficulty.*from.*types"
    - from: "BORDER_CLASSES[question.level]"
      to: "outer container <div> className"
      via: "template literal interpolation"
      pattern: "BORDER_CLASSES\\[question\\.level\\]"
    - from: "BADGE_CLASSES[question.level]"
      to: "badge chip <span> className"
      via: "template literal interpolation"
      pattern: "BADGE_CLASSES\\[question\\.level\\]"
---

<objective>
Add colored left border (VIS-01) and difficulty badge chip (VIS-02) to QuestionCard.

Purpose: Each QuestionCard must communicate its difficulty level at a glance. The left border provides a strong color anchor spanning the full card height; the badge chip provides an explicit text label. Both are derived from row.question.level which is already available in the component.

Output:
- QuestionCard.tsx — BORDER_CLASSES and BADGE_CLASSES Record maps; border-l-4 + dynamic color on outer container; difficulty badge chip <span> in screen row and print row
- QuestionCard.test.tsx — 8+ new class-presence test assertions covering all 4 difficulty levels for both VIS-01 and VIS-02
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-difficulty-indicators/17-CONTEXT.md
@.planning/phases/17-difficulty-indicators/17-PATTERNS.md
@.planning/phases/17-difficulty-indicators/17-UI-SPEC.md
@.planning/phases/17-difficulty-indicators/17-VALIDATION.md
@.planning/phases/16-bug-fixes-dark-mode-polish/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add BORDER_CLASSES + BADGE_CLASSES maps and apply visual indicators in QuestionCard.tsx</name>
  <files>src/components/QuestionCard.tsx</files>

  <read_first>
    - src/components/QuestionCard.tsx — read the full file to see current outer container class string (line ~57), the screen row flex layout and existing custom badge chip (lines ~79-90), and the print-only div structure (lines ~122-127)
    - src/components/DifficultyFilter.tsx — read lines 1-30 to see the DOT_CLASSES: Record<Difficulty, string> pattern and the Difficulty import
    - .planning/phases/17-difficulty-indicators/17-PATTERNS.md — Pattern A (color maps), Pattern B (outer container), Pattern C (badge chip), Pattern D (print row badge)
    - .planning/phases/17-difficulty-indicators/17-UI-SPEC.md — "Color Map Implementation Pattern" and "DOM Structure" sections
  </read_first>

  <action>
    1. Add Difficulty type import at the top of QuestionCard.tsx if not already present:
       import type { Difficulty } from '../data/bank/types.js';

    2. After the imports and before the Props interface (or component-level const declarations), add two static Record maps using full string literals — NO template literals (D-06 pattern, Tailwind content scanner requires complete class names):

       BORDER_CLASSES: Record<Difficulty, string> with values:
         novice: 'border-green-500'
         intermediate: 'border-blue-500'
         advanced: 'border-orange-500'
         expert: 'border-pink-500'

       BADGE_CLASSES: Record<Difficulty, string> with values:
         novice: 'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400'
         intermediate: 'bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400'
         advanced: 'bg-orange-100 text-orange-700 dark:bg-orange-900/30 dark:text-orange-400'
         expert: 'bg-pink-100 text-pink-700 dark:bg-pink-900/30 dark:text-pink-400'

       Add a comment above each map: "// Full class strings as static literals so Tailwind's content scanner includes them (D-06)"

    3. Modify the outer container <div> (currently "bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800") to become a template literal that appends "border-l-4 ${BORDER_CLASSES[question.level]}" — per D-03 locked decision: apply to the outer container. The final className string must be a template literal: `bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 border-l-4 ${BORDER_CLASSES[question.level]}`.

    4. In the screen row flex <div> (the one with print:hidden or group class), insert a difficulty badge <span> AFTER the question text <span> and BEFORE the existing custom badge block. The badge must:
       - Use className template literal: `text-xs font-normal px-1.5 py-0.5 rounded uppercase shrink-0 ${BADGE_CLASSES[question.level]}`
       - Include aria-label attribute: aria-label={`${question.level} difficulty`}
       - Render {question.level} as text content (the uppercase CSS class handles visual display — do NOT call .toUpperCase())
       - NOT be wrapped in any conditional — always render (difficulty is always present and always a primary signal per locked decision)

    5. In the print-only <div> (the sibling div with hidden print:flex classes, currently containing question text span and score span), insert the same difficulty badge <span> — identical markup to step 4 — after the question text <span> and before the score span. Per CONTEXT.md locked decision and UI-SPEC "Print Row" section: difficulty context is useful in print output, no print:hidden.

    Do NOT add a dark: variant to BORDER_CLASSES values — per locked decision, green/blue/orange/pink -500 are saturated enough for dark backgrounds without a separate dark: override.
    Do NOT wrap the badge chip in {row.isCustom === ...} or any condition.
    Do NOT use .toUpperCase() anywhere — use the uppercase CSS utility.
  </action>

  <verify>
    <automated>npm test -- --reporter=dot src/components/QuestionCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "BORDER_CLASSES" src/components/QuestionCard.tsx returns 5 (1 declaration + 4 difficulty keys + 1 usage = at minimum 3; actual count includes map keys so expect >= 3)
    - grep -c "BADGE_CLASSES" src/components/QuestionCard.tsx returns >= 3
    - grep "border-l-4" src/components/QuestionCard.tsx shows the class in the outer container className string
    - grep "border-green-500" src/components/QuestionCard.tsx shows the value in BORDER_CLASSES map
    - grep "aria-label" src/components/QuestionCard.tsx shows aria-label on the difficulty badge span
    - grep "uppercase" src/components/QuestionCard.tsx returns >= 1 hit (the badge chip class)
    - grep "shrink-0" src/components/QuestionCard.tsx shows shrink-0 on the difficulty badge (may also exist on custom badge)
    - npx tsc --noEmit exits with code 0 (no type errors — especially important for Record<Difficulty, string> exhaustive coverage)
    - The outer container div has both border-b (existing, for bottom border) and border-l-4 (new, for left border) in its className — these are different edges and do not conflict
    - A badge <span> with aria-label attribute appears in BOTH the screen row and the print row (two occurrences in the file)
  </acceptance_criteria>

  <done>QuestionCard.tsx has BORDER_CLASSES and BADGE_CLASSES maps as static Record literals, outer div className includes border-l-4 and BORDER_CLASSES[question.level], screen row has a difficulty badge chip after question text span, print row has the same badge chip, TypeScript compiles clean, and existing QuestionCard tests still pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add VIS-01 border and VIS-02 badge class-presence tests to QuestionCard.test.tsx</name>
  <files>src/components/QuestionCard.test.tsx</files>

  <read_first>
    - src/components/QuestionCard.test.tsx — read the full file to see: mockRow fixture (level: 'intermediate'), makeState() helper, existing custom badge test at lines ~215-224 (class-presence assertion pattern using badge.className.toContain()), and the render/screen import patterns
    - .planning/phases/17-difficulty-indicators/17-PATTERNS.md — Pattern section for QuestionCard.test.tsx: per-difficulty row fixture pattern, badge class-presence test pattern, border class-presence test pattern
    - .planning/phases/17-difficulty-indicators/17-RESEARCH.md — Pitfall 4 (test assertions: use aria-label not uppercase text), Validation Architecture section (exact test file and command)
  </read_first>

  <action>
    Add a new describe block "difficulty indicators" (or equivalent flat it blocks grouped by a comment) containing the following tests. All tests follow the class-presence assertion pattern from the existing custom badge test.

    VIS-01 Left Border tests (one per difficulty level):
    For each of novice, intermediate, advanced, expert:
    - Create an inline per-difficulty row fixture by spreading mockRow and overriding question.level to the target difficulty ('novice' as const, etc.)
    - Render the component: const { container } = render(<QuestionCard row={...row} />)
    - Target the outer div: const outerDiv = container.firstChild as HTMLElement
    - Assert: expect(outerDiv.className).toContain('border-l-4')
    - Assert the specific border color class: expect(outerDiv.className).toContain('border-green-500') for novice, 'border-blue-500' for intermediate, 'border-orange-500' for advanced, 'border-pink-500' for expert

    VIS-02 Badge Chip tests (one per difficulty level):
    For each of novice, intermediate, advanced, expert:
    - Create inline per-difficulty row fixture (same as above)
    - Render the component
    - Query the badge by aria-label: const badge = screen.getByLabelText('{level} difficulty') — e.g. 'novice difficulty'
    - Assert: expect(badge.className).toContain('uppercase')
    - Assert: expect(badge.className).toContain('shrink-0')
    - Assert the light-mode bg class: e.g. expect(badge.className).toContain('bg-green-100') for novice
    - Assert the light-mode text class: e.g. expect(badge.className).toContain('text-green-700') for novice
    - Assert the dark-mode bg class: e.g. expect(badge.className).toContain('dark:bg-green-900/30') for novice
    - Assert the dark-mode text class: e.g. expect(badge.className).toContain('dark:text-green-400') for novice

    Do NOT use screen.getByText('NOVICE') — the DOM text content is lowercase 'novice'; CSS uppercase is visual-only (Pitfall 4). Use screen.getByLabelText('novice difficulty') instead.

    Do NOT use toHaveClass() if it is not imported — use .className.toContain() which matches the existing badge test pattern.

    If the makeState() helper requires additional fields to avoid test failures, inspect the existing setup and pass the same overrides used in other tests.
  </action>

  <verify>
    <automated>npm test -- --reporter=verbose src/components/QuestionCard.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - npm test -- --reporter=verbose src/components/QuestionCard.test.tsx output shows all new difficulty indicator tests as "pass" (at least 8 new passing tests: 4 border + 4 badge)
    - npm test (full suite) exits with code 0 and reports >= 2007 tests passing (no regressions)
    - npx tsc --noEmit exits with code 0
    - npx biome check src/ exits with code 0
    - grep -c "border-l-4" src/components/QuestionCard.test.tsx returns >= 1 (border test assertions exist)
    - grep -c "getByLabelText" src/components/QuestionCard.test.tsx returns >= 4 (badge tests use aria-label queries for all 4 difficulty levels)
    - No test uses screen.getByText('NOVICE') or screen.getByText('INTERMEDIATE') or similar uppercase text queries (CSS uppercase does not affect DOM text content)
  </acceptance_criteria>

  <done>QuestionCard.test.tsx contains at least 8 new passing test assertions covering all 4 difficulty levels for VIS-01 (border-l-4 + per-color border class on outer container) and VIS-02 (badge chip aria-label, uppercase class, correct light/dark color classes). Full test suite passes with 0 regressions. TypeScript and Biome report 0 errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| question.level → className interpolation | Difficulty value from QuestionRow prop flows into className string via BORDER_CLASSES/BADGE_CLASSES record lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | BORDER_CLASSES/BADGE_CLASSES lookup | accept | `question.level` is typed as `Difficulty` union (`'novice' | 'intermediate' | 'advanced' | 'expert'`) — TypeScript ensures the key is always a valid union member; no user-controlled input path; value rendered as text content, not dangerouslySetInnerHTML |
| T-17-02 | Information Disclosure | badge chip text content | accept | Difficulty is public metadata, not PII; rendering it visually is the explicit feature goal |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — zero install surface |
</threat_model>

<verification>
Phase gate — all must be TRUE before marking Phase 17 complete:

1. npm test — exits 0, >= 2007 tests passing
2. npx tsc --noEmit — exits 0
3. npx biome check src/ — exits 0
4. grep "border-l-4" src/components/QuestionCard.tsx returns a result (VIS-01 implemented)
5. grep "BORDER_CLASSES" src/components/QuestionCard.tsx returns >= 3 results (map declared and used)
6. grep "BADGE_CLASSES" src/components/QuestionCard.tsx returns >= 3 results (map declared and used)
7. grep "aria-label" src/components/QuestionCard.tsx returns >= 2 results (screen row + print row badge)
8. grep "border-l-4" src/components/QuestionCard.test.tsx returns >= 1 result (VIS-01 tested)
9. grep "getByLabelText" src/components/QuestionCard.test.tsx returns >= 4 results (VIS-02 badge chip tested per difficulty)
10. Visual spot-check (human): QuestionCard rows show a colored left border and uppercase badge chip matching the difficulty, consistent with DifficultyFilter dot colors
</verification>

<success_criteria>
- VIS-01: Every QuestionCard outer container has border-l-4 plus the correct difficulty border color class (green-500 / blue-500 / orange-500 / pink-500), matching DifficultyFilter DOT_CLASSES colors
- VIS-02: Every QuestionCard screen row shows a difficulty badge chip with uppercase text (NOVICE / INTERMEDIATE / ADVANCED / EXPERT), correct light and dark mode color classes, and aria-label for accessibility
- Print output: Both the left border (on outer container) and badge chip (explicit duplicate in print div) appear in printed output
- Custom questions: Border and badge apply to custom questions (no conditional guard around difficulty indicators)
- Regression: All 2007+ existing tests pass with zero new failures
- Type safety: TypeScript Record<Difficulty, string> maps provide exhaustive coverage — adding a new Difficulty value would surface a compile error
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| BORDER_CLASSES | const (Record<Difficulty, string>) | src/components/QuestionCard.tsx | Maps difficulty level to border color Tailwind class (border-green-500, border-blue-500, border-orange-500, border-pink-500) |
| BADGE_CLASSES | const (Record<Difficulty, string>) | src/components/QuestionCard.tsx | Maps difficulty level to badge chip Tailwind classes (light + dark mode bundled per key) |
| border-l-4 on outer QuestionCard container | className addition | src/components/QuestionCard.tsx | 4px left border on outer div, colored by BORDER_CLASSES[question.level] |
| difficulty badge chip <span> (screen row) | JSX element | src/components/QuestionCard.tsx | Always-visible badge chip after question text span; uses BADGE_CLASSES[question.level]; includes aria-label |
| difficulty badge chip <span> (print row) | JSX element | src/components/QuestionCard.tsx | Duplicate badge chip in print-only div for printed output |
| VIS-01 border tests (×4) | test assertions | src/components/QuestionCard.test.tsx | border-l-4 + per-difficulty border color class on container.firstChild |
| VIS-02 badge chip tests (×4) | test assertions | src/components/QuestionCard.test.tsx | aria-label queries + class-presence assertions for all 4 difficulty levels |

<output>
Create `.planning/phases/17-difficulty-indicators/17-01-SUMMARY.md` when done.
</output>
