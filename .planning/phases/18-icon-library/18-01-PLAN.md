---
phase: 18-icon-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/App.tsx
  - src/components/SidebarHeader.tsx
  - src/components/Sidebar.tsx
  - src/components/SectionFilter.tsx
  - src/components/ActionsGroup.tsx
autonomous: true
requirements:
  - VIS-03
must_haves:
  truths:
    - "App.tsx sidebar toggle renders Lucide Menu SVG instead of ☰ character"
    - "SidebarHeader.tsx renders Menu (close) and User (candidate) SVG icons"
    - "Sidebar.tsx passes Lucide JSX elements as icon props to all four SidebarGroup instances"
    - "SectionFilter.tsx label span contains ClipboardList SVG instead of 📋 emoji"
    - "ActionsGroup.tsx renders all nine Lucide SVG icons (RefreshCw, Bot, Sun/Moon, ChevronsUpDown, ChevronsLeftRight, Eye, Download, Upload, Trash2)"
    - "lucide-react appears in package.json dependencies (not devDependencies)"
    - "npm test passes with no new failures after this plan's changes"
  artifacts:
    - path: "package.json"
      provides: "lucide-react runtime dependency"
      contains: "lucide-react"
    - path: "src/app/App.tsx"
      provides: "Menu icon import and usage"
      exports: []
    - path: "src/components/ActionsGroup.tsx"
      provides: "Nine Lucide icon imports and usages"
      exports: []
    - path: "src/components/Sidebar.tsx"
      provides: "Four SidebarGroup icon props as ReactNode JSX elements"
      exports: []
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/components/SidebarGroup.tsx"
      via: "icon prop (ReactNode)"
      pattern: "icon=\\{<[A-Z].*className"
---

<objective>
Install the lucide-react package and replace all emoji/special-character icons in the five highest-density files: App.tsx (☰), SidebarHeader.tsx (☰, 👤), Sidebar.tsx (🔍 🎯 📋 ⚡ as icon props), SectionFilter.tsx (📋 in aria-hidden span), and ActionsGroup.tsx (🔄 🤖 ☀/🌙 ↕ ↔ 👁 📥 📤 🗑 — nine icons).

Purpose: Establish the lucide-react dependency and replace the highest-count files (Action-tier icons and the SidebarGroup icon-prop pattern) so that Plan 02 can focus purely on the Inline-X pattern files without package install concerns.

Output: package.json updated with lucide-react in dependencies; five component files with Lucide SVG imports replacing all emoji and special characters.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react and replace icons in App.tsx, SidebarHeader.tsx, SectionFilter.tsx</name>
  <read_first>
    - /Users/dallask/Projects/dallask/interviewer-checklist/package.json — confirm current dependencies before adding lucide-react
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx — read full file to find ☰ at line 96 and surrounding button element
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarHeader.tsx — read full file to find ☰ at line 79 and 👤 at line 89
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionFilter.tsx — read full file to find 📋 at line 44 inside aria-hidden span
  </read_first>
  <action>
    Run `npm install lucide-react` in the project root. Confirm `lucide-react` appears in package.json `dependencies` (not `devDependencies`) after install.

    App.tsx (per D-01, D-02, D-06, D-08):
    - Add named import `import { Menu } from 'lucide-react'` to the import block at the top of the file.
    - Find the button at approximately line 96 (the `aria-label="Open sidebar"` button whose body is the ☰ character).
    - Replace the ☰ text body with `<Menu className="w-5 h-5" aria-hidden="true" />`. Do not change any button attributes (type, aria-label, aria-expanded, onClick, className).

    SidebarHeader.tsx (per D-01, D-02, D-06, D-08):
    - Add named import `import { Menu, User } from 'lucide-react'` to the import block.
    - Find the button at approximately line 79 (the `aria-label="Close sidebar"` button whose body is ☰). Replace body with `<Menu className="w-5 h-5" aria-hidden="true" />`.
    - Find the button at approximately line 89 (the `aria-label="Candidate details"` button whose body is 👤). Replace body with `<User className="w-5 h-5" aria-hidden="true" />`.
    - Do not change any button attributes on either button.

    SectionFilter.tsx (per D-01, D-02, D-06, Pattern C from PATTERNS.md):
    - Add named import `import { ClipboardList } from 'lucide-react'` to the import block.
    - Find the `<span aria-hidden="true" className="mr-1">📋</span>` at approximately line 44.
    - Replace the 📋 text content with `<ClipboardList className="w-4 h-4" />` (NO aria-hidden on the SVG — the span already carries aria-hidden="true", per Pattern C deviation from D-08).
    - Do not change the span's attributes.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test -- --reporter=dot</automated>
  </verify>
  <done>
    - `package.json` `dependencies` block contains `"lucide-react"` key (confirmed by reading the file)
    - `src/app/App.tsx` imports `Menu` from `lucide-react` and the sidebar-open button body is `<Menu className="w-5 h-5" aria-hidden="true" />`
    - `src/components/SidebarHeader.tsx` imports `Menu, User` from `lucide-react`; close-sidebar button body is `<Menu className="w-5 h-5" aria-hidden="true" />` and candidate button body is `<User className="w-5 h-5" aria-hidden="true" />`
    - `src/components/SectionFilter.tsx` imports `ClipboardList` from `lucide-react`; the aria-hidden span body is `<ClipboardList className="w-4 h-4" />` with no aria-hidden on the SVG
    - `npm test` passes (no new failures introduced)
  </done>
  <acceptance_criteria>
    - `grep -n "lucide-react" /Users/dallask/Projects/dallask/interviewer-checklist/package.json` returns a line in the `dependencies` block (not `devDependencies`)
    - `grep -n 'import.*Menu.*lucide-react' /Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx` returns a match
    - `grep -n '☰\|📝\|📋\|👤\|✓\|×' /Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx` returns 0 lines
    - `grep -n '☰\|👤' /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarHeader.tsx` returns 0 lines
    - `grep -n '📋' /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionFilter.tsx` returns 0 lines
    - `npm test -- --reporter=dot` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace icons in Sidebar.tsx (icon prop migration) and ActionsGroup.tsx (nine icons)</name>
  <read_first>
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx — read full file to find the four SidebarGroup calls with icon="🔍", icon="🎯", icon="📋", icon="⚡" at approximately lines 38, 48, 58, 68
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx — read full file to find all nine emoji/special-char icons at approximately lines 163, 173, 183, 192, 201, 211, 231, 240, 250
  </read_first>
  <action>
    Sidebar.tsx (per D-01, D-02, D-06, Pattern D from PATTERNS.md; SidebarGroup already accepts icon?: ReactNode — no prop type change needed):
    - Add named import `import { Search, Target, ClipboardList, Zap } from 'lucide-react'` to the import block.
    - Find the four `<SidebarGroup>` call sites that pass `icon="..."` as a string prop:
      - `icon="🔍"` at line ~38 → change to `icon={<Search className="w-5 h-5" aria-hidden="true" />}`
      - `icon="🎯"` at line ~48 → change to `icon={<Target className="w-5 h-5" aria-hidden="true" />}`
      - `icon="📋"` at line ~58 → change to `icon={<ClipboardList className="w-5 h-5" aria-hidden="true" />}`
      - `icon="⚡"` at line ~68 → change to `icon={<Zap className="w-5 h-5" aria-hidden="true" />}`
    - NOTE: The SidebarGroup renders `<span aria-hidden="true">{icon}</span>` — do NOT also add aria-hidden to the SVG icons passed as props here (the span handles it, per Pattern D and Pitfall 5 in RESEARCH.md).
    - Do not change any other SidebarGroup props.

    ActionsGroup.tsx (per D-01, D-02, D-06, D-07, D-08; Pattern A from PATTERNS.md):
    - Add a single named import statement: `import { RefreshCw, Bot, Sun, Moon, ChevronsUpDown, ChevronsLeftRight, Eye, Download, Upload, Trash2 } from 'lucide-react'`
    - Replace each icon body using the full map from PATTERNS.md Pattern A. Every icon uses `w-5 h-5` (Action tier) and `aria-hidden="true"` directly on the SVG:
      - Line ~163: 🔄 → `<RefreshCw className="w-5 h-5" aria-hidden="true" />`
      - Line ~173: 🤖 → `<Bot className="w-5 h-5" aria-hidden="true" />`
      - Line ~183: conditional `{darkMode ? '☀' : '🌙'}` → `{darkMode ? <Sun className="w-5 h-5" aria-hidden="true" /> : <Moon className="w-5 h-5" aria-hidden="true" />}`
      - Line ~192: ↕ → `<ChevronsUpDown className="w-5 h-5" aria-hidden="true" />`
      - Line ~201: ↔ → `<ChevronsLeftRight className="w-5 h-5" aria-hidden="true" />`
      - Line ~211: 👁 → `<Eye className="w-5 h-5" aria-hidden="true" />`
      - Line ~231: 📥 → `<Download className="w-5 h-5" aria-hidden="true" />`
      - Line ~240: 📤 → `<Upload className="w-5 h-5" aria-hidden="true" />`
      - Line ~250: 🗑 → `<Trash2 className="w-5 h-5" aria-hidden="true" />`
    - Do not change any button attributes; only the icon body text/expression inside each button is replaced.
    - Do NOT add color classes (text-*) to any icon element — icons inherit currentColor from parent button.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit && npm test -- --reporter=dot</automated>
  </verify>
  <done>
    - `src/components/Sidebar.tsx` imports Search, Target, ClipboardList, Zap from lucide-react; all four SidebarGroup calls pass JSX icon props with className="w-5 h-5"; no emoji string icon props remain
    - `src/components/ActionsGroup.tsx` imports all ten named icons from lucide-react; all nine emoji/special chars replaced with Lucide SVGs using w-5 h-5 and aria-hidden="true"
    - `npx tsc --noEmit` exits 0 (validates all Lucide icon names exist in lucide-react v1.21.0)
    - `npm test` passes with no new failures
  </done>
  <acceptance_criteria>
    - `grep -n "icon=\"" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx` returns 0 lines (no string-form icon props remain)
    - `grep -n "🔍\|🎯\|📋\|⚡" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx` returns 0 lines
    - `grep -c "lucide-react" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx` returns at least 1
    - `grep -n "🔄\|🤖\|☀\|🌙\|↕\|↔\|👁\|📥\|📤\|🗑" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx` returns 0 lines
    - `npx tsc --noEmit` exits 0
    - `npm test -- --reporter=dot` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Third-party package (lucide-react) introduced into build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | npm install of lucide-react | accept | Package has 87M+/wk downloads, MIT license, source at github.com/lucide-icons/lucide, no postinstall script — SUS verdict was version-freshness only; LOCKED decision D-01 |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | Package legitimacy audit completed in 18-RESEARCH.md; lucide-react approved as locked decision; no additional checkpoint required |
</threat_model>

<verification>
After both tasks complete:
- `grep -rn "☰\|👤\|📋" /Users/dallask/Projects/dallask/interviewer-checklist/src/app/App.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarHeader.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionFilter.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/Sidebar.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ActionsGroup.tsx` returns 0 lines
- `npx tsc --noEmit` exits 0
- `npm test` passes all tests (no new failures)
</verification>

<success_criteria>
All emoji and special-character icons in the five target files are replaced with Lucide SVG components. The lucide-react package is installed as a runtime dependency. TypeScript compiles without errors (confirming all Lucide icon names are valid in the installed version). The test suite continues to pass with no new failures.
</success_criteria>

<output>
Create `.planning/phases/18-icon-library/18-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan creates no new symbols. It modifies existing JSX in existing files. New import statements introduced:

- `import { Menu } from 'lucide-react'` — in `src/app/App.tsx`
- `import { Menu, User } from 'lucide-react'` — in `src/components/SidebarHeader.tsx`
- `import { ClipboardList } from 'lucide-react'` — in `src/components/SectionFilter.tsx`
- `import { Search, Target, ClipboardList, Zap } from 'lucide-react'` — in `src/components/Sidebar.tsx`
- `import { RefreshCw, Bot, Sun, Moon, ChevronsUpDown, ChevronsLeftRight, Eye, Download, Upload, Trash2 } from 'lucide-react'` — in `src/components/ActionsGroup.tsx`

New runtime dependency: `lucide-react` (in `package.json` `dependencies`)
