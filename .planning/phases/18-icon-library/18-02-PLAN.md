---
phase: 18-icon-library
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/components/QuestionCard.tsx
  - src/components/SearchGroup.tsx
  - src/components/SessionRow.tsx
  - src/components/SectionRow.tsx
  - src/components/TopicRow.tsx
  - src/components/SessionSwitcherModal.tsx
  - src/components/StorageToast.tsx
  - src/components/UndoToast.tsx
  - src/components/UpdateBanner.tsx
  - src/components/TopicMarkDisplay.tsx
  - src/components/MigrationErrorBanner.tsx
autonomous: true
requirements:
  - VIS-03
must_haves:
  truths:
    - "QuestionCard.tsx renders Pencil icon for note toggle and X icon for dismiss; no 📝 or × characters remain"
    - "SessionRow.tsx renders Check in aria-hidden span, Pencil for rename, Copy for duplicate, X for delete; no ✓ ✎ ⧉ × characters remain"
    - "All nine dismiss/remove/clear × buttons in remaining files render X SVG instead of × character"
    - "Every × replacement button has an aria-label attribute on its parent <button>"
    - "npm test passes with no new failures after all replacements in this plan"
  artifacts:
    - path: "src/components/SessionRow.tsx"
      provides: "Check, Pencil, Copy, X Lucide icons in correct patterns"
      exports: []
    - path: "src/components/QuestionCard.tsx"
      provides: "Pencil and X Lucide icons"
      exports: []
  key_links:
    - from: "src/components/SessionRow.tsx"
      to: "SessionRow.test.tsx"
      via: "getByText('✓') test must be updated in Plan 03"
      pattern: "Check.*w-4 h-4"
---

<objective>
Replace all remaining emoji and × characters in the eleven Inline-tier component files: QuestionCard (📝 Pencil, × X), SearchGroup (× X), SessionRow (✓ Check, ✎ Pencil, ⧉ Copy, × X), SectionRow (× X), TopicRow (× X), SessionSwitcherModal (× X), StorageToast (× X), UndoToast (× X), UpdateBanner (× X), TopicMarkDisplay (× X), MigrationErrorBanner (× X).

Purpose: Complete the icon replacement coverage so zero emoji or special characters remain in any UI chrome component. The test fix for SessionRow.test.tsx getByText('✓') is deferred to Plan 03, which runs after this plan. This plan's test run may show exactly two expected failures in SessionRow.test.tsx — those are expected and will be fixed in Plan 03.

Output: All eleven remaining component files updated with named Lucide imports and SVG icon replacements. All × buttons verified to have aria-label on their parent button element.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace icons in QuestionCard, SearchGroup, SectionRow, TopicRow, SessionSwitcherModal</name>
  <read_first>
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.tsx — find 📝 at ~line 130 (button body) and × at ~line 149 (button body); confirm aria-label on each button
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SearchGroup.tsx — find × at ~line 124 (clear search button); confirm aria-label="Clear search"
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionRow.tsx — find × at ~line 39 (remove section button); confirm aria-label template
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicRow.tsx — find × at ~line 86 (remove topic button); confirm aria-label template
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionSwitcherModal.tsx — find × at ~line 100 (close modal button); confirm or add aria-label
  </read_first>
  <action>
    All replacements follow Pattern B (Inline tier, w-4 h-4, aria-hidden="true" on SVG directly in button) from PATTERNS.md.

    QuestionCard.tsx (per D-01, D-02, D-06, D-08, D-10):
    - Add `import { Pencil, X } from 'lucide-react'` to the import block.
    - Line ~130 (note toggle button, aria-label="Toggle note for {question.q}"): replace 📝 with `<Pencil className="w-4 h-4" aria-hidden="true" />`. Confirm the button has aria-label before replacing.
    - Line ~149 (dismiss/remove button): replace × with `<X className="w-4 h-4" aria-hidden="true" />`. If the button lacks aria-label, add `aria-label="Remove question"` (or the existing contextual label if present) per D-10.
    - Do not change any button attributes beyond adding aria-label if missing.

    SearchGroup.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~124 (clear search button, aria-label="Clear search" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    SectionRow.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~39 (remove section button, aria-label="Remove section {label}" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    TopicRow.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~86 (remove topic button, aria-label="Remove topic {name}" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    SessionSwitcherModal.tsx (per D-01, D-02, D-06, D-08, D-10):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~100 (close modal button): replace × with `<X className="w-4 h-4" aria-hidden="true" />`. If the button lacks aria-label, add `aria-label="Close sessions"` per D-10.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test -- --reporter=dot 2>&1 | grep -E "FAIL|PASS|Error" | head -30</automated>
  </verify>
  <done>
    - `src/components/QuestionCard.tsx` imports Pencil and X from lucide-react; no 📝 or × text in JSX; both icon buttons have aria-label
    - `src/components/SearchGroup.tsx` imports X from lucide-react; no × text in JSX
    - `src/components/SectionRow.tsx` imports X from lucide-react; no × text in JSX
    - `src/components/TopicRow.tsx` imports X from lucide-react; no × text in JSX
    - `src/components/SessionSwitcherModal.tsx` imports X from lucide-react; no × text in JSX; button has aria-label
    - No new test failures beyond the expected SessionRow.test.tsx getByText('✓') failures
  </done>
  <acceptance_criteria>
    - `grep -n "📝\|×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SearchGroup.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionRow.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicRow.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionSwitcherModal.tsx` returns 0 lines
    - Each file has exactly one `import.*from 'lucide-react'` line containing the needed icon name(s)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace icons in SessionRow, StorageToast, UndoToast, UpdateBanner, TopicMarkDisplay, MigrationErrorBanner</name>
  <read_first>
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionRow.tsx — find ✓ at ~line 78 (inside aria-hidden span), ✎ at ~line 111, ⧉ at ~line 119, × at ~line 128; note Pattern C applies to ✓ (no aria-hidden on SVG itself)
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/StorageToast.tsx — find × at ~line 30; aria-label="Dismiss storage warning" confirmed
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/UndoToast.tsx — find × at ~line 36; confirm or add aria-label
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/UpdateBanner.tsx — find × at ~line 123; confirm or add aria-label
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicMarkDisplay.tsx — find × at ~line 116; aria-label="Clear override mark for {topic.name}" confirmed
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/MigrationErrorBanner.tsx — find × at ~line 40; aria-label="Dismiss migration error" confirmed
  </read_first>
  <action>
    SessionRow.tsx (per D-01, D-02, D-04, D-06, D-08; Pattern B and Pattern C from PATTERNS.md):
    - Add `import { Check, Pencil, Copy, X } from 'lucide-react'` to the import block.
    - Line ~78 (the ✓ character inside `<span className={checkmarkClass} aria-hidden="true">`):
      Replace ✓ text with `<Check className="w-4 h-4" />`. CRITICAL: do NOT add aria-hidden="true" to the Check SVG — the parent span already carries aria-hidden="true" (Pattern C deviation from D-08). This will cause the two getByText('✓') tests in SessionRow.test.tsx to fail — this is expected and will be fixed in Plan 03.
    - Line ~111 (rename button, aria-label="Rename {session.name}" confirmed): replace ✎ with `<Pencil className="w-4 h-4" aria-hidden="true" />`.
    - Line ~119 (duplicate button, aria-label="Duplicate {session.name}" confirmed): replace ⧉ with `<Copy className="w-4 h-4" aria-hidden="true" />`.
    - Line ~128 (delete button, aria-label="Delete {session.name}" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    StorageToast.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~30 (dismiss button, aria-label="Dismiss storage warning" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    UndoToast.tsx (per D-01, D-02, D-06, D-08, D-10):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~36 (dismiss button): replace × with `<X className="w-4 h-4" aria-hidden="true" />`. If button lacks aria-label, add `aria-label="Dismiss"` per D-10.

    UpdateBanner.tsx (per D-01, D-02, D-06, D-08, D-10):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~123 (dismiss button): replace × with `<X className="w-4 h-4" aria-hidden="true" />`. If button lacks aria-label, add `aria-label="Dismiss update notification"` per D-10.

    TopicMarkDisplay.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~116 (clear override button, aria-label="Clear override mark for {topic.name}" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    MigrationErrorBanner.tsx (per D-01, D-02, D-06, D-08):
    - Add `import { X } from 'lucide-react'` to the import block.
    - Line ~40 (dismiss button, aria-label="Dismiss migration error" confirmed): replace × with `<X className="w-4 h-4" aria-hidden="true" />`.

    After all replacements: run `npx tsc --noEmit`. Expect 0 errors. If TypeScript reports an unknown icon name, verify the spelling against the import name in the other files (e.g., `Check` not `Checkmark`).
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit && npm test -- --reporter=dot 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `src/components/SessionRow.tsx` imports Check, Pencil, Copy, X from lucide-react; no ✓ ✎ ⧉ × text remains; Check is inside aria-hidden span WITHOUT aria-hidden on the SVG itself
    - `src/components/StorageToast.tsx` imports X; no × text in JSX; button has aria-label
    - `src/components/UndoToast.tsx` imports X; no × text in JSX; button has aria-label
    - `src/components/UpdateBanner.tsx` imports X; no × text in JSX; button has aria-label
    - `src/components/TopicMarkDisplay.tsx` imports X; no × text in JSX; button has aria-label
    - `src/components/MigrationErrorBanner.tsx` imports X; no × text in JSX; button has aria-label
    - `npx tsc --noEmit` exits 0
    - `npm test` shows at most two failures (the expected SessionRow.test.tsx getByText('✓') tests); all other tests pass
  </done>
  <acceptance_criteria>
    - `grep -n "✓\|✎\|⧉\|×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionRow.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/StorageToast.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/UndoToast.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/UpdateBanner.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicMarkDisplay.tsx` returns 0 lines
    - `grep -n "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/MigrationErrorBanner.tsx` returns 0 lines
    - `npx tsc --noEmit` exits 0
    - `npm test 2>&1 | grep -c "FAIL"` returns 0 or 1 (at most the SessionRow test file fails; all other files PASS)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| icon prop on button | SVG rendered as React component — no user-controlled markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | ReactNode icon prop in SidebarGroup | accept | Icon values are static JSX expressions from named lucide-react imports — not user input, no XSS surface |
| T-18-03 | Information Disclosure | aria-label text additions | accept | Labels describe the button's function only (e.g. "Dismiss storage warning") — no sensitive data exposed |
</threat_model>

<verification>
After both tasks in this plan:
- `grep -rn "📝\|📋\|👁\|📥\|📤\|🗑\|✓\|✎\|⧉" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ /Users/dallask/Projects/dallask/interviewer-checklist/src/app/` returns 0 lines
- `grep -rn "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/` returns 0 lines
- `grep -rn "☰\|👤\|🔍\|🎯\|⚡\|🔄\|🤖\|☀\|🌙\|↕\|↔" /Users/dallask/Projects/dallask/interviewer-checklist/src/` returns 0 lines
- `npx tsc --noEmit` exits 0
- `npm test` shows at most two failures (SessionRow.test.tsx getByText('✓') — to be fixed in Plan 03)
</verification>

<success_criteria>
All eleven remaining component files have emoji and × characters replaced with Lucide X, Check, Pencil, Copy SVG components. TypeScript compiles clean (0 errors). Every replaced × button has an aria-label attribute. The test suite has at most two expected failures in SessionRow.test.tsx, which are the getByText('✓') tests awaiting Plan 03 fixes.
</success_criteria>

<output>
Create `.planning/phases/18-icon-library/18-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

No new exported symbols created. New import statements introduced per file:

- `import { Pencil, X } from 'lucide-react'` — `src/components/QuestionCard.tsx`
- `import { X } from 'lucide-react'` — `src/components/SearchGroup.tsx`
- `import { Check, Pencil, Copy, X } from 'lucide-react'` — `src/components/SessionRow.tsx`
- `import { X } from 'lucide-react'` — `src/components/SectionRow.tsx`
- `import { X } from 'lucide-react'` — `src/components/TopicRow.tsx`
- `import { X } from 'lucide-react'` — `src/components/SessionSwitcherModal.tsx`
- `import { X } from 'lucide-react'` — `src/components/StorageToast.tsx`
- `import { X } from 'lucide-react'` — `src/components/UndoToast.tsx`
- `import { X } from 'lucide-react'` — `src/components/UpdateBanner.tsx`
- `import { X } from 'lucide-react'` — `src/components/TopicMarkDisplay.tsx`
- `import { X } from 'lucide-react'` — `src/components/MigrationErrorBanner.tsx`
