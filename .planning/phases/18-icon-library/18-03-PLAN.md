---
phase: 18-icon-library
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified:
  - src/components/SessionRow.test.tsx
autonomous: true
requirements:
  - VIS-03
must_haves:
  truths:
    - "SessionRow.test.tsx no longer queries getByText('✓') — tests use DOM queries targeting the aria-hidden span's className"
    - "npm test passes all 2693+ tests with 0 failures"
    - "npx tsc --noEmit exits 0"
    - "npx biome check src/ exits 0"
    - "No emoji or × characters remain in any src/app/ or src/components/ file"
  artifacts:
    - path: "src/components/SessionRow.test.tsx"
      provides: "Fixed checkmark visibility assertions using DOM querySelector instead of getByText"
      exports: []
  key_links:
    - from: "src/components/SessionRow.test.tsx"
      to: "src/components/SessionRow.tsx"
      via: "renders <Check className='w-4 h-4' /> inside aria-hidden span; test must query span by aria-hidden attribute"
      pattern: "querySelector.*aria-hidden"
---

<objective>
Fix the two broken test assertions in SessionRow.test.tsx that previously used getByText('✓') to locate the active-session checkmark span. After Plan 02's replacement of ✓ with the Lucide Check SVG, these queries find no text node and throw. Rewrite them to query the aria-hidden span by DOM attribute and assert its className. Then run all acceptance gates to confirm the full suite is green.

Purpose: Complete the test layer for VIS-03. The icon replacement is complete after Plan 02; this plan restores the test suite to fully passing and runs all three acceptance gates (tests, TypeScript, Biome).

Output: SessionRow.test.tsx with two fixed assertions; full suite green; 0 type errors; 0 lint errors.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-RESEARCH.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/18-icon-library/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix getByText('✓') assertions in SessionRow.test.tsx and run full acceptance gates</name>
  <read_first>
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionRow.test.tsx — read the full file; locate the two getByText('✓') calls at approximately lines 85 and 101; understand the full test context (what render args are passed, what the assertion is checking)
    - /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionRow.tsx — confirm the post-Plan-02 state: the aria-hidden span now wraps `<Check className="w-4 h-4" />` and carries the `checkmarkClass` conditional class; find the exact span structure and the className expression used for active vs inactive states
  </read_first>
  <action>
    The two failing assertions in SessionRow.test.tsx both call `screen.getByText('✓')` to find the checkmark span. After the Check SVG replacement, this text query fails because the span no longer contains a text node.

    Per RESEARCH.md Pitfall 2 and Pattern F from PATTERNS.md, the fix is to query the span by its `aria-hidden="true"` attribute inside the rendered session row element:

    For each of the two failing test cases (approximately lines 85 and 101):
    - Identify what `container` or `document.getElementById` scope is available in that test.
    - Replace the `screen.getByText('✓')` call with a DOM query that finds the `<span aria-hidden="true">` inside the session row. Two valid approaches (choose whichever fits the existing test structure):
      - Pattern A: `document.getElementById('session-row-{sessionId}')?.querySelector('[aria-hidden="true"]')` — where `{sessionId}` is the ID used in the test render (e.g. `session-row-session-1`)
      - Pattern B: use the `container` returned by `render(...)` — `container.querySelector('[aria-hidden="true"]')`
    - After obtaining the checkSpan, replace the old `getByText` assertion with a `className` assertion on the span. The checkmark's visibility is controlled by a class such as `text-blue-600` (visible/active) vs `text-transparent` (hidden/inactive). Assert `checkSpan?.className` contains or does not contain the appropriate class — matching the original intent of each test.
    - If the test used `expect(getByText('✓')).toBeVisible()` or similar, replace with `expect(checkSpan).toBeTruthy()` and the class check.

    After fixing both assertions, run the full acceptance gate sequence:
    1. `npm test` — all 2693+ tests must pass (0 failures)
    2. `npx tsc --noEmit` — must exit 0
    3. `npx biome check src/` — must exit 0

    If biome reports lint issues in any of the files modified across Plans 01, 02, or 03, fix them now (unused imports, formatting). Do not suppress biome warnings with ignore comments.

    Final verification: run `grep -rn "☰\|👤\|📋\|🔍\|🎯\|⚡\|🔄\|🤖\|☀\|🌙\|↕\|↔\|👁\|📥\|📤\|🗑\|📝\|✓\|✎\|⧉" /Users/dallask/Projects/dallask/interviewer-checklist/src/` to confirm 0 emoji remain in source (excluding the `src/data/bank/` directory which is out of scope per D-05).
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test && npx tsc --noEmit && npx biome check src/</automated>
  </verify>
  <done>
    - `npm test` exits 0 with 0 failures across all test files (2693+ tests passing)
    - `npx tsc --noEmit` exits 0
    - `npx biome check src/` exits 0
    - `grep -rn "getByText('✓')" src/components/SessionRow.test.tsx` returns 0 lines
    - `grep -rn "×\|☰\|👤\|📋\|🔍\|🎯\|⚡\|🔄\|🤖\|☀\|🌙\|↕\|↔\|👁\|📥\|📤\|🗑\|📝\|✓\|✎\|⧉" /Users/dallask/Projects/dallask/interviewer-checklist/src/app/ /Users/dallask/Projects/dallask/interviewer-checklist/src/components/` returns 0 lines
  </done>
  <acceptance_criteria>
    - `cd /Users/dallask/Projects/dallask/interviewer-checklist && npm test 2>&1 | grep -E "^Tests"` shows 0 failed tests
    - `npx tsc --noEmit` exits with code 0 (no output = success)
    - `npx biome check src/ 2>&1 | grep -c "error"` returns 0
    - `grep -rn "getByText\('✓'\)" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SessionRow.test.tsx` returns 0 lines
    - `grep -rn "×" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ /Users/dallask/Projects/dallask/interviewer-checklist/src/app/` returns 0 lines (the × multiplication sign U+00D7)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions | Test code only — no production trust boundary changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | Test DOM query via querySelector | accept | querySelector targets aria-hidden attribute in the rendered DOM — test-only change, no production code modified |
</threat_model>

<verification>
Phase-level gate (run after this plan completes):
- `npm test` — 0 failures across entire suite
- `npx tsc --noEmit` — 0 type errors
- `npx biome check src/` — 0 lint errors
- `grep -rn "×\|☰\|📋\|🔍\|🎯\|⚡\|🔄\|🤖\|☀\|🌙\|↕\|↔\|👁\|📥\|📤\|🗑\|📝\|✓\|✎\|⧉\|👤" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ /Users/dallask/Projects/dallask/interviewer-checklist/src/app/` — 0 lines

Then proceed to `/gsd-verify-work 18` for visual verification checkpoint.
</verification>

<success_criteria>
The test suite is fully green (2693+ tests, 0 failures). TypeScript reports 0 errors. Biome reports 0 lint errors. The getByText('✓') queries in SessionRow.test.tsx are replaced with stable DOM attribute queries. The phase is ready for human visual verification via `/gsd-verify-work 18`.
</success_criteria>

<output>
Create `.planning/phases/18-icon-library/18-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

No new symbols. Only test assertion patterns are changed:

- `SessionRow.test.tsx` lines ~85 and ~101: `screen.getByText('✓')` replaced with `element.querySelector('[aria-hidden="true"]')` pattern (with className assertion on the resulting span element)
