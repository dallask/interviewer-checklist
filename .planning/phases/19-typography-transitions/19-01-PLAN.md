---
phase: 19-typography-transitions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SidebarGroup.tsx
  - src/components/SectionRow.tsx
  - src/components/SectionFilter.tsx
  - src/components/TopicRow.tsx
  - src/components/QuestionCard.tsx
  - src/components/ContentTree.tsx
  - src/components/ActionsGroup.tsx
  - src/components/SessionRow.tsx
  - src/components/CandidateModal.tsx
  - src/components/ImportPreviewModal.tsx
  - src/components/AiPromptModal.tsx
  - src/components/AddTopicForm.tsx
  - src/components/AddSectionForm.tsx
  - src/components/AboutModal.tsx
  - src/components/CustomQuestionForm.tsx
  - src/components/ResetConfirmDialog.tsx
  - src/components/DeleteSessionConfirmDialog.tsx
  - src/components/DifficultyFilter.tsx
  - src/components/SearchGroup.tsx
  - src/components/StorageToast.tsx
  - src/components/SessionSwitcherModal.tsx
autonomous: true
requirements:
  - POL-02

must_haves:
  truths:
    - "Every body-text element across the sidebar and content tree reads as 13px (Tailwind `text-[13px]`), not 14px (Tailwind `text-sm`)"
    - "Section/group headers (text-base) and accent/meta text (text-xs) are unchanged"
    - "SectionRow, TopicRow, SectionFilter, SidebarGroup, and ContentTree add-buttons have tighter vertical padding per D-02 table"
    - "No `min-h-[44px]` touch target is violated by padding reductions"
    - "All 2693 existing tests pass after the changes"
  artifacts:
    - path: "src/components/QuestionCard.tsx"
      provides: "13px body text on question span, print row, textarea"
      contains: "text-[13px]"
    - path: "src/components/TopicRow.tsx"
      provides: "13px font on topic row header div and textarea, py-1.5 on toggle/delete buttons"
      contains: "text-[13px]"
    - path: "src/components/SectionRow.tsx"
      provides: "13px on question count span, py-2 on toggle/delete buttons"
      contains: "text-[13px]"
    - path: "src/components/SectionFilter.tsx"
      provides: "13px + py-1.5 on filter buttons"
      contains: "text-[13px]"
    - path: "src/components/SidebarGroup.tsx"
      provides: "pb-2 on inner content div (density reduction)"
      contains: "pb-2"
    - path: "src/components/CandidateModal.tsx"
      provides: "13px on all 11 body-text instances"
      contains: "text-[13px]"
  key_links:
    - from: "src/components/SectionRow.tsx"
      to: "touch target floor"
      via: "min-h-[44px] still present after py-3 → py-2"
      pattern: "min-h-\\[44px\\]"
    - from: "src/components/TopicRow.tsx"
      to: "touch target floor"
      via: "min-h-[44px] still present after py-2 → py-1.5"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Apply font-density changes (D-01 + D-02) across all 20 component files. This is a pure class-string replacement — no structural changes to JSX, no new state, no logic changes.

Purpose: Satisfy POL-02 — base font size 13px with tightened spacing throughout the sidebar and content tree.

Output: 20 modified TSX files with `text-sm` → `text-[13px]` (65 instances) and 7 targeted padding class changes per D-02 table.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-sm → text-[13px] in all 20 component files (D-01, per D-06 static literals only)</name>
  <files>
    src/components/SidebarGroup.tsx,
    src/components/SectionRow.tsx,
    src/components/SectionFilter.tsx,
    src/components/TopicRow.tsx,
    src/components/QuestionCard.tsx,
    src/components/ContentTree.tsx,
    src/components/ActionsGroup.tsx,
    src/components/SessionRow.tsx,
    src/components/CandidateModal.tsx,
    src/components/ImportPreviewModal.tsx,
    src/components/AiPromptModal.tsx,
    src/components/AddTopicForm.tsx,
    src/components/AddSectionForm.tsx,
    src/components/AboutModal.tsx,
    src/components/CustomQuestionForm.tsx,
    src/components/ResetConfirmDialog.tsx,
    src/components/DeleteSessionConfirmDialog.tsx,
    src/components/DifficultyFilter.tsx,
    src/components/SearchGroup.tsx,
    src/components/StorageToast.tsx,
    src/components/SessionSwitcherModal.tsx
  </files>
  <read_first>
    Read each file before editing. The full file list is confirmed by grep: 65 total `text-sm` instances across 20 component files (SidebarGroup has 0 — confirmed from file read above; check SidebarGroup.tsx before and skip if none present).

    Key per-file counts (from grep audit):
    - CandidateModal.tsx: 11 instances (largest)
    - ImportPreviewModal.tsx: 6
    - AiPromptModal.tsx: 5
    - CustomQuestionForm.tsx: 4, AddTopicForm.tsx: 4, AddSectionForm.tsx: 4, AboutModal.tsx: 4
    - ResetConfirmDialog.tsx: 3, QuestionCard.tsx: 3, DeleteSessionConfirmDialog.tsx: 3
    - TopicRow.tsx: 2, SessionSwitcherModal.tsx: 2, SessionRow.tsx: 2, SectionFilter.tsx: 2, DifficultyFilter.tsx: 2
    - StorageToast.tsx: 1, SectionRow.tsx: 1, SearchGroup.tsx: 1, ContentTree.tsx: 1, ActionsGroup.tsx: 1

    Exceptions (DO NOT change):
    - `text-base` instances: SectionRow line 19 (`font-semibold text-base`), SidebarGroup line 28 (`font-semibold text-base`), CandidateModal heading — keep these unchanged
    - `text-xs` instances throughout — keep all unchanged
    - The class being added must be static string literal `text-[13px]` — no template string construction (D-06)
  </read_first>
  <action>
    In each of the 20 listed files, perform a targeted find-and-replace: every occurrence of the string `text-sm` (within className strings) → `text-[13px]`. The replacement is purely textual — the surrounding class strings remain identical otherwise.

    Rules:
    1. Replace ONLY `text-sm` — do not change `text-base` or `text-xs`
    2. The new class literal is `text-[13px]` (Tailwind v4 arbitrary value, no escaping needed in JSX className strings)
    3. In QuestionCard.tsx, the textarea on line 177 has `text-sm` inside a template literal — this template literal must be changed to a static string by extracting the dynamic `hidden` logic. After D-04 (Plan 02), this textarea's structure changes further; for this task, simply replace `text-sm` → `text-[13px]` within the existing className value

    Do NOT touch SidebarGroup.tsx for D-01 (confirmed 0 text-sm instances). Still read the file for the D-02 task below.
  </action>
  <verify>
    <automated>grep -r "text-sm" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/ --include="*.tsx" | grep -v "\.test\." | grep -c "text-sm"</automated>
    Expected output: 0 (all non-test component files have zero text-sm instances after replacement)
  </verify>
  <done>
    Zero `text-sm` instances remain in any non-test .tsx file under src/components/. The only font-size classes present are `text-[13px]` (body), `text-base` (headers, unchanged), and `text-xs` (accents, unchanged).
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply D-02 padding reductions to SectionRow, TopicRow, SectionFilter, SidebarGroup content div, and ContentTree add-buttons</name>
  <files>
    src/components/SectionRow.tsx,
    src/components/TopicRow.tsx,
    src/components/SectionFilter.tsx,
    src/components/SidebarGroup.tsx,
    src/components/ContentTree.tsx
  </files>
  <read_first>
    Read all five files before editing. The current padding values confirmed by source:

    SectionRow.tsx:
    - Line 24: toggle button className contains `py-3` — change to `py-2`
    - Line 38: delete button className contains `py-3` — change to `py-2`
    - Line 19: outermost div has `text-base font-semibold` — leave intact (D-01 exception)
    - No `min-h-[44px]` currently on these buttons (confirmed from file read) — after py-3 → py-2, verify height floor still holds via flex-1 stretch. If needed, add `min-h-[44px]` to button className (per Pitfall 5 note in RESEARCH.md).

    TopicRow.tsx:
    - Line 69: toggle button className contains `py-2` — change to `py-1.5`
    - Line 85: delete button className contains `py-2` — change to `py-1.5`
    - `min-h-[44px]` is present at line 82 on TopicRow button per PATTERNS.md — verify it stays

    SectionFilter.tsx:
    - Lines 42 and 72: button classNames contain `py-2` — change both to `py-1.5`

    SidebarGroup.tsx:
    - Line 43: inner content div className contains `pb-3` — change to `pb-2`
    - NOTE: Plan 02 will later restructure this div into a grid-rows wrapper (D-03). This task only changes `pb-3` → `pb-2` on the existing div. Do NOT restructure the hidden={!isOpen} div here — that is Plan 02's work.

    ContentTree.tsx:
    - Line 125: add-section button className contains `py-2` — change to `py-1.5`
    - Line 141: add-topic button className contains `py-2` — change to `py-1.5` (or confirm these line numbers by reading the file)
  </read_first>
  <action>
    Apply exactly the 7 padding changes specified in D-02:

    1. SectionRow.tsx toggle button: `py-3` → `py-2` (keep all other classes on that button unchanged)
    2. SectionRow.tsx delete button: `py-3` → `py-2`
    3. TopicRow.tsx toggle button: `py-2` → `py-1.5`
    4. TopicRow.tsx delete button: `py-2` → `py-1.5`
    5. SectionFilter.tsx both filter buttons: `py-2` → `py-1.5` (two separate className strings)
    6. SidebarGroup.tsx content div: `pb-3` → `pb-2` (the `className="px-4 pb-3"` div — change ONLY the pb, keep px-4)
    7. ContentTree.tsx add-section button and add-topic button: `py-2` → `py-1.5` (two buttons)

    After changes 1 and 2 on SectionRow, check whether `min-h-[44px]` is already present on the SectionRow toggle/delete button class strings. If not present, add `min-h-[44px]` to both buttons (same pattern as TopicRow line 82 and QuestionCard line 82 which already have it). This guards against Pitfall 5 from RESEARCH.md.

    QuestionCard.tsx row is already at `py-1.5` — skip it (D-02 says "keep as-is").
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -5</automated>
    All tests must pass (2693 tests, ~6 seconds).

    Additionally verify the 7 specific substitutions landed correctly:
    <automated>grep -n "py-3" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionRow.tsx | wc -l</automated>
    Expected: 0 (no more py-3 in SectionRow)
    <automated>grep -n "pb-3" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SidebarGroup.tsx | wc -l</automated>
    Expected: 0 (no more pb-3 in SidebarGroup)
  </verify>
  <done>
    All 7 D-02 padding changes applied. No `py-3` remains in SectionRow, no `pb-3` remains in SidebarGroup content div, no `py-2` remains on the four targeted TopicRow/SectionFilter/ContentTree buttons. All 2693 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component JSX → DOM | Class string literals are injected into DOM attributes. Only static literals used (D-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | Tailwind class scanner | accept | All new classes are static string literals (D-06); Tailwind v4 content scan picks them up at build time. Dynamic class construction is explicitly prohibited. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
Run full test suite after all tasks complete:

```
npm test -- --run
```

Must return: 2693 tests pass, 0 failures.

Spot-check: open extension in browser → DevTools inspect any QuestionCard question text → computed font-size should read 13px.
</verification>

<success_criteria>
1. Zero `text-sm` instances in any non-test .tsx file under src/components/
2. All 7 D-02 padding changes landed (confirmed by grep)
3. `text-base` (headers) and `text-xs` (accents) unchanged — grep shows they still exist where expected
4. All 2693 tests pass (no regressions)
</success_criteria>

<output>
Create `.planning/phases/19-typography-transitions/19-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols and classes introduced by this plan:

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `text-[13px]` | Tailwind arbitrary class | 20 component TSX files | Replaces `text-sm`; used as static literal per D-06 |
| `py-2` (on SectionRow toggle/delete) | Tailwind padding class | src/components/SectionRow.tsx | Replaces `py-3`; D-02 density reduction |
| `py-1.5` (on TopicRow toggle/delete) | Tailwind padding class | src/components/TopicRow.tsx | Replaces `py-2`; D-02 density reduction |
| `py-1.5` (on SectionFilter buttons) | Tailwind padding class | src/components/SectionFilter.tsx | Replaces `py-2`; D-02 density reduction |
| `pb-2` (on SidebarGroup content div) | Tailwind padding class | src/components/SidebarGroup.tsx | Replaces `pb-3`; D-02 density reduction |
| `py-1.5` (on ContentTree add-buttons) | Tailwind padding class | src/components/ContentTree.tsx | Replaces `py-2`; D-02 density reduction |
</output>
