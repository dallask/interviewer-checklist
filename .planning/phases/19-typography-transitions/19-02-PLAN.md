---
phase: 19-typography-transitions
plan: "02"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/components/SidebarGroup.tsx
  - src/components/SidebarGroup.test.tsx
  - src/components/QuestionCard.tsx
  - src/components/QuestionCard.test.tsx
  - src/components/TopicRow.tsx
autonomous: true
requirements:
  - POL-03

must_haves:
  truths:
    - "SidebarGroup collapses by setting gridTemplateRows to 0fr — no hidden attribute on the region div"
    - "SidebarGroup region div remains in the DOM when isOpen=false (aria-controls resolves)"
    - "QuestionCard textarea is wrapped in a grid-rows div; toggling notesOpen changes gridTemplateRows between 0fr and 1fr"
    - "TopicRow textarea is wrapped in a grid-rows div; toggling topicNotesOpen changes gridTemplateRows between 0fr and 1fr"
    - "SidebarGroup.test.tsx passes with updated grid-rows assertion instead of hidden-attribute assertion"
    - "QuestionCard.test.tsx passes with updated grid-rows assertion instead of hidden-class assertion"
    - "All 2693 existing tests pass"
  artifacts:
    - path: "src/components/SidebarGroup.tsx"
      provides: "grid-rows accordion replacing hidden attr (D-03)"
      contains: "gridTemplateRows"
    - path: "src/components/SidebarGroup.test.tsx"
      provides: "updated assertion: style.gridTemplateRows === '0fr' when isOpen=false"
      contains: "gridTemplateRows"
    - path: "src/components/QuestionCard.tsx"
      provides: "grid-rows wrapper around textarea replacing hidden class (D-04)"
      contains: "gridTemplateRows"
    - path: "src/components/QuestionCard.test.tsx"
      provides: "updated assertion: wrapper.style.gridTemplateRows === '0fr' when notesOpen=false"
      contains: "gridTemplateRows"
    - path: "src/components/TopicRow.tsx"
      provides: "grid-rows wrapper around textarea replacing hidden HTML attr (D-04)"
      contains: "gridTemplateRows"
  key_links:
    - from: "src/components/SidebarGroup.tsx"
      to: "aria-controls resolution"
      via: "region div always in DOM; grid-rows collapses visually"
      pattern: "id={regionId}"
    - from: "src/components/QuestionCard.tsx"
      to: "notes open/close"
      via: "gridTemplateRows driven by notesOpen state"
      pattern: "notesOpen.*1fr.*0fr"
    - from: "src/components/TopicRow.tsx"
      to: "topic notes open/close"
      via: "gridTemplateRows driven by topicNotesOpen || localTopicNote || printMode"
      pattern: "topicNotesOpen.*1fr.*0fr"
---

<objective>
Replace `hidden`-attribute and `hidden`-class toggle patterns with CSS `grid-template-rows: 0fr → 1fr` animated transitions (D-03 for SidebarGroup, D-04 for QuestionCard and TopicRow). Update the two test files that assert the now-removed hidden behaviors.

Purpose: Satisfy POL-03 — expand/collapse interactions have visible CSS transitions. Also fixes the pre-existing limitation where `hidden` attribute prevented any CSS animation.

Output: 3 modified component TSX files with structural grid wrapper changes + 2 updated test files with grid-rows assertions.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-PATTERNS.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SidebarGroup D-03 — replace hidden attr with grid-rows transition wrapper; update SidebarGroup.test.tsx (per D-03, D-07, D-08)</name>
  <files>
    src/components/SidebarGroup.tsx,
    src/components/SidebarGroup.test.tsx
  </files>
  <read_first>
    Read src/components/SidebarGroup.tsx (full file — 49 lines). The current collapsible region is:
    ```
    <div
      id={regionId}
      hidden={!isOpen}
      className="px-4 pb-3"
    >
      {children}
    </div>
    ```
    Note: pb-3 was already changed to pb-2 by Plan 01 Task 2 — the file may now read `pb-2` at that point.

    Read src/components/SidebarGroup.test.tsx (full file). The breaking assertion is at lines 52–68:
    ```
    expect(region).toHaveAttribute('hidden');
    ```
    The test title is: 'region div has hidden attribute when isOpen=false (aria-controls always resolves)'
  </read_first>
  <action>
    In SidebarGroup.tsx — replace the single `<div id={regionId} hidden={!isOpen} className="px-4 pb-2">` block with the D-03 locked pattern:

    Outer div: id={regionId}, className="grid motion-safe:transition-[grid-template-rows] motion-safe:duration-200 overflow-hidden", style={{ gridTemplateRows: isOpen ? '1fr' : '0fr' }}
    Inner div: className="min-h-0 px-4 pb-2", contains {children}

    Key rules:
    - Remove the `hidden={!isOpen}` prop entirely — this is the critical change (hidden=display:none blocks CSS animation)
    - The outer div MUST have className starting with `grid` (sets display:grid, required for grid-template-rows to work)
    - The `style` prop uses an object `{ gridTemplateRows: isOpen ? '1fr' : '0fr' }` — this is D-08-approved because the value is runtime-dynamic
    - `motion-safe:transition-[grid-template-rows]` and `motion-safe:duration-200` must both be present (each needs its own `motion-safe:` prefix)
    - `overflow-hidden` clips content during animation
    - `min-h-0` on inner div prevents implicit min-height:auto blocking collapse
    - Do NOT touch the existing `motion-reduce:transition-none` on the chevron span (D-07)
    - The inner div keeps `px-4 pb-2` (pb-2 from Plan 01 Task 2)

    In SidebarGroup.test.tsx — update the breaking test at lines 52–68:
    - Change test title to: 'region div is always in DOM when isOpen=false (aria-controls always resolves)'
    - Remove `expect(region).toHaveAttribute('hidden')`
    - Add these two assertions in its place:
      1. `expect(region).not.toHaveAttribute('hidden')` — D-03: no hidden attr; grid-rows controls visibility
      2. `expect(region?.style.gridTemplateRows).toBe('0fr')` — collapsed state verified via style prop

    Do not change any other assertions in the file.
  </action>
  <verify>
    <automated>npm test -- --run src/components/SidebarGroup.test.tsx 2>&1 | tail -10</automated>
    All SidebarGroup tests must pass. Then run full suite:
    <automated>npm test -- --run 2>&1 | tail -5</automated>
    All 2693 tests must pass.
  </verify>
  <done>
    SidebarGroup.tsx has no `hidden` prop on the region div. The region div has class `grid` and a `style` prop with `gridTemplateRows`. SidebarGroup.test.tsx has zero occurrences of `toHaveAttribute('hidden')`. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: QuestionCard D-04 — wrap textarea in grid-rows div, update QuestionCard.test.tsx; TopicRow D-04 — wrap textarea in grid-rows div (per D-04, D-07, D-08)</name>
  <files>
    src/components/QuestionCard.tsx,
    src/components/QuestionCard.test.tsx,
    src/components/TopicRow.tsx
  </files>
  <read_first>
    Read src/components/QuestionCard.tsx lines 169–181 (the notes section). After Plan 01 Task 1, `text-sm` in the textarea className will have been replaced with `text-[13px]`. The current structure:
    ```
    <div className={hideNotes && !printMode ? 'hidden' : ''}>
      <textarea
        id={`notes-${questionId}`}
        ...
        className={`w-full resize-y min-h-[64px] text-[13px] ... ${!notesOpen && !printMode ? ' hidden' : ''}`}
        style={{ width: 'calc(100% - 1.5rem)' }}
      />
    </div>
    ```
    The outer `hideNotes` wrapper stays (it's a static mode toggle, not an animated toggle). Only the inner `notesOpen` toggle needs the grid-rows treatment.

    Read src/components/QuestionCard.test.tsx lines 179–189. The breaking assertions:
    ```
    expect(textarea.className).toContain('hidden');       // line 182
    expect(textarea.className).not.toContain('hidden');   // line 187
    ```

    Read src/components/TopicRow.tsx lines 92–115. The textarea with `hidden={!topicNotesOpen && !localTopicNote && !printMode}` HTML attribute (line 111). After Plan 01 Task 1, `text-sm` on this textarea will have been replaced with `text-[13px]`.
  </read_first>
  <action>
    In QuestionCard.tsx — restructure the notes section (lines 169–180):

    Keep the outer `<div className={hideNotes && !printMode ? 'hidden' : ''}>` wrapper exactly as-is (this controls notes-disabled mode, not animated).

    Inside that outer div, add a grid-rows wrapper div and an inner min-h-0 div, then place the textarea inside:
    - Grid wrapper div: className="grid motion-safe:transition-[grid-template-rows] motion-safe:duration-200 overflow-hidden", style={{ gridTemplateRows: notesOpen || printMode ? '1fr' : '0fr' }}
    - Inner wrapper div: className="min-h-0"
    - The textarea: remove `${!notesOpen && !printMode ? ' hidden' : ''}` from its className (the grid wrapper now controls visibility). Keep all other textarea attributes and classes intact, including `style={{ width: 'calc(100% - 1.5rem)' }}` and `print:h-auto print:overflow-visible print:resize-none print:border-0 print:p-0`

    Important: the condition on gridTemplateRows is `notesOpen || printMode ? '1fr' : '0fr'` (includes printMode so notes expand for print).

    In QuestionCard.test.tsx — update the test at lines 179–189:
    - Change test title to: 'clicking note icon button expands the textarea grid wrapper'
    - Remove both `expect(textarea.className).toContain('hidden')` and `.not.toContain('hidden')` assertions
    - Replace with:
      1. `expect(textarea.className).not.toContain('hidden')` — textarea no longer uses hidden class
      2. `const wrapper = textarea.closest('.grid') as HTMLElement` — find the grid wrapper
      3. `expect(wrapper?.style.gridTemplateRows).toBe('0fr')` — collapsed initially
      4. After `fireEvent.click(btn)`: `expect(wrapper?.style.gridTemplateRows).toBe('1fr')` — expanded after click
    - Keep the existing `expect(textarea).not.toHaveAttribute('hidden')` assertion (it was already there and remains valid)

    In TopicRow.tsx — restructure the textarea in the topic notes panel (lines 106–114):

    The `<textarea ... hidden={!topicNotesOpen && !localTopicNote && !printMode} ...>` gets:
    - Remove the `hidden={...}` HTML attribute from the textarea element
    - Wrap the textarea in a grid-rows outer div: className="grid motion-safe:transition-[grid-template-rows] motion-safe:duration-200 overflow-hidden", style={{ gridTemplateRows: (topicNotesOpen || localTopicNote || printMode) ? '1fr' : '0fr' }}
    - Add inner wrapper div: className="min-h-0", then the textarea inside
    - Keep all existing textarea attributes (id, aria-label, value, onChange, placeholder, className) unchanged except that `text-sm` was already replaced by Plan 01 Task 1

    Do not modify TopicRow test file — no assertions break for TopicRow in existing tests (TopicRow test does not assert on the hidden attribute).
  </action>
  <verify>
    <automated>npm test -- --run src/components/QuestionCard.test.tsx 2>&1 | tail -10</automated>
    QuestionCard tests must pass. Then:
    <automated>npm test -- --run 2>&1 | tail -5</automated>
    All 2693 tests must pass.
  </verify>
  <done>
    QuestionCard.tsx: the textarea has no `hidden` conditional in its className; it is wrapped in a `.grid` div with a `style.gridTemplateRows` prop driven by `notesOpen || printMode`. QuestionCard.test.tsx: zero `toContain('hidden')` assertions on the textarea className. TopicRow.tsx: the textarea has no `hidden` HTML attribute; it is wrapped in a `.grid` div with `style.gridTemplateRows` driven by `topicNotesOpen || localTopicNote || printMode`. All 2693 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component state → DOM style | `gridTemplateRows` value is derived from boolean React state (`isOpen`, `notesOpen`). Values are hardcoded literals `'1fr'` and `'0fr'` — no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | Tampering | inline style prop | accept | `gridTemplateRows` values are static string literals `'1fr'` and `'0fr'` derived from boolean state — not from user input. No XSS vector. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
Run full test suite:

```
npm test -- --run
```

Must return: 2693 tests pass, 0 failures.

Manual check (after loading the extension):
1. Open the sidebar — click a SidebarGroup header — the content should animate open/closed (grid-rows transition at 200ms)
2. Click a note icon on a QuestionCard — textarea should animate open/closed
3. Click "Add topic notes" on a TopicRow — textarea should animate open/closed
</verification>

<success_criteria>
1. SidebarGroup.tsx: no `hidden` prop on region div; region div has class `grid` and inline style with `gridTemplateRows`
2. SidebarGroup.test.tsx: zero `toHaveAttribute('hidden')` assertions
3. QuestionCard.tsx: textarea wrapped in `.grid` div with `gridTemplateRows`; no `hidden` in textarea className
4. QuestionCard.test.tsx: zero `toContain('hidden')` on textarea.className
5. TopicRow.tsx: textarea wrapped in `.grid` div with `gridTemplateRows`; no `hidden` attribute on textarea
6. All 2693 tests pass
</success_criteria>

<output>
Create `.planning/phases/19-typography-transitions/19-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols and structures introduced by this plan:

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| Grid-rows accordion wrapper (outer div) | JSX element | src/components/SidebarGroup.tsx | `className="grid motion-safe:transition-[grid-template-rows] motion-safe:duration-200 overflow-hidden"` + `style={{ gridTemplateRows: isOpen ? '1fr' : '0fr' }}` |
| Grid-rows accordion inner div | JSX element | src/components/SidebarGroup.tsx | `className="min-h-0 px-4 pb-2"` |
| Grid-rows textarea wrapper (outer div) | JSX element | src/components/QuestionCard.tsx | `style={{ gridTemplateRows: notesOpen \|\| printMode ? '1fr' : '0fr' }}` |
| Grid-rows textarea inner div | JSX element | src/components/QuestionCard.tsx | `className="min-h-0"` |
| Grid-rows textarea wrapper (outer div) | JSX element | src/components/TopicRow.tsx | `style={{ gridTemplateRows: (topicNotesOpen \|\| localTopicNote \|\| printMode) ? '1fr' : '0fr' }}` |
| Grid-rows textarea inner div | JSX element | src/components/TopicRow.tsx | `className="min-h-0"` |
| `motion-safe:transition-[grid-template-rows]` | Tailwind arbitrary transition class | SidebarGroup, QuestionCard, TopicRow | D-07 motion-safe opt-in; paired with `motion-safe:duration-200` |
