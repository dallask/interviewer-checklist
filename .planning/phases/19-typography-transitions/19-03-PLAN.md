---
phase: 19-typography-transitions
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/styles.css
  - src/components/SectionRow.tsx
  - src/components/TopicRow.tsx
  - src/components/QuestionCard.tsx
autonomous: true
requirements:
  - POL-03

must_haves:
  truths:
    - "@keyframes fade-in is defined in src/app/styles.css"
    - "SectionRow, TopicRow, and QuestionCard each have motion-safe:animate-[fade-in_150ms_ease-out] on their outermost div"
    - "dialog elements animate open with opacity 0→1 and scale 0.95→1 via @starting-style"
    - "prefers-reduced-motion users see no animation (motion-safe prefix + media query in CSS)"
    - "All 2693 existing tests pass"
  artifacts:
    - path: "src/app/styles.css"
      provides: "@keyframes fade-in + dialog @starting-style animation block"
      contains: "@keyframes fade-in"
    - path: "src/components/SectionRow.tsx"
      provides: "fade-in mount animation on outermost div"
      contains: "motion-safe:animate-[fade-in_150ms_ease-out]"
    - path: "src/components/TopicRow.tsx"
      provides: "fade-in mount animation on outermost div"
      contains: "motion-safe:animate-[fade-in_150ms_ease-out]"
    - path: "src/components/QuestionCard.tsx"
      provides: "fade-in mount animation on outermost div"
      contains: "motion-safe:animate-[fade-in_150ms_ease-out]"
  key_links:
    - from: "src/app/styles.css"
      to: "Tailwind arbitrary animation class"
      via: "@keyframes fade-in name referenced in animate-[fade-in_150ms_ease-out]"
      pattern: "@keyframes fade-in"
    - from: "src/app/styles.css"
      to: "dialog elements (CandidateModal, ImportPreviewModal, AiPromptModal, AboutModal, ResetConfirmDialog, DeleteSessionConfirmDialog)"
      via: "dialog CSS rule targets all <dialog> elements globally"
      pattern: "dialog \\{"
---

<objective>
Add global CSS for the fade-in keyframe (D-05) and dialog open animation (D-06), then apply the fade-in class to the outermost div of SectionRow, TopicRow, and QuestionCard.

Purpose: Satisfy POL-03 — row mount animations and modal open animations for smooth feel.

Output: 1 modified CSS file (styles.css) + 3 modified component files with fade-in class on outermost divs.

Note: This plan runs in Wave 1 in parallel with Plan 01. It touches styles.css (no overlap with Plan 01) and the outermost divs of SectionRow, TopicRow, QuestionCard (Plan 01 modifies text-sm instances inside these files but NOT the outermost divs). Plan 02 (Wave 2) will later modify the internal structure of TopicRow and QuestionCard — the outermost div changes in this plan are safe to apply first.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/ROADMAP.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-CONTEXT.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.planning/phases/19-typography-transitions/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @keyframes fade-in and dialog @starting-style animation to src/app/styles.css (D-05 + D-06)</name>
  <files>
    src/app/styles.css
  </files>
  <read_first>
    Read src/app/styles.css (full file — 12 lines). Current content:
    ```css
    @import "tailwindcss";

    @custom-variant dark (&:where(.dark, .dark *));

    dialog::backdrop {
      background: rgba(0, 0, 0, 0.5);
    }

    @keyframes slide-up {
      from { transform: translateY(100%); }
      to   { transform: translateY(0); }
    }
    ```
    The existing `@keyframes slide-up` is the pattern to follow. Append new rules after it.
  </read_first>
  <action>
    Append the following two blocks to src/app/styles.css after the existing `@keyframes slide-up` block:

    Block 1 (D-05 — fade-in keyframe):
    ```
    @keyframes fade-in {
      from { opacity: 0; }
      to   { opacity: 1; }
    }
    ```

    Block 2 (D-06 — dialog open animation):
    ```
    dialog {
      opacity: 1;
      scale: 1;
      transition: opacity 150ms ease-out, scale 150ms ease-out,
                  display 150ms allow-discrete, overlay 150ms allow-discrete;
    }

    @starting-style {
      dialog[open] {
        opacity: 0;
        scale: 0.95;
      }
    }

    @media (prefers-reduced-motion: reduce) {
      dialog {
        transition: none;
      }
    }
    ```

    Rules:
    - Use exact CSS from D-05 and D-06 (locked decisions). Do not alter property names or values.
    - `allow-discrete` in the transition value is required — it enables animating the discrete `display` and `overlay` properties
    - The `@media (prefers-reduced-motion: reduce)` block overrides the dialog transition for users who prefer no motion (separate from the `motion-safe:` prefix used in TSX — CSS-only context here)
    - The existing `dialog::backdrop` rule stays as-is above these new blocks
    - Do NOT add motion-safe prefix here — this is global CSS, not a Tailwind class context
  </action>
  <verify>
    <automated>grep -c "@keyframes fade-in" /Users/dallask/Projects/dallask/interviewer-checklist/src/app/styles.css</automated>
    Expected: 1

    <automated>grep -c "@starting-style" /Users/dallask/Projects/dallask/interviewer-checklist/src/app/styles.css</automated>
    Expected: 1

    <automated>grep -c "allow-discrete" /Users/dallask/Projects/dallask/interviewer-checklist/src/app/styles.css</automated>
    Expected: 2 (appears twice: once for display, once for overlay in the transition shorthand)

    <automated>npm test -- --run 2>&1 | tail -5</automated>
    All 2693 tests pass.
  </verify>
  <done>
    src/app/styles.css contains both `@keyframes fade-in` and `@starting-style { dialog[open] { ... } }` and the `@media (prefers-reduced-motion: reduce)` dialog block. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fade-in mount animation to outermost div of SectionRow, TopicRow, and QuestionCard (D-05, per D-07 motion-safe)</name>
  <files>
    src/components/SectionRow.tsx,
    src/components/TopicRow.tsx,
    src/components/QuestionCard.tsx
  </files>
  <read_first>
    Read src/components/SectionRow.tsx to identify the outermost div (line 18 area). The outermost div currently: `<div className="bg-gray-50 dark:bg-gray-800/50 font-semibold text-base border-b border-gray-200 dark:border-gray-700 w-full flex items-center text-gray-900 dark:text-gray-100">`. Note: if Plan 01 ran concurrently, check current state. No `text-sm` on outermost div — this is the `text-base` header div — so it's unchanged by Plan 01.

    Read src/components/TopicRow.tsx to identify the outermost div (line 61 area). The return starts with `<div>` (plain, no classes). This is the outermost div for TopicRow — add `motion-safe:animate-[fade-in_150ms_ease-out]` to this element.

    Read src/components/QuestionCard.tsx lines 77–80 to identify the outermost div: `<div className={\`bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 ${BORDER_CLASSES[question.level]}\`}>`. This uses a template literal because of `BORDER_CLASSES[question.level]` dynamic insertion — D-06 says no dynamic class construction in new classes, but this template literal pre-exists and already embeds a dynamic BORDER_CLASSES value. Adding `motion-safe:animate-[fade-in_150ms_ease-out]` as a static string within the template literal is acceptable (the dynamic part is BORDER_CLASSES, which is a D-06-approved lookup map pattern established in Phase 17).

    Reference pattern from UndoToast.tsx line 16: `className="... motion-safe:animate-[slide-up_150ms_ease-out]"` — same syntax, different keyframe name.
  </read_first>
  <action>
    In SectionRow.tsx — add `motion-safe:animate-[fade-in_150ms_ease-out]` to the outermost `<div>` className:
    Current className string ends with `text-gray-100`. Append ` motion-safe:animate-[fade-in_150ms_ease-out]` to the className string. Resulting className: `"bg-gray-50 dark:bg-gray-800/50 font-semibold text-base border-b border-gray-200 dark:border-gray-700 w-full flex items-center text-gray-900 dark:text-gray-100 motion-safe:animate-[fade-in_150ms_ease-out]"`.

    In TopicRow.tsx — the outermost div at the top of the return is `<div>` with no className. Add className: `<div className="motion-safe:animate-[fade-in_150ms_ease-out]">`.

    In QuestionCard.tsx — the outermost div uses a template literal. Add the static class to the template literal string portion:
    Change: `` className={`bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 ${BORDER_CLASSES[question.level]}`} ``
    To: `` className={`bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 motion-safe:animate-[fade-in_150ms_ease-out] ${BORDER_CLASSES[question.level]}`} ``
    The `motion-safe:animate-[fade-in_150ms_ease-out]` string is a static literal within the template — not derived from any variable.

    Rules:
    - Use ONLY `motion-safe:` prefix — this is D-07 opt-in convention for all Phase 19 animations
    - The keyframe name `fade-in` matches exactly the `@keyframes fade-in` added in Task 1
    - Duration `150ms` and easing `ease-out` match the locked D-05 spec
    - Do NOT add fade-in to any other elements (only the outermost div of the three named components)
    - Do NOT add fade-in to SidebarGroup (not in scope per D-05)
  </action>
  <verify>
    <automated>grep -l "motion-safe:animate-\[fade-in" /Users/dallask/Projects/dallask/interviewer-checklist/src/components/SectionRow.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/TopicRow.tsx /Users/dallask/Projects/dallask/interviewer-checklist/src/components/QuestionCard.tsx | wc -l</automated>
    Expected: 3 (all three files contain the fade-in class)

    <automated>npm test -- --run 2>&1 | tail -5</automated>
    All 2693 tests pass.
  </verify>
  <done>
    All three outermost divs (SectionRow, TopicRow, QuestionCard) have `motion-safe:animate-[fade-in_150ms_ease-out]` in their className. The keyframe name matches the `@keyframes fade-in` defined in styles.css Task 1. All 2693 tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Global CSS → all dialog elements | The `dialog { transition: ... }` rule applies to every `<dialog>` in the extension. No user-controlled input affects this style. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering | Global CSS dialog rule | accept | Rule applies to all `<dialog>` elements; values are hardcoded in CSS file — no user input path. Chrome Extension context means only trusted extension code can open dialogs. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
Run full test suite:

```
npm test -- --run
```

Must return: 2693 tests pass, 0 failures.

Manual visual check (after loading extension):
1. Expand/collapse a section or topic in the content tree — each row should fade in when mounting
2. Open any modal (candidate, import, about, etc.) — it should animate open with opacity 0→1 and slight scale from 0.95→1
3. Verify no animation when "Reduce motion" is enabled in OS accessibility settings
</verification>

<success_criteria>
1. src/app/styles.css contains `@keyframes fade-in { from { opacity: 0; } to { opacity: 1; } }`
2. src/app/styles.css contains `@starting-style { dialog[open] { opacity: 0; scale: 0.95; } }`
3. src/app/styles.css contains `@media (prefers-reduced-motion: reduce) { dialog { transition: none; } }`
4. SectionRow.tsx, TopicRow.tsx, QuestionCard.tsx each have `motion-safe:animate-[fade-in_150ms_ease-out]` on their outermost div
5. All 2693 tests pass
</success_criteria>

<output>
Create `.planning/phases/19-typography-transitions/19-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols and classes introduced by this plan:

| Symbol | Type | File | Notes |
|--------|------|------|-------|
| `@keyframes fade-in` | CSS keyframe | src/app/styles.css | `from { opacity: 0 } to { opacity: 1 }` — used by `animate-[fade-in_150ms_ease-out]` |
| `dialog { transition: ... }` | CSS rule | src/app/styles.css | Global rule for D-06 modal open animation; targets all `<dialog>` elements |
| `@starting-style { dialog[open] { ... } }` | CSS at-rule | src/app/styles.css | D-06 initial state for dialog open animation (Chrome 117+) |
| `@media (prefers-reduced-motion: reduce) { dialog { transition: none; } }` | CSS media query | src/app/styles.css | Accessibility guard for dialog animation |
| `motion-safe:animate-[fade-in_150ms_ease-out]` | Tailwind arbitrary animation class | SectionRow.tsx, TopicRow.tsx, QuestionCard.tsx | D-05 row mount fade-in; motion-safe opt-in per D-07 |
