---
phase: 21-layout-content-ordering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContentTree.tsx
autonomous: true
requirements:
  - LAYOUT-01

must_haves:
  truths:
    - "On viewports wider than 1232px, content rows are visually centered with equal whitespace on both sides"
    - "On narrow viewports the content spans full width with 16px padding on each side and no horizontal scrollbar"
    - "The scroll container ref={parentRef} remains full viewport width — scrollbar tracks the full viewport"
    - "The print-only candidate header is also constrained to max-w-[1200px] mx-auto so printed output matches"
    - "All five row types (SectionRow, TopicRow, QuestionCard, add-section-trigger button, add-topic-trigger button) are wrapped identically"
  artifacts:
    - path: "src/components/ContentTree.tsx"
      provides: "Centering wrapper inside each virtual row item div"
      contains: "max-w-[1200px]"
  key_links:
    - from: "ContentTree.tsx virtualItem div"
      to: "centering wrapper div"
      via: "inner div with className mx-auto w-full max-w-[1200px] px-4"
      pattern: "max-w-\\[1200px\\]"
    - from: "ContentTree.tsx print header"
      to: "centering class"
      via: "max-w-[1200px] mx-auto on the print-only div"
      pattern: "hidden print:block.*max-w-\\[1200px\\]"
---

<objective>
Implement LAYOUT-01: add a centering wrapper inside each virtual row item in ContentTree.tsx so the content area is horizontally centered at 1200px max-width on wide viewports and spans full width with 16px side padding on narrow viewports. The print-only candidate header receives the same constraint.

Purpose: Wide-viewport users currently see content stretched edge-to-edge. Capping at 1200px centers the reading experience, matching the content-width conventions of the rest of the app and improving readability at large screen sizes.

Output: Modified `src/components/ContentTree.tsx` with a single centering wrapper div (className "mx-auto w-full max-w-[1200px] px-4") inserted inside every virtual row item div, plus the same constraint on the print-only header block. The scroll container and inner virtualizer container are not changed.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-layout-content-ordering/21-CONTEXT.md
@.planning/phases/21-layout-content-ordering/21-UI-SPEC.md
@.planning/phases/21-layout-content-ordering/21-PATTERNS.md
@src/components/ContentTree.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap virtual row items with centering div in ContentTree.tsx</name>
  <files>src/components/ContentTree.tsx</files>
  <action>
    Per D-01, D-02, D-03, D-04, D-05 from 21-CONTEXT.md:

    Change 1 — Virtual row items centering wrapper. In the virtualItems.map() callback (lines 100–149), the outer div for each virtualItem has style={{ position: 'absolute', top: 0, left: 0, width: '100%', transform: ... }} and currently renders row components directly as children. Insert a new wrapper div as the single child of that outer div, wrapping all row-type renders:

    The wrapper div must use exactly this Tailwind class string (static literal, not dynamic): className="mx-auto w-full max-w-[1200px] px-4"

    All five conditional row renders go inside this wrapper:
    - {row.type === 'section' && <SectionRow row={row} />}
    - {row.type === 'topic' && <TopicRow row={row} />}
    - {row.type === 'question' && <QuestionCard row={row} />}
    - The add-section-trigger conditional (AddSectionForm or "+ Add section" button)
    - The add-topic-trigger conditional (AddTopicForm or "+ Add topic" button)

    Do NOT add max-w-[1200px] to:
    - The ref={parentRef} div (line 79, className "flex-1 overflow-y-auto") — must remain full viewport width so the scrollbar tracks correctly (D-01)
    - The inner virtualizer div with style={{ height, width: '100%', position: 'relative' }} — must track total scrollable height (D-01)

    Change 2 — Print-only header. The print-only div at line 83 has className "hidden print:block print:mb-4". Add max-w-[1200px] mx-auto px-4 to this className so the candidate name/role/date block is also constrained in print layout (D-04). Final className: "hidden print:block print:mb-4 max-w-[1200px] mx-auto px-4".

    No other changes to the file. Do not touch imports, the ESTIMATE_SIZE constant, Props interface, useVirtualizer config, the scroll-after-add useEffect, or any component logic.

    The Tailwind class string max-w-[1200px] is a valid Tailwind v4 JIT arbitrary-value literal. Use it as a static string — do not construct it dynamically (Tailwind v4 requires static literal strings for JIT resolution).
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ContentTree.tsx compiles without TypeScript errors. The string "max-w-[1200px]" appears at least twice in the file (once per virtualItem wrapper, once on the print header). The string "flex-1 overflow-y-auto" on the parentRef div does NOT include max-w-[1200px]. npx vitest run exits with all tests passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Centering wrapper applied to all content tree rows in ContentTree.tsx. On wide viewports content is constrained to 1200px centered. On narrow viewports content spans full width with 16px padding. Print layout also constrained.
  </what-built>
  <how-to-verify>
    1. Run the extension locally: npm run dev (or the equivalent dev build command for the Chrome extension).
    2. Open the extension in a browser tab at a wide viewport (e.g., 1440px or full-screen).
    3. Confirm: the question/section/topic rows are centered horizontally with equal whitespace on both sides — content does not span edge to edge.
    4. Resize the browser window to a narrow viewport (~600px wide).
    5. Confirm: the content fills the full width with small padding on each side and there is NO horizontal scrollbar.
    6. Open browser print preview (Cmd+P / Ctrl+P).
    7. Confirm: the candidate header (name/role/date) is also constrained and not spanning full print width.
    8. Confirm all existing interactive elements (expand/collapse sections, score dropdowns, note icons) still function normally.
  </how-to-verify>
  <resume-signal>Type "approved" if the layout looks correct, or describe any visual issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM layout → user display | CSS class string applied to DOM elements; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | Tailwind class string max-w-[1200px] | accept | Static literal string; JIT-resolved at build time; no runtime injection |
| T-21-04 | Denial of Service | Virtual row overflow on narrow viewports | accept | px-4 padding prevents content touching viewport edge; no horizontal scroll confirmed by verify step |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
1. TypeScript compile: npx tsc --noEmit exits 0
2. grep -c 'max-w-\[1200px\]' src/components/ContentTree.tsx returns >= 2 (virtual row wrapper + print header)
3. Confirm parentRef div does NOT have max-w-[1200px]: grep 'flex-1 overflow-y-auto' src/components/ContentTree.tsx must not include max-w-[1200px] on the same line
4. Full test suite: npx vitest run — all tests pass (ContentTree is a component with no unit test file; existing tests must not regress)
5. Human visual check: wide viewport shows centered content; narrow viewport shows no horizontal scrollbar
</verification>

<success_criteria>
- Content area is horizontally centered at 1200px on wide viewports, full-width with 16px padding on narrow viewports
- No horizontal scrollbar at any viewport width
- Scroll container (parentRef) remains width: 100% — scrollbar tracks full viewport
- Print-only candidate header is also constrained to max-w-[1200px]
- npx tsc --noEmit exits 0 and npx vitest run exits with all tests passing
- User visually approves the layout in the checkpoint
</success_criteria>

<output>
Create `.planning/phases/21-layout-content-ordering/21-02-SUMMARY.md` when done
</output>
