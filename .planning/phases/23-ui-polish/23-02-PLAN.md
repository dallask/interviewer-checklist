---
phase: 23-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ResetConfirmDialog.tsx
  - src/components/ImportPreviewModal.tsx
  - src/components/ActionsGroup.tsx
autonomous: true
requirements:
  - POL-02
  - POL-03
  - POL-04
must_haves:
  truths:
    - "ResetConfirmDialog cancel button reads 'Keep data' (not 'Keep scores') and has an X icon"
    - "ResetConfirmDialog destructive button has a Trash2 icon before 'Reset all'"
    - "ImportPreviewModal cancel button reads 'Discard import' (not 'Cancel') and has an X icon"
    - "ImportPreviewModal confirm button reads 'Import' (not 'Confirm') and has a Download icon"
    - "ActionsGroup grid is grid-cols-2 gap-2 (not grid-cols-3 gap-1.5)"
    - "Each ActionsGroup button shows icon (w-4 h-4) and a text label span below it"
    - "btnBase and btnActive constants include transition-colors duration-150"
    - "All modified buttons in ResetConfirmDialog and ImportPreviewModal have transition-colors duration-150"
    - "No existing aria-label values change on ActionsGroup buttons"
  artifacts:
    - path: "src/components/ResetConfirmDialog.tsx"
      provides: "Keep data / Trash2 Reset all buttons with icons (POL-02 per D-05, D-06)"
      contains: "lucide-react"
    - path: "src/components/ImportPreviewModal.tsx"
      provides: "Discard import / Download Import buttons with icons (POL-02 per D-05, D-06)"
      contains: "lucide-react"
    - path: "src/components/ActionsGroup.tsx"
      provides: "Two-column labeled grid with smaller icons (POL-03 per D-07 through D-11)"
      contains: "grid-cols-2"
  key_links:
    - from: "ActionsGroup.tsx btnBase constant"
      to: "all 9 action buttons"
      via: "className={btnBase} applied uniformly"
      pattern: "grid-cols-2"
    - from: "ResetConfirmDialog.tsx cancel button"
      to: "user-visible text"
      via: "label change to Keep data"
      pattern: "Keep data"
    - from: "ImportPreviewModal.tsx cancel button"
      to: "user-visible text"
      via: "label change to Discard import"
      pattern: "Discard import"
---

<objective>
Apply POL-02 (modal button icons) and POL-04 (hover transition-colors) to ResetConfirmDialog and ImportPreviewModal, and apply POL-03 (two-column labeled grid) and POL-04 to ActionsGroup.

Purpose: ResetConfirmDialog and ImportPreviewModal cancel button labels are misleading in the current codebase ("Keep scores", "Cancel") — this plan also corrects them per the copywriting contract in 23-UI-SPEC.md. ActionsGroup shifts from 3-column icon-only grid to 2-column grid with visible labels, improving discoverability of each action.

Output: Three modified files — two dialogs with icon-annotated and correctly labeled buttons, and a redesigned action panel grid.
</objective>

<execution_context>
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/workflows/execute-plan.md
@/Users/dallask/Projects/dallask/interviewer-checklist/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-ui-polish/23-CONTEXT.md
@.planning/phases/23-ui-polish/23-UI-SPEC.md
@src/components/ResetConfirmDialog.tsx
@src/components/ImportPreviewModal.tsx
@src/components/ActionsGroup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ResetConfirmDialog and ImportPreviewModal with icon buttons and corrected labels (POL-02 + POL-04 per D-05, D-06, D-12, D-13)</name>
  <files>src/components/ResetConfirmDialog.tsx, src/components/ImportPreviewModal.tsx</files>
  <action>
    ResetConfirmDialog.tsx:

    1. Add Lucide imports at the top (after existing React import):
       Import `Trash2` and `X` from `lucide-react`.

    2. "Keep scores" cancel button (line 85–89):
       - Change button text from "Keep scores" to "Keep data" (per 23-UI-SPEC.md copywriting contract).
       - Add `flex items-center gap-2 transition-colors duration-150` to the existing className string.
       - Insert `<X className="w-4 h-4" aria-hidden="true" />` as first child before "Keep data" (per D-05, D-12, D-13).

    3. "Reset" destructive button (line 90–99):
       - Change button text from "Reset" to "Reset all" (per 23-UI-SPEC.md copywriting contract).
       - Add `flex items-center gap-2 transition-colors duration-150` to the existing className string.
       - Insert `<Trash2 className="w-4 h-4" aria-hidden="true" />` as first child before "Reset all" (per D-05, D-06, D-12, D-13).

    Do NOT change: handleKeep, handleReset, storageAdapter.snapshot logic, focus trap, aria-labelledby, or dialog structure.

    ---

    ImportPreviewModal.tsx:

    1. Add Lucide imports at the top (after existing React import):
       Import `Download` and `X` from `lucide-react`.

    2. "Cancel" button (line 154–160):
       - Change button text from "Cancel" to "Discard import" (per 23-UI-SPEC.md copywriting contract and objective constraint).
       - Add `flex items-center gap-2 transition-colors duration-150` to the existing className string.
       - Insert `<X className="w-4 h-4" aria-hidden="true" />` as first child before "Discard import" (per D-05, D-12, D-13).

    3. "Confirm" / "Importing…" primary button (line 161–169):
       - Change button text from "Confirm" to "Import" (when not pending) and keep "Importing…" for pending state — update the conditional: `{isPending ? 'Importing…' : 'Import'}`.
       - Add `flex items-center gap-2 transition-colors duration-150` to the existing className string.
       - Insert `<Download className="w-4 h-4" aria-hidden="true" />` as first child (before the conditional text). The icon shows regardless of pending state (per D-05, D-12, D-13).

    Do NOT change: session mode toggle buttons (New session / Overwrite active session) — these are not dialog-level action buttons (per D-04 scope). Do NOT change: overwriteActive state, isPending guard, onConfirm handler, focus trap, or any preview display logic.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -c "Keep data" src/components/ResetConfirmDialog.tsx && grep -c "Discard import" src/components/ImportPreviewModal.tsx && grep -c "lucide-react" src/components/ResetConfirmDialog.tsx && grep -c "lucide-react" src/components/ImportPreviewModal.tsx && npx vitest run src/components/ResetConfirmDialog.test.tsx src/components/ImportPreviewModal.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    ResetConfirmDialog.tsx: "Keep data" with X icon, "Reset all" with Trash2 icon, transition-colors on both, lucide-react imported.
    ImportPreviewModal.tsx: "Discard import" with X icon, "Import"/"Importing…" with Download icon, transition-colors on both, lucide-react imported.
    All tests pass (if tests check for old text like "Keep scores" or "Cancel", update those test assertions to match the new labels — this is expected because the copy changed).
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert ActionsGroup to two-column labeled grid (POL-03 + POL-04 per D-07 through D-11, D-12, D-13)</name>
  <files>src/components/ActionsGroup.tsx</files>
  <action>
    Make the following targeted changes to ActionsGroup.tsx:

    1. Replace btnBase constant (line 158–159) with:
       `'flex flex-col items-center gap-1 px-2 py-2 text-[10px] text-gray-900 dark:text-gray-100 bg-gray-100 dark:bg-gray-700 hover:bg-gray-200 dark:hover:bg-gray-600 rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none transition-colors duration-150'`
       (per D-08, D-09, D-12, D-13 — removes `p-2 min-h-[36px] min-w-[36px] text-[13px]`, adds flex-col layout, 10px text, transition)

    2. Replace btnActive constant (line 160–161) with:
       `'flex flex-col items-center gap-1 px-2 py-2 text-[10px] rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 transition-colors duration-150'`
       (per D-09, D-12, D-13)

    3. Grid container (line 171): Change `grid grid-cols-3 gap-1.5` to `grid grid-cols-2 gap-2` (per D-07).

    4. For each of the 9 action buttons, change the icon size from `w-5 h-5` to `w-4 h-4`, and add a `<span className="truncate">Label</span>` immediately after the icon. Labels per D-10:
       - RefreshCw button → label: "Sessions"
       - Bot button → label: "AI Prompt"
       - Sun/Moon dark-mode toggle → label: "Light" (Sun) / "Dark" (Moon) — the conditional must cover both icon and label, or use a single label that reads the current state. Implementation: since the button toggles dark mode and shows either Sun or Moon, use a single conditional for the full button content: when darkMode is true show `<Sun w-4 h-4 /><span>Light</span>`, when false show `<Moon w-4 h-4 /><span>Dark</span>`.
       - ChevronsUpDown button → label: "Expand"
       - ChevronsLeftRight button → label: "Collapse"
       - Eye button → label: "Hide Done"
       - Download (import) button → label: "Import"
       - Upload (export) button → label: "Export"
       - Trash2 (reset) button → label: "Reset"

    5. Reset button inline className (line 269): Replace `p-2 min-h-[36px] min-w-[36px] text-[13px] text-red-600 ...` with:
       `'flex flex-col items-center gap-1 px-2 py-2 text-[10px] text-red-600 dark:text-red-400 bg-gray-100 dark:bg-gray-700 hover:bg-red-50 dark:hover:bg-red-900/20 rounded focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:outline-none transition-colors duration-150'`
       (per D-09, D-11, D-12, D-13 — Reset uses inline className, not btnBase, because it has a red color variant)

    Do NOT change: all aria-label values (tests query by these), all id attributes, all onClick handlers, the hidden file input element, importError display, the SessionSwitcherModal / ResetConfirmDialog / AiPromptModal / ImportPreviewModal render at the bottom. Do NOT add new Lucide icon imports — ActionsGroup already imports all needed icons.

    IMPORTANT: Verify lucide-react imports at the top of ActionsGroup.tsx already include all 9 icons used (Bot, ChevronsLeftRight, ChevronsUpDown, Download, Eye, Moon, RefreshCw, Sun, Trash2, Upload). No new imports needed.
  </action>
  <verify>
    <automated>cd /Users/dallask/Projects/dallask/interviewer-checklist && grep -c "grid-cols-2" src/components/ActionsGroup.tsx && grep -c "truncate" src/components/ActionsGroup.tsx && grep -c "transition-colors" src/components/ActionsGroup.tsx && npx vitest run src/components/ActionsGroup.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    ActionsGroup.tsx: grid-cols-2 gap-2, btnBase and btnActive updated with flex-col layout and transition-colors, all 9 buttons have w-4 h-4 icon and a truncate label span, Reset button inline className updated. All ActionsGroup tests pass (aria-labels are unchanged so existing test queries remain valid).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component props → render | All changes are purely presentational — button text labels, icon insertions, className edits. No data, auth, or storage paths affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering | ResetConfirmDialog label change | accept | Label change from "Keep scores" to "Keep data" is cosmetic; the underlying handleKeep handler is unchanged |
| T-23-04 | Tampering | ImportPreviewModal label change | accept | Label change from "Cancel"/"Confirm" to "Discard import"/"Import" is cosmetic; handlers are unchanged |
| T-23-05 | Tampering | ActionsGroup grid restructure | accept | All aria-labels preserved; no handler logic changes; purely visual restructuring |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — lucide-react already in package.json; no installs required |
</threat_model>

<verification>
After both tasks complete:
- `grep "grid-cols-2" src/components/ActionsGroup.tsx` returns a match
- `grep "Keep data" src/components/ResetConfirmDialog.tsx` returns a match
- `grep "Discard import" src/components/ImportPreviewModal.tsx` returns a match
- `grep -c "transition-colors" src/components/ActionsGroup.tsx` returns at least 3 (btnBase, btnActive, Reset inline)
- `npx vitest run src/components/ActionsGroup.test.tsx src/components/ResetConfirmDialog.test.tsx src/components/ImportPreviewModal.test.tsx` all pass
</verification>

<success_criteria>
1. ResetConfirmDialog: "Keep data" button with X icon, "Reset all" button with Trash2 icon, both with transition-colors duration-150
2. ImportPreviewModal: "Discard import" with X icon, "Import" with Download icon, both with transition-colors duration-150
3. ActionsGroup: 2-column grid, btnBase/btnActive use flex-col layout with 10px text and transition-colors, all 9 buttons display w-4 h-4 icon + truncate label, Reset inline className updated
4. All existing tests for these three components pass (update test assertions for label copy changes as needed)
</success_criteria>

<output>
Create `.planning/phases/23-ui-polish/23-02-SUMMARY.md` when done
</output>
